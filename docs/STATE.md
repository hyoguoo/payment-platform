# 현재 작업 상태

> 최종 수정: 2026-04-24 (T-B1 GREEN — PaymentConfirmResultUseCase.handleFailed FailureCompensationService.compensate(orderId, productId, qty) 경유로 실 qty 전달 완료. 전수 288 PASS. T-B2 진입 가능)

## 활성 작업
- **주제**: PRE-PHASE-4-HARDENING
- **단계**: execute
- **이슈**: #69
- **브랜치**: `#69`
- **활성 태스크**: T-B2 (T-B1 완료)
- **비고**: 하드닝 시작 — baseline critical 4·major 8·minor 5 → 19 태스크. **Phase 3.5 연속 완료(2026-04-24, 커밋 보류) — T3.5-08(PgEventPublisher 동기화 버그 수정: whenComplete fire-and-forget → .get(timeout) 동기, PgEventPublisherTest 4케이스 신규) / T3.5-09(StockCommit/StockRestore consumer groupId 분리: product-service-stock-commit + product-service-stock-restore, ARCHITECTURE.md Consumer Group 정책 섹션 신설) / T3.5-10(ProductHttpAdapterContractTest + UserHttpAdapterContractTest 각 4케이스: 404/503/429/500 분기 계약 고정) / T3.5-11(FakePgGatewayStrategy 신설 — pg-service/src/main/infrastructure/gateway/fake/ @ConditionalOnProperty pg.gateway.type=fake, PostConstruct 경고 배너. application-smoke.yml + docker-compose.smoke.yml + scripts/phase-gate/phase-3-integration-smoke.sh + docs/phase-gate/phase-3-integration-smoke.md — 시나리오 A/D/B/C/E/F/H, 해피 패스만 검증) / T3.5-12(PgOutboxImmediateWorkerTest exactly-once 케이스를 @RepeatedTest(50)으로 확장, 50+1=51 PASS) / T3.5-Gate(scripts/phase-gate/phase-3-5-gate.sh + phase-3-5-gate.md — 전수 test + @Lazy 0건 + matchIfMissing=true 의도적 2건 + groupId 분리 + 동기 publisher + HTTP 계약 + RepeatedTest 7섹션 전부 PASS). **코드 클렌징**: LogFmt.debug 메서드 5개 LogFmt 파일 모두 추가,  13개 파일 평문 log.* → LogFmt 치환. EventType enum 15종 추가(KAFKA_PUBLISH_SUCCESS, PAYMENT_CONFIRM_RESULT_*, PRODUCT/USER_SERVICE_* 등). ARCHITECTURE.md Cross-Context Communication 섹션 MSA 기준으로 갱신 (InternalProductAdapter 등 구버전 레거시 참조 제거). ./gradlew test 전체 461/461 PASS(gateway 3 + payment-service 279 + pg-service 151 + product-service 26 + user-service 1 + eureka 1). 회귀 없음. T3.5-06 완료(2026-04-24) — LogFmt/LogDomain/EventType을 pg-service(50 EventType)/product-service/user-service/gateway 4개 서비스에 ADR-19 복제(b) 방침으로 독립 복사. commons-lang3 미사용 → normalizeSpace 인라인 구현. pg-service TODO 주석 4건 제거 + 4개 서비스 평문 log.* 전수 교체. logback-spring.xml(traceId MDC) 4개 서비스 신설. ./gradlew test 전체 PASS.** **T3.5-05 완료(2026-04-24) — PgGatewayPort 분해(PgStatusLookupPort+PgConfirmPort 신설) + PgGatewayPort 삭제. DuplicateApprovalHandler @Lazy 제거 + PgStatusLookupPort 의존. TossPaymentGatewayStrategy·NicepayPaymentGatewayStrategy·PgVendorCallService·PgFinalConfirmationGate 신 포트로 완전 이전. FakePgGatewayAdapter 두 포트 구현. DuplicateApprovalHandlerCircularDependencyTest 4케이스 신규. 98/98 PASS, 전체 회귀 없음.** **T3.5-04 완료(2026-04-24) — phase-gate 문서(phase-0-gate.md/phase-1-gate.md) 구버전 Redis 컨테이너명 잔재(redis-payment-proxy→redis-stock-proxy, payment-redis-payment→payment-redis-stock) 치환. product-service application.yml 주석 정리. CONVENTIONS.md 환경변수 네이밍 규약 섹션 신설(Redis dedupe/stock 분리 + MySQL 서비스별 컨테이너 명명). Java 소스·compose·스크립트 구버전 잔재 0건. 전체 회귀 없음.** **T3.5-03 완료(2026-04-24) — product-service `eventUuid` → `eventUUID` 전수 치환(12파일). EventDedupeStore 포트·Fake·UseCase·Consumer·DTO·테스트 일괄 변경. StockRestoreMessage record 필드명 `eventUUID` + @JsonProperty("eventUUID") 추가(payment-service 발행 키 정합). 26/26 PASS, 회귀 없음.** **T3.5-02 완료(2026-04-24) — StockCommitConsumer·StockRestoreConsumer(product-service)·ConfirmedEventConsumer(payment-service) matchIfMissing=true 제거. application-test.yml spring.kafka.listener.auto-startup=false 추가. CONVENTIONS.md @ConditionalOnProperty 규약 섹션 추가. 514/514 PASS, 회귀 없음.** **T3.5-01 완료(2026-04-24) — PaymentGateway 참조 18파일 전수 재평가. 삭제 0건(모든 참조 실사용 경로 확인). PaymentGatewayType enum + PaymentEventEntity.gatewayType 경계 주석 추가. 273/273 PASS, 전체 회귀 없음.** **서브도메인 REST 오류 응답 정규화(2026-04-22) — 실기동 테스트에서 /api/v1/products/{id} 404 케이스가 product-service 기본 500 경로로 떨어져 긴 stacktrace 덤프 + payment-service 쪽 500 승격으로 이어지던 문제 일괄 수습. (a) product-service: `@RestControllerAdvice GlobalExceptionHandler` 신설 — `ProductNotFoundException` → 404(WARN, 스택 없이), 기타 `RuntimeException` → 500. `product.presentation.dto.ErrorResponse` 공통 DTO. (b) user-service: 동일 패턴 신설 — `UserNotFoundException` → 404, 예기치 못한 오류 → 500. (c) product-service V4 시드 `INSERT IGNORE INTO product/stock (1, 100)`, user-service V2 시드 `INSERT IGNORE INTO user (1, smoke@test.com)` — 시드 부재로 테스트마다 404 뜨던 근본 원인 해결. (d) payment-service `ProductHttpAdapter.mapResponseException` 분기 확장: 404 → `ProductFoundException.of(PRODUCT_NOT_FOUND)` (payment-service `ProductExceptionHandler`가 404로 매핑), 503/429 → `ProductServiceRetryableException` (기존), 나머지 → `IllegalStateException`. 모든 분기에서 WARN 레벨 단일 라인 로그. (e) `UserHttpAdapter.mapResponseException` 동일 패턴: 404 → `UserFoundException` (기존 `UserExceptionHandler`가 404 처리), 503/429 → `UserServiceRetryableException`. (f) `ProductExceptionHandler`에 `ProductFoundException` → 404 분기 추가. pg-service는 REST 컨트롤러 없음(Kafka-first)이므로 스킵. 검증: `./gradlew test` 전체 PASS(eureka 1+gateway 3+payment-service 388+pg-service 95+product-service 26+user-service 1 = 514/514). 회귀 없음.** **docker/ 레거시 compose 축소(2026-04-22) — 모놀리식 시대 관측성 스택 전면 삭제. docker/compose/docker-compose.yml에서 elasticsearch·logstash·kibana·kibana-init·prometheus·grafana·cadvisor 서비스 + es-data·prometheus-data·grafana-data 볼륨 제거. mysql+app만 남김(k6 벤치마크 경로가 참조하므로 container_name payment-mysql/payment-app 및 network payment-network는 유지, cAdvisor 8090 포트 충돌 해소로 MSA Gateway 8090과 혼선 방지). 하위 디렉토리 kibana/·logstash/·grafana/·prometheus/ 일괄 삭제. scripts/run.sh services 배열 축소(mysql+app만). README 포트 표·URL 표에서 ELK/Prometheus/Grafana 행 제거 + 관측성은 docker-compose.observability.yml로 안내. .claude/skills/k6-benchmark/SKILL.md 서비스 상태 출력 항목 축소. ELK 필요 시 재작성 전제. docker-compose.yml 헤더에 "MSA 스택과 동시 기동 금지" 경고 추가. 검증: compose file YAML 파싱 유효성만 — k6 경로 실행은 사용자 요청 시점까지 보류.** **인프라 네이밍 일관화(2026-04-22) — 역할 기반 컨테이너·볼륨·환경변수·Java 빈 명칭 전면 정리. (a) infra compose: `redis` → `redis-dedupe`(dedupe 키 전용, AOF off), `redis-payment` → `redis-stock`(재고 캐시, AOF on everysec), `mysql` → `mysql-payment` — 각 container_name·volume·env var도 동반 개명(MYSQL_PAYMENT_*, SPRING_DATA_REDIS_HOST). (b) 사용 안 하던 `mysql-payment` 빈 스캐폴드(포트 3307) 제거, `toxiproxy`는 `docker-compose.chaos.yml`로 opt-in 분리(Phase 4 T4-01 실행 시만 기동). (c) payment-service `application.yml` `REDIS_PAYMENT_HOST` 치환 → 표준 `SPRING_DATA_REDIS_HOST`. (d) product-service Java 클래스 개명: `PaymentRedisStockAdapter` → `StockRedisAdapter`, `RedisPaymentConfig` → `StockRedisConfig`, 빈명 `paymentRedisTemplate/paymentRedisConnectionFactory` → `stockRedisTemplate/stockRedisConnectionFactory`, 프로퍼티 키 `product.cache.payment-redis.*` → `product.cache.stock-redis.*`. (e) `chaos/toxiproxy-config.json` proxy 이름(mysql-payment-proxy/redis-stock-proxy) + upstream 경로 갱신, `chaos/README.md` 표 정합성 복구. (f) `scripts/compose-up.sh` 인프라 대기 목록 업데이트(payment-redis-dedupe·payment-redis-stock·payment-mysql-payment), phase-gate 스크립트(0·1·3) 컨테이너 참조 동기화. 검증: `./gradlew :product-service:test :payment-service:test` 391/391 PASS, `scripts/compose-up.sh --skip-build` 5개 앱 healthy + Eureka 5/5 UP, POST `/api/v1/payments/checkout` → 201 Created 재확인.** **T3-Integration-Closure-2 완료(2026-04-22) — compose-up 기동까지 healthy였으나 실제 checkout 호출 시 드러난 갭 일괄 해소. 해소 항목: (1) product-service REST 프레젠테이션 층 전면 신설(ProductController + ProductQueryUseCase + StockCommandUseCase + ProductQueryService/StockCommandService 인바운드 포트 + ProductResponse/StockCommandItem DTO + ProductRepository 아웃바운드 포트 + JpaProductRepository/ProductRepositoryImpl 어댑터 + ProductEntity JPA 매핑 + ProductNotFoundException) — payment-service ProductHttpAdapter가 찌르는 /api/v1/products/{id}·/stock/{decrease,increase} 엔드포인트 부재로 404가 나던 문제 해결, (2) product-service StockSnapshotPublisher JSON 사전 직렬화(ObjectMapper.writeValueAsString로 Map<productId,quantity,capturedAt> 빌드 후 발행) — Kafka 기본 StringSerializer 유지하면서 SerializationException 해소, (3) payment-service StockSnapshotWarmupConsumer 파라미터 타입 StockSnapshotEvent → String 변경 + ObjectMapper.readValue 수동 역직렬화 — @KafkaListener record 바인딩 실패(MessageConversionException) 해소, (4) payment-service spring.application.name `payment-platform` → `payment-service`로 교정 — Gateway `lb://payment-service` 라우팅 503 해소, (5) product-service JpaStockRepository.findByProductIdPessimistic(@Lock PESSIMISTIC_WRITE) 추가 — StockCommandUseCase 기반 동기 재고 증감 동시성 방어 토대, (6) ProductServiceApplicationTest @MockitoBean ProductRepository 추가 — smoke context 재통과. 검증: `./gradlew :product-service:test :payment-service:test` 391/391 PASS, `scripts/compose-up.sh` 5개 앱 healthy + Eureka 5/5 UP(GATEWAY·PAYMENT-SERVICE·PG-SERVICE·PRODUCT-SERVICE·USER-SERVICE), E2E POST /api/v1/payments/checkout {userId:1, gatewayType:TOSS, orderedProductList:[{productId:1,quantity:1}]} → 201 Created {orderId, totalAmount:1000.00}.** **T3-Integration-Closure 완료(2026-04-22) — 실제 docker compose 스택 기동 시점에 드러난 Phase 3 배선 갭 9종 일괄 해소. T3-Gate(2026-04-21)는 단위 테스트 기반 검증이었고, compose-up 실행에서만 드러나는 production 전용 빈 미구현·DI 순환·Flyway MySQL 문법 호환·Kafka 토픽 누락 문제를 별도 묶음으로 처리. 해소 항목: payment-service StockRestore Kafka publisher + EventDedupeStore Redis 어댑터, pg-service Toss/NicePay `@ConditionalOnProperty` 분기 + DuplicateApprovalHandler `@Lazy` 순환 차단 + PgInbox/PgOutbox JPA 어댑터(CAS 4종 + SELECT FOR UPDATE + available_at 배치 조회) + EventDedupeStore Redis 어댑터(+build.gradle spring-data-redis 의존성), product-service StockRepository JPA 어댑터 + V2 Flyway `ADD INDEX` MySQL 8.0 문법 수정, user-service UserRepository JPA 어댑터. 인프라: docker-compose.apps.yml pg-service에 redis 의존+`SPRING_DATA_REDIS_HOST` 주입, scripts/phase-gate/create-topics.sh에 `stock.events.restore` 추가. 검증: `./gradlew test` 전체 PASS(pg 95·product 26·기타 회귀 없음), `scripts/compose-up.sh` 5개 앱 healthy + Eureka 5/5 UP, pg-service actuator redis 7.2.13 UP + consumer 파티션 할당, gateway → user-service JPA → MySQL 200 OK 검증, InternalOnlyGatewayFilter 403 유지.** **T3-Gate 완료(2026-04-21) — scripts/phase-gate/phase-3-gate.sh 신설(executable). docs/phase-gate/phase-3-gate.md 신설. 검증 10섹션(pre~j): pre(516건)/a(product-service health)/b(user-service health)/c(Gateway 라우트 T3-07)/d(stock-snapshot 토픽)/e(StockCommit dedupe 4케이스)/f(StockRestore dedupe 5케이스 불변식 14)/g(Redis SET 단위 테스트)/h(FailureCompensationService 2케이스)/i(ProductHttpAdapter Strangler Vine 2케이스)/j(516건 이상). Phase 3 ADR 확정: ADR-22/ADR-16/ADR-14/ADR-02. 516/516 PASS, 회귀 없음.** **T3-07 완료(2026-04-21) — gateway/src/main/resources/application.yml에 products-service-route(lb://product-service, /api/v1/products/**) + users-service-route(lb://user-service, /api/v1/users/**) 추가. Eureka lb 경유, StripPrefix=0. /internal/** 경로 충돌 없음. 516/516 PASS, 회귀 없음.** **T3-06 완료(2026-04-21) — ProductHttpAdapter(@ConditionalOnProperty product.adapter.type=http) + UserHttpAdapter(@ConditionalOnProperty user.adapter.type=http) 신설. HttpOperator(WebClient 기반 기존 패턴) 재사용. HTTP 503/429/연결 타임아웃 → ProductServiceRetryableException/UserServiceRetryableException(RuntimeException). InternalProductAdapter/InternalUserAdapter(infrastructure/internal/)는 matchIfMissing=true로 기본 유지. application.yml base-url + adapter.type 환경변수 추가. PaymentErrorCode E03031/E03032 추가. ProductHttpAdapterTest 2케이스 GREEN. 516/516 PASS, 회귀 없음.** **T3-05 완료(2026-04-21) — StockRestoreCommandService 시그니처 확장(orderId, eventUuid, productId, qty). EventDedupeStore 포트에 existsValid(TTL-aware 중복 확인) 추가. FakeEventDedupeStore existsValid 구현. StockRestoreUseCase(@Transactional): existsValid 선호출 → 재고 조회·증가·save → recordIfAbsent(성공 후 dedupe 기록) — 불변식 14 충족. StockRestoreMessage record + StockRestoreConsumer(@KafkaListener stock.events.restore, groupId=product-service, matchIfMissing=true) 신설. JdbcEventDedupeStore: existsValid + recordIfAbsent(DELETE만료행+INSERT IGNORE) — V1 product_event_dedupe 테이블 사용. V2 마이그레이션: expires_at 인덱스 보강만. ProductTopics.STOCK_EVENTS_RESTORE 추가. TTL=8일. 5케이스 GREEN. 514/514 PASS(eureka 1+gateway 3+payment-service 388+pg-service 95+product-service 26+user-service 1), 회귀 없음.** **T3-04b 완료(2026-04-21) — FailureCompensationService(orderId+productId 기반 결정론적 UUID v3 생성, StockRestoreEventPublisherPort.publishPayload 경유) + StockRestoreEventPayload record(eventUUID·orderId·productId·qty) 신설. StockRestoreEventPublisherPort에 publishPayload 메서드 추가. FakeStockRestoreEventPublisher UUID 멱등 시뮬레이션 확장. FailureCompensationServiceTest 2케이스(whenFailed_ShouldEnqueueStockRestoreCompensation/whenFailed_IdempotentWhenCalledTwice) GREEN. 509/509 PASS(eureka 1+gateway 3+payment-service 388+pg-service 95+product-service 21+user-service 1), 회귀 없음.** **T3-04 완료(2026-04-21) — StockCommitUseCase(application/usecase, @Transactional): commit(eventUuid, orderId, productId, qty, expiresAt) — EventDedupeStore dedupe(1단) → RDB UPDATE(StockRepository.findByProductId+qty감소+save) → RDB 성공 후에만 PaymentStockCachePort.setStock(원자성). RDB 실패 시 IllegalStateException throw, Redis SET 미호출. StockCommitConsumer(@KafkaListener payment.events.stock-committed, groupId=product-service, matchIfMissing=true): StockCommittedMessage → usecase 위임. PaymentRedisStockAdapter(@ConditionalOnProperty product.cache.payment-redis.host): paymentRedisTemplate(@Qualifier) → SET "stock:{productId}" TTL=24h. RedisPaymentConfig(LettuceConnectionFactory + paymentRedisTemplate 빈). V3__add_stock_commit_dedupe.sql 신설. application.yml: spring.data.redis + product.cache.payment-redis 설정 추가. ProductServiceApplicationTest: @MockitoBean PaymentStockCachePort + spring.kafka.listener.auto-startup=false 추가. StockCommitUseCaseTest 3케이스(TC1 RDB+Redis 순서/TC2 dedupe noOp/TC3 RDB실패 Redis미호출) + StockCommitConsumerTest 1케이스(TC4 usecase 위임) GREEN. 507/507 PASS(eureka 1+gateway 3+payment-service 386+pg-service 95+product-service 21+user-service 1), 회귀 없음.** **T3-03 완료(2026-04-21) — PaymentStockCachePort 인터페이스 main source 신설(application/port/out). FakeStockRepository(ConcurrentHashMap, increment/decrement 원자 헬퍼) + FakeEventDedupeStore(Clock 주입, TTL 만료 시뮬레이션) + FakePaymentStockCachePort(SetRecord 이력 + AtomicInteger 호출 횟수) test source 신설. 스모크 테스트 16케이스(FakeStockRepositoryTest 5 + FakeEventDedupeStoreTest 4 + FakePaymentStockCachePortTest 6 + contextLoads 1) GREEN. 503/503 PASS(eureka 1+gateway 3+payment-service 386+pg-service 95+product-service 17+user-service 1), 회귀 없음.** **T3-02 완료(2026-04-21) — user-service 모듈 신설(settings.gradle include 추가). User 도메인 이관(id/email/createdAt, 삭제 T3-06). UserRepository 포트. UserQueryUseCase(queryById: UserNotFoundException). UserController(GET /api/v1/users/{id}). Flyway V1__user_schema.sql. UserServiceApplicationTest 스모크 1케이스 GREEN. 487/487 PASS(eureka 1+gateway 3+payment-service 386+pg-service 95+product-service 1+user-service 1), 회귀 없음.** Phase 2 완료, Phase 3 진행 중. T3-01 완료(2026-04-21) — product-service 모듈 신설(settings.gradle include 추가). Product/Stock 도메인 이관(복사, 삭제 T3-06). StockRepository/EventDedupeStore 포트. StockRestoreUseCase 스캐폴드(T3-05 완성 예정). Flyway V1(product/stock/product_event_dedupe). ProductTopics+KafkaTopicConfig+StockSnapshotPublisher(ApplicationReadyEvent warmup pair). VT application.yml port=8083. 스모크 테스트 1케이스 GREEN. 486/486 PASS(eureka 1+gateway 3+payment-service 386+pg-service 95+product-service 1), 회귀 없음.** Phase-2-Gate 완료(2026-04-21) — scripts/phase-gate/phase-2-gate.sh 신설(executable). docs/phase-gate/phase-2-gate.md 신설. 검증 10섹션(pre~j): clean test 484건 이상/Sub-Gate 3종 위임/pg-service health/PaymentConfirmConsumerTest 5케이스+PaymentConfirmDlqConsumerTest 4케이스+ConfirmedEventConsumerTest 5케이스/eventUUID dedupe/PgVendorCallServiceTest+PgFinalConfirmationGateTest/DuplicateApprovalHandlerTest+PgInboxAmountStorageTest/토픽 파티션 동일(불변식 6b)/InternalOnlyGatewayFilterTest+실제 HTTP/PgStatusAbsenceContractTest. 485/485 PASS(gateway 3+payment-service 387+pg-service 95), 회귀 없음. Phase 2 완료(ADR-21·ADR-02·ADR-30·ADR-15·ADR-05 불변 확정). **T2d-03 완료(2026-04-21) — InternalOnlyGatewayFilter(GlobalFilter, HIGHEST_PRECEDENCE+1) 신설: /internal/** 403 차단. application.yml block-internal 라우트(uri=no://op, SetStatus:403) 추가. ADR-21/ADR-02 적용. InternalOnlyGatewayFilterTest 2케이스 GREEN. 484/484 PASS(gateway 3 신규 포함), 회귀 없음.** **T2d-02 완료(2026-04-21) — ADR-12 결론(네이밍 규약 `<source-service>.<type>.<action>[.modifier]` + 토픽 목록표 6종) + ADR-31 결론(관측 지표 4종 + 알림 4종) MSA-TRANSITION.md 기록. PaymentOutboxRepository 포트 확장(countPending/countFuturePending/findOldestPendingCreatedAt). PgOutboxRepository 포트 확장. PaymentOutboxMetrics(Gauge 3종+DistributionSummary, @Scheduled 60s, @ConditionalOnProperty scheduler.enabled). PgOutboxMetrics(동일 구조, Clock 주입). PgServiceConfig(Clock Bean + @EnableScheduling). chaos/grafana/payment-dashboard.json payment_outbox 패널 4개 + alerting 4종 추가. ProductTopics.java 실파일 생성 스킵(product-service T3-01에서 신설 예정 — ADR-12 토픽 목록표 예고 기록). PaymentOutboxMetricsTest 2케이스 + PgOutboxMetricsTest 2케이스 GREEN. 481/481 PASS(payment-service 386+pg-service 95), 회귀 없음.** T2d-01 완료(2026-04-21) — ConfirmedEventConsumer(@KafkaListener payment.events.confirmed, groupId=payment-service, matchIfMissing=true). PaymentConfirmResultUseCase: eventUUID dedupe(EventDedupeStore) → 3-way 분기(APPROVED→done+StockCommit/FAILED→fail+StockRestore/QUARANTINED→QuarantineCompensationHandler.handle FCG). 포트 신설: EventDedupeStore(ADR-30 독립 복제)/StockRestoreEventPublisherPort. ConfirmedEventConsumerTest 5케이스 GREEN. 477/477 PASS(payment-service 384+pg-service 93), 회귀 없음. T2c-Gate 완료(2026-04-21) — scripts/phase-gate/phase-2c-gate.sh 신설(executable). docs/phase-gate/phase-2c-gate.md 신설. 검증 7섹션: Phase 2.b 전제 확인, payment-service cutover(PgStatusAbsenceContractTest 3케이스+소스 부재), pg.retry.mode=outbox 설정, consumer group(pg-service·pg-service-dlq), Kafka E2E(PaymentConfirmConsumerTest 5케이스+PaymentConfirmDlqConsumerTest 4케이스), 잔존 삭제 코드 부재, 전체 Gradle test 472건. Phase 2.c 완료, Phase 2.d 진행 중.** T2c-02 완료(2026-04-21) — payment-service PG 직접 호출 잔존 코드 전면 삭제: 구버전 PaymentGatewayPort(getStatus/getStatusByOrderId), InternalPaymentGatewayAdapter(infrastructure/internal), PaymentCommandUseCase.confirmPaymentWithGateway/getPaymentStatusByOrderId, PaymentGatewayStrategy 인터페이스 getStatus/getStatusByOrderId, TossPaymentGatewayStrategy·NicepayPaymentGatewayStrategy 관련 구현 전부 삭제. PgStatusAbsenceContractTest 3케이스(불변식 19) GREEN. 472/472 PASS(payment-service 379 + pg-service 93), 회귀 없음. 삭제 테스트 16건. ADR-02/ADR-21 불변 확정. T2c-01 완료(2026-04-21) — pg-service/src/main/resources/application.yml 신설(server.port=8082, pg.retry.mode=outbox, spring.kafka.bootstrap-servers 환경변수, PG datasource/flyway, actuator/prometheus 노출). OutboxProcessingService PG 직접 호출 경로는 T1-11c에서 이미 삭제 완료 확인(scheduler/ 패키지 부재). 488/488 PASS(payment-service 395 + pg-service 93), 회귀 없음. Phase 2.c 진행 중.** T2b-Gate 완료(2026-04-21) — scripts/phase-gate/phase-2b-gate.sh 신설(executable). docs/phase-gate/phase-2b-gate.md 신설. 검증 항목 8섹션(a~h): Phase 2.a 전제 9항목(DLQ 토픽 payment.commands.confirm.dlq + pg-service-dlq consumer group 포함), DuplicateApprovalHandlerTest 6케이스(중복 승인 시나리오), PgFinalConfirmationGateTest 4케이스(FCG 불변: getStatus 1회+재시도 0회), PgVendorCallServiceTest 5케이스+RetryPolicyTest(ADR-30 available_at 지연), PaymentConfirmDlqConsumerTest 4케이스(QUARANTINED+no-op), AmountConverterTest+PgInboxAmountStorageTest(amount 규약)+DB smoke, 전체 Gradle test 488건. 488/488 PASS, 회귀 없음. Phase 2.b 완료, Phase 2.c 진행 중.** T2b-05 완료(2026-04-21) — DuplicateApprovalHandler(application/service) 신설: handleDuplicateApproval @Transactional — VendorQueryOutcome 캡슐화. vendor 조회 실패→QUARANTINED(VENDOR_INDETERMINATE). 경로(1) DB존재: amount일치→stored_status_result 재발행, 불일치→QUARANTINED+AMOUNT_MISMATCH. 경로(2) DB부재: amount일치→APPROVED+운영알림, 불일치→QUARANTINED+AMOUNT_MISMATCH. NicepayPaymentGatewayStrategy: DuplicateApprovalHandler 주입+handleDuplicateApproval(2201 분기) 추가. TossPaymentGatewayStrategy: DuplicateApprovalHandler 주입+ALREADY_PROCESSED_PAYMENT 분기 자리 주석. DuplicateApprovalHandlerTest 6케이스 GREEN. 전체 488/488 PASS(payment-service 395 + pg-service 93), 회귀 없음.** T2b-04 완료(2026-04-21) — AmountConverter(fromBigDecimalStrict: null/scale>0/음수 거부, longValueExact 반환) + PgInboxAmountService(불변식 4c 3경로: recordPayloadAmount/validateAndApprove/recordAndApproveDirect) 신설. PgInboxAmountStorageTest 4케이스 + AmountConverterTest 4케이스 GREEN. 전체 482/482 PASS(payment-service 395 + pg-service 87), 회귀 없음. T2b-03 완료(2026-04-21) — PgFinalConfirmationGate(performFinalCheck @Transactional) 신설. FCG 불변(ADR-15): getStatusByOrderId 1회만 호출, 재시도 래핑 금지. FcgOutcome 캡슐화(APPROVED/FAILED/INDETERMINATE 3-way). timeout·5xx → 무조건 QUARANTINED(FCG_INDETERMINATE). PgFinalConfirmationGateTest 4케이스 GREEN. 전체 479/479 PASS(payment-service 395 + pg-service 79), 회귀 없음. T2b-02 완료(2026-04-21) — PaymentConfirmDlqConsumer(groupId=pg-service-dlq, @KafkaListener(payment.commands.confirm.dlq)) + PgDlqService(@Transactional handle: FOR UPDATE 조회→terminal no-op(불변식 6c)→QUARANTINED CAS 전이+events.confirmed outbox INSERT→PgOutboxReadyEvent 발행) 신설. PgInboxRepository.transitToQuarantined/findByOrderIdForUpdate 추가. FakePgInboxRepository 확장. 전체 470/470 PASS(payment-service 395 + pg-service 75), 회귀 없음.** T2b-01 완료(2026-04-21) — PgVendorCallService(ADR-30) + RetryPolicy(base=2s/multi=3/attempts=4/jitter=±25%) 신설. GatewayOutcome 캡슐화로 try-catch 외부 변수 재할당 없이 성공/재시도/DLQ/확정실패 4경로 구현. PgInboxRepository transitToApproved/transitToFailed 추가. PgConfirmService callVendor() placeholder 교체 완료. 전체 464/464 PASS. Phase 2.b 진행 중. T2a-Gate 완료(2026-04-21) — scripts/phase-gate/phase-2a-gate.sh 신설(executable), docs/phase-gate/phase-2a-gate.md 신설. 검증 항목 9섹션: pg-service actuator/health, mysql-pg 컨테이너+pg DB 접속, Flyway V1(pg_inbox/pg_outbox), Kafka 토픽 존재, consumer group pg-service 등록, PgOutboxImmediateWorker SmartLifecycle 기동, pg_inbox NONE→IN_PROGRESS CAS 멱등성, pg.outbox.channel.* 메트릭, pg_outbox clean state. 포트 충돌 주의(기본 8080=Gateway) 가이드 명시. 418/418 회귀 없음. T2a-06 완료(2026-04-21) — EventDedupeStore 포트(markSeen) + FakeEventDedupeStore 신설. PgInboxRepository.transitNoneToInProgress(orderId, amount): boolean CAS 추가. FakePgInboxRepository putIfAbsent+compute 원자 전이. FakePgOutboxRepository id=null auto-increment 처리. PgConfirmService(application/service): handle(PgConfirmCommand) eventUUID dedupe(1단) → inbox 5상태 분기(2단): NONE→CAS+PG 호출, IN_PROGRESS→no-op, terminal→stored_status_result 재발행(벤더 호출 금지). PaymentConfirmConsumer(infrastructure/messaging/consumer): @KafkaListener(payment.commands.confirm, pg-service) + @ConditionalOnProperty. PaymentConfirmConsumerTest 5케이스 GREEN. 전체 418/418 PASS(payment-service 395 + pg-service 23). T2a-05c 완료(2026-04-21) — PgOutboxImmediateWorker(SmartLifecycle, getPhase=Integer.MAX_VALUE-100, Virtual Thread 워커 1개 기본, channel.take() 루프 → relayExecutor.submit → PgOutboxRelayService.relay, stop() running=false+interrupt+awaitTermination 10s, 생성자 주입 방식 workerCount=Spring @Value 또는 직접 지정), PgOutboxPollingWorker(@Scheduled fixedDelayString=2000ms, findPendingBatch→relay, Clock 주입). TDD GREEN: PgOutboxImmediateWorkerTest 2케이스(stop_DrainsInFlightBeforeShutdown, exactly-once 불변식 11). 전체 411/411 PASS(payment-service 395 + pg-service 16). T2a-05b 완료(2026-04-21) — PgOutboxReadyEvent(record, domain/event), PgOutboxChannel(LinkedBlockingQueue<Long> capacity=1024, isNearFull 80% 정수 연산, Micrometer 게이지 2종, @Slf4j 평문 로깅 — LogFmt T5-02 대상), OutboxReadyEventHandler(@TransactionalEventListener AFTER_COMMIT, @ConditionalOnProperty 미부여 T1-18 교훈). 스모크 테스트 PgOutboxChannelTest(5케이스)+OutboxReadyEventHandlerTest(3케이스). 전체 411/411 PASS. T2a-05a 완료(2026-04-21) — PgEventPublisherPort 시그니처 publish(topic,key,payload,headers)로 교체(ADR-30 대칭). PgOutboxRepository.findById(long) 추가. FakePgEventPublisher·FakePgOutboxRepository 업데이트. PgOutboxRelayService 신설(application/service, Clock 주입, relay(id): 이미처리·미래available_at skip·publish실패시row미갱신). PgEventPublisher 신설(infrastructure/messaging/publisher, @ConditionalOnProperty, KafkaTemplate ProducerRecord 헤더전달). PgOutboxRelayServiceTest 3케이스 PASS. 전체 401/401 회귀 없음. T2a-04 완료(2026-04-21) — pg-service/src/main/resources/db/migration/V1__pg_schema.sql 신설(pg_inbox 5상태 ENUM+amount BIGINT+stored_status_result+reason_code+ux/idx, pg_outbox available_at+attempt+processed_at+복합인덱스). PgInboxStatus는 T2a-03 기신설 enums/ 경로 유지(DDL enum 1:1 일치·TERMINAL SSOT). pg-service/build.gradle에 data-jpa+mysql-connector-j+flyway-core+flyway-mysql 추가(사전 승인). docker-compose.infra.yml에 mysql-pg 컨테이너(포트 3308)+mysql-pg-data 볼륨 추가. :pg-service:compileJava PASS, 398/398 회귀 없음, YAML 파싱 검증 통과. T2a-03 완료(2026-04-21) — PgInboxStatus(5상태)·PgInbox·PgOutbox plain POJO + PgInboxRepository·PgOutboxRepository 포트 인터페이스 신설(main source). FakePgGatewayAdapter(응답/예외 주입·호출 횟수 추적)·FakePgInboxRepository·FakePgOutboxRepository·FakePgEventPublisher(이벤트 캡처) test source 배치. 스모크 3케이스 PASS. :pg-service:compileJava PASS, 398/398 회귀 없음. T2a-02 완료(2026-04-21) — TossApiMetric(annotation)·ErrorCode(annotation)·TossApiMetrics·TossApiMetricsAspect를 pg.infrastructure.aspect.* 패키지에 신설. payment-service TossPaymentErrorCode 참조 제거(extractErrorCode Optional<String> 반환). pg-service/build.gradle에 spring-boot-starter-aop + spring-boot-starter-actuator 최소 추가. payment-service 원본 미수정(Phase 2.b/2.c 삭제 예정). :pg-service:compileJava PASS, 395/395 회귀 없음. T2a-01 완료(2026-04-21) — pg-service 신규 Spring Boot 모듈 스캐폴드. settings.gradle include 'pg-service' 추가. PgGatewayPort·PgEventPublisherPort outbound 포트, PgConfirmCommandService inbound 포트 선언. TossPaymentGatewayStrategy·NicepayPaymentGatewayStrategy 스켈레톤 이관(실제 HTTP 호출은 T2b-01). PgTopics·KafkaTopicConfig 독립 복제(ADR-30). 전 타입 payment-service 의존 없음. :pg-service:compileJava PASS, 395/395 회귀 없음. T0-01·T0-02·T0-03a/b/c·T0-04·T0-05·T0-Gate·T1-03·T1-04·T1-05·T1-06·T1-07·T1-08·T1-09·T1-10·T1-11a·T1-11b·T1-11c·T1-12 완료, T1-13 스킵, T1-14·T1-15·T1-16·T1-17·T1-18·T1-Gate·T2a-01·T2a-02·T2a-03·T2a-04·T2a-05a 완료. PLAN 66 태스크 · domain_risk 43건 · 의존 엣지 59개.

## 최근 완료
- **주제**: NICEPAY-PG-STRATEGY
- **완료일**: 2026-04-14
- **아카이브**: docs/archive/nicepay-pg-strategy/
