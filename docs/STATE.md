# 현재 작업 상태

> 최종 수정: 2026-07-03 (DOCS-CONSISTENCY-OVERHAUL Task 14 완료 — 위키 2차 4페이지(message-delivery-and-dedupe/idempotency/compensation-tx/stock-cache-recovery) 본문 현행화 + 문체 교정 + 서사 섹션 반영. `compensation-tx.md` 배너 사실 오류(재고 복구가 product-service 호출로 진화했다는 서술) 정정 + "이 모델의 이후 변화" 절 신설로 RD/RETRYING/FCG 클러스터를 명확히 역사로 격하, `AfterRollbackProcessor` DLQ 경로 반영(message-delivery-and-dedupe/stock-cache-recovery), Task 13 이월 항목(outbox-channel-dispatch DLQ 임계 모호성) 소스 재확인으로 해소. 위키는 파일 수정까지만, 커밋은 사용자)

## 활성 작업

- **주제**: DOCS-CONSISTENCY-OVERHAUL — 문서 전수 정합 개선 (docs/context + README + 위키, 코드 대조 정정·완료 항목 정리·문체 교정)
- **단계**: execute
- **활성 태스크**: Task 15: 위키 3차 — 상태 머신·복구·검증·PG 전략 5페이지 (state-management/retry-recovery/scenario-test/cross-validation/pg-strategy, Task 13 과 동일 작업 패턴)
- **이슈/브랜치**: #120
- **파일**: docs/topics/DOCS-CONSISTENCY-OVERHAUL.md / docs/DOCS-CONSISTENCY-OVERHAUL-PLAN.md / docs/DOCS-CONSISTENCY-OVERHAUL-DIAGNOSIS.md

## 재개 메모

- plan 게이트 2R 양측 pass (2026-07-02). 19태스크 — 진단(1~6) → 정정(7~12) → 위키 5배치(13~17, Task 7~10 완료 후 착수) → 재발 방지(18) → 최종 검증(19).
- Task 1 완료 — `docs/DOCS-CONSISTENCY-OVERHAUL-DIAGNOSIS.md` 생성(항목 형식·심각도·기본값 층위 규칙 확정 + 사실 목록 28건(F1~F28) + 표본 12건 판정, 전건 소스 파일:라인 채록).
- Task 2 완료 — 플로우·대장·함정 5파일(CONFIRM-FLOW/PAYMENT-FLOW/TODOS/CONCERNS/PITFALLS) §4.1 채움. outbox REQUIRES_NEW/IN_FLIGHT stale 클러스터가 표본 #12 지목 위치 외 4곳 추가 확인, `PaymentOutboxStatus.FAILED` dead-terminal 신규 발견, TODOS "토픽 묶음 계획"+"## 완료" 섹션 전체 archive/README.md 중복으로 (a) 삭제 판정, CONCERNS L-3/L-6 이미 CAPACITY-AND-SCALEOUT 으로 해소됐으나 미마킹임을 신규 발견, PITFALLS §18/§17 의 CONCERNS L-* ID 참조 오류 2건 발견.
- Task 3 완료 — 잔여 에이전트 문서 12파일 + smoke 5파일 §4.2 채움. Task 2 확정 S1 클러스터 3종(outbox REQUIRES_NEW/IN_FLIGHT·FAILED dead-terminal·parallel-enabled 층위)은 이 17파일에 잔존 0건(레벨이 달라 outbox 재시도 디테일 미서술) 확인. 대신 **17파일 상호 대조로 신규 S1 4건** 발견 — `STRUCTURE.md` 의 "빌드 트리거"(`./gradlew test` 범위)·"JaCoCo 설정 위치" 서술이 각각 `STACK.md`/`TESTING.md` 와 정면 모순(코드 확인 결과 `STRUCTURE.md` 쪽이 stale), `STACK.md` 스케줄러 활성화 매트릭스에서 user-service 전체 + 4서비스 공통 `DependencyHealthMetrics` 가 누락, `conventions/transactions.md` 예시 코드가 `PaymentConfirmResultUseCase.handle` 의 실제 qualifier(`transactionManager = "transactionManager"`, F1/CONCERNS L-1 과 같은 축)를 빠뜨림. S4 중복 4건 SSOT 지정(JaCoCo=`TESTING.md`, 빌드 명령=`STACK.md`, Contract test 상세=`TESTING.md`, CircuitBreaker 근거=`INTEGRATIONS.md`).
- Task 4 완료 — README + PAYMENT-FLOW-GUIDE §4.3 채움. outbox 발행 실패 stale 클러스터(표본 #12)가 GUIDE 에도 독립 5곳 잔존(S1 critical, Task 12 최우선). **신규 발견**: README "주요 해결 과제" 표 "장애 내성 복구 체계" 행이 서술하는 4개념 중 3개가 삭제/미연결 — `RecoveryDecision` 클래스 완전 삭제, `canCompensateStock` 이중 조건 가드 완전 삭제(F7), FCG(`PgFinalConfirmationGate`)는 클래스만 존재하고 프로덕션 호출처 0건. "결제 상태 관리" 섹션 캡션 "보상 안전 가드 자체는 유지"도 같은 축에서 코드와 정반대(S1 critical, 신규). Outbox 모델 표 `FAILED` dead-terminal 미표기 확인(S1 minor). 배너 "589 PASS"는 표본#5 그대로, `@Test` grep 재실행 641(근사치)로 노후 재확인. 위키 링크 25개 앵커 전건 파일 대조 — 깨진 링크 0건. Phase 표기 3축(README 개발순서/결제처리단계/MSA로드맵) 전수 채록 — README "다음 Phase 7"과 내부 로드맵 "Phase 4"(TODOS T4-A~E)가 동일 작업 뭉치를 다른 번호로 지칭함을 신규 확인(Task 11 disambiguation 권고, 선택). README 도메인 사실(S1) 3항목 §4.3.3 에 ship domain-expert 대조 입력용 별도 표기. GUIDE 문체(S5)는 구조화 기술 문서 장르라 수정 대상 0건.
- Task 5 완료 — 위키 도메인 코어 12페이지 §4.4 채움. outbox 클러스터와 별도인 **RD/RETRYING/FCG 클러스터**(`RecoveryDecision` 값 객체 + `RETRYING` 상태 + FCG 가 지금도 산다는 서술)가 12페이지 최대 문제로 확정 — `state-management.md` 배너 "RecoveryDecision + FCG + 격리 사이클은 유지된다"가 소스와 정반대(`RecoveryDecision` grep 0, `PaymentEventStatus` 8상태뿐(RETRYING 없음, F6), `handleFailed`/`handleQuarantined` 가드 없는 직접 호출), 상태 머신·RecoveryDecision 섹션·재고 복구 가드·복구 사이클 플로우 4곳 전면 재작성 대상(S1 critical, Task 15 최우선 — retry-recovery.md 의 "한계→Phase 5 개선" 프레이밍을 템플릿으로 권고). **신규 발견**: (1) `compensation-tx.md` 배너 자체가 "재고 복구는 product-service 호출(Kafka 이벤트)로 진화"라는 새 오류(S1 critical) — 실제는 payment-service 내부 Redis Lua `compensateAtomic` 완결, product 호출·Kafka 이벤트 없음. (2) `pg-confirm-flow.md` 가 FCG(`PgFinalConfirmationGate`, pg-service, `@Service`+테스트 완비하나 호출처 0)를 같은 페이지 안에서 살아있는 트리거(표)와 미래 계획(향후 확장 절)으로 모순 서술(S1+S2). (3) `async-outbox.md` 에서 §0.3 층위 위반 신규 사례(`scheduler.outbox-worker.batch-size` — default 50 vs benchmark 100을 application.yml 기본값처럼 제시). (4) `message-delivery-and-dedupe.md`/`stock-cache-recovery.md` 둘 다 DLQ-REACHABILITY(AfterRollbackProcessor) 반영 누락. (5) `pg-strategy.md` 배너가 payment-service 클래스 전면 소멸+pg-service 재구성 규모를 "경계 이동"으로 과소 서술. **양호**: `outbox-pattern.md` 핵심 동작은 정확(topic 문체 기준 예문 대상 L72/L94-98 정확 특정, Task 13 바로 실반영 가능) — FAILED dead-terminal 각주만 필요. `tx-scope.md`/`retry-recovery.md` 는 배너·역사 프레이밍 모범 사례. 서사 후보 9페이지 archive 경로 채록(3페이지는 근거 부족으로 미강제).
- Task 6 완료 — 위키 잔여 13페이지 §4.5 채움, **진단 단계(Task 1~6) 전체 종결**. 이 배치 최대 오류는 `structured-logging.md` — 페이지 절반 이상(로깅 파이프라인 다이어그램·민감정보 마스킹 섹션·TraceId 전파 섹션·Logstash 연동 섹션)이 완전히 삭제된 인프라·클래스(`MaskingPatternLayout`/`TraceIdFilter`/`LogstashTcpSocketAppender`/Elasticsearch·Kibana, 전건 grep 0, logback-spring.xml 은 Console appender 뿐)를 현재형으로 서술(S1 critical 4건) — 같은 위키의 `trace-propagation.md` 가 정확히 같은 주제를 Promtail/Loki 기준으로 서술해 극단적 대조군. 민감정보 마스킹이 대체 없이 사라진 것으로 보여 Task 8 TODOS "코드 확인 필요 항목" 후보 추가. RD/RETRYING/FCG 클러스터(Task 5 발견)가 `architecture.md`(설계 결정 표의 FCG 행 + `RecoveryDecision.java` 패키지 트리 잔존)·`metrics.md`(관측 맹점 다이어그램 RETRYING 분기)·`scenario-test.md`(`OutboxProcessingServiceTest` 섹션 전체가 완전 삭제된 클래스, 배너 고지 범위 밖 신규 발견)에 추가 확산 확인. **신규 발견**: (1) `msa-transition.md` 토폴로지가 `redis-stock` 연결을 `Prod --> RedS` 로 서술해 같은 위키의 `architecture.md`(`Pay --> RedS`, 정확)와 정면 모순(S1+S2, 소스 재확인 payment-service 만 연결). (2) `metrics.md` 가 이미 삭제된 `payment_health_max_retry_reached_total`(F9) 을 표에 나열하면서 F14 `dependency_up` 게이지·알람 규칙 4그룹(F13)은 배너·표 양쪽 누락 — 이 페이지의 정확히 그 주제의 최근 확장이 빠짐. (3) `TossApiMetrics` 가 pg-strategy 이관 후 pg-service 소속임에도 payment-service 소속처럼 서술. **양호**: `event-driven-choreography.md`(13페이지 중 가장 정합)·`trace-propagation.md`(최신·최정확, structured-logging 재작성 기준 문서)·`ai-workflow.md`(2026-06-12 개편을 정확히 반영, 파일 자신이 그 개편의 산출물이라 F28 갱신 격차의 유일한 예외)·`cross-validation.md`(배너-본문 정합 모범) 는 변경 불요. `Home`/`_Sidebar`/`_Footer` 링크-슬러그 25페이지 전건 대조 — 깨진 링크 0건, 고아 페이지 0건. 신규 위키 페이지(알람 규칙+장애 드릴)는 비강제, `metrics.md` 확장 흡수를 Task 16 에 권고. `Benchmark-Report.md` 는 이미 정확한 자기 배너로 변경 불요.
- Task 7 완료 — 정정 단계 개시, `docs/context/CONFIRM-FLOW.md`/`docs/context/PAYMENT-FLOW.md` §4.1.1(8건)·§4.1.2(3건) 전건 종결. **S1 최우선 클러스터**: outbox 발행 실패 복구 서술을 소스 기준(`OutboxRelayService.relay` 단일 `@Transactional` 안에서 claim·발행·완료 마킹 전부 수행, REQUIRES_NEW 분리 없음, 발행 실패 시 TX 전체 롤백 → PENDING 그대로 → `OutboxWorker` 5초 주기 배치 재픽업이 1차 회복 경로, IN_FLIGHT 5분 타임아웃 회수는 워커 비정상 종료 등 드문 경로의 보조 안전장치)으로 CONFIRM-FLOW §3·§4·§10·§11·§13 + PAYMENT-FLOW Phase 3 다이어그램·장애 복원 포인트 전면 재작성. `PaymentOutboxStatus.FAILED` dead-terminal 각주(§9/§10), `parallel-enabled` 기본값 코드 fallback(false)/default 프로파일(true) 층위 병기, dedup TTL 정리 완료 서술(`DedupeCleanupWorker`), pg-confirm-listener-split 이동 완료 반영, PAYMENT-FLOW 도입부 봉인 시점 앵커를 DLQ-REACHABILITY 기준으로 교체, 두 문서 헤더 날짜 2026-07-02 동기화. DIAGNOSIS.md §4.1.1·§4.1.2 종결 마킹 완료. TODOS/CONCERNS/PITFALLS(§4.1.3~4.1.5)는 Task 8 범위.
- Task 8 완료 — `docs/context/TODOS.md`/`docs/context/CONCERNS.md` §4.1.3·§4.1.4 전건 종결. **TODOS.md**: "토픽 묶음 계획"·"## 완료" 섹션(`docs/archive/README.md` 완전 중복) 전체 삭제 — "관련" 절에 archive/README.md 1줄 포인터로 대체. ✅완료+archive 경로 확인된 항목 24건 전체 삭제(진단 예비 판정 22건 + 판정표 누락분 2건 TC-13-FOLLOW-7/TC-9, 동일 패턴이라 동일 판정 적용 — PLAN.md Task 8 완료 결과에 사유 기록), 혼합 항목 3건(TC-13-FOLLOW-6/[CLEANUP-BATCH-B 후속]/TC-3) 해소분 문장만 제거·잔여 한계 보존, 보존 항목 중 TC-7 은 stale "현황" 서술("한도 초과 시 종결")을 `incrementRetryOrFail` 프로덕션 호출처 0(F3) 반영으로 정정. 코드 확인 필요 항목 3건 신규 등재(코드 수정 없음, 등재만) — `[PAYMENT-OUTBOX-INFLIGHT-UNUSED]`(REQUIRES_NEW 선점 경로 미사용 + 무백오프 5초 재시도)·`[STRUCTURED-LOGGING-MASKING-GAP]`(`MaskingPatternLayout` grep 0, 민감정보 마스킹 대체 없이 소실 추정 — 도메인 리스크 우선순위 상향)·`[PAYMENT-STATUS-TRIGGER-DETECT-DEAD-BRANCH]`(`detectTriggerFromCallStack()` 이 존재하지 않는 클래스명 매칭, 항상 폴백 가능성). 분류 룰에 "내부 Phase 5 번호는 README 개발 과정 Phase 1~7 체계와 별개" 1줄 명시. **CONCERNS.md**: L92 qualifier stale 문장을 "qualifier 명시 완료" 사실로 정정(F1), L97 ID 오기(TC-13-FOLLOW-1→FOLLOW-6) 정정. ✅해소+archive 경로 확인 6건(C-7/C-12/C-11/L-2/L-10/L-13) + 신규 발견 2건(L-3/L-6, CAPACITY-AND-SCALEOUT 2-인스턴스 fencing 실측으로 이미 해소됐으나 미마킹이었음을 확인) 총 8건 전체 삭제, C-9 완료분/Alertmanager 미도입 잔여 불릿 분리. 삭제 부작용(Rule 1)으로 L-5/L-14 내부 상호참조 dangling 2건 정정. PITFALLS.md §17/§18 의 L2/L6 dangling ID 참조는 지시대로 범위 밖 — Task 9 로 이월(§4.1.5 에 이미 항목 존재). DIAGNOSIS.md §4.1.3/§4.1.4 종결 마킹 완료.
- Task 9 완료 — `docs/context/{ARCHITECTURE,STRUCTURE,STACK,INTEGRATIONS,TESTING,PITFALLS}.md` + `docs/context/stack/flyway-operations.md` §4.2/§4.1.5 전건 종결. **STRUCTURE.md**: §빌드 트리거 절 전체를 `STACK.md` 참조 1줄로 교체(`./gradlew test`="전 모듈 단위+통합" 이라던 서술이 실제로는 단위만인 정면 모순을 표 삭제로 해소, S4 SSOT 동시 반영) + §정적 분석 JaCoCo "모듈별" → "루트 `build.gradle` `subprojects` 블록(4서비스 공통)의" 정정(`TESTING.md` 와 모순 해소). **STACK.md**: 스케줄러 활성화 매트릭스에 누락됐던 user-service 행 + payment/pg/product/user 4개 역할별 목록에 `DependencyHealthMetrics`(availability 알람 소비 폴링 게이지) 각각 추가(FAULT-INJECTION-RESILIENCE 반영 누락 정정). JaCoCo 정적분석 행은 S4 SSOT(`TESTING.md`)로 1줄 축소. **TESTING.md**: 테스트 카운트 재실행 갱신(단위 861/통합 59, 이전 스냅샷 873/48 대비 변동 — 코드 무변경, 문서 스냅샷 갱신뿐). **INTEGRATIONS.md**/**ARCHITECTURE.md**: S4 SSOT(Contract test=TESTING.md/CircuitBreaker=INTEGRATIONS.md) 상호 링크 추가. **PITFALLS.md**: 헤더 시점을 §24(2026-06-27) 기준 동기화, §18 "L6"→"L12"(CONCERNS.md 현행 ID, Task 8 재편 반영) 정정, §17 dangling "(L2 알려진 한계)" 인용을 자연어 서술로 교체(CONCERNS.md 는 이 태스크 대상 파일 밖이라 신규 등재 안 함). `conventions/transactions.md` S1 은 Task 10 범위로 유보. `./gradlew test` 재실행 861 PASS(문서 전용 태스크, 코드 무변경).
- Task 10 완료 — `docs/context/CONVENTIONS.md`(인덱스) + `conventions/{code-style,error-logging,kafka,testing,transactions}.md` + `docs/smoke/{alert-firing-check,infra-healthcheck,observability-load,observability-walkthrough,trace-continuity-check}.md` §4.2.5·4.2.8~4.2.17 전건 종결. 실질 변경 1건 — `conventions/transactions.md` 예시 코드에 `transactionManager = "transactionManager"` qualifier 추가(F1/CONCERNS L-1 축) + 명시 근거 1줄. 나머지 10파일은 재검토 결과 불일치 0건(보존). **완료 기준 stale 마커 게이트**(`docs/context/**`+`docs/smoke/**` 전체 grep, `RETRYING`/`StockOutbox`/payment 측 `EventDedupeStore`/`RecoveryDecision`/"REQUIRES_NEW 선점"/Elasticsearch·Logstash/`MaskingPatternLayout` 7종, 역사 서술은 잔존 허용)가 **현행 서술 위반 3건 신규 발견** — `ARCHITECTURE.md`(핵심 설계 결정 인덱스의 FCG/RecoveryDecision 행이 무표시 현재형, Task 9 가 바로 아래 "재고 복구 가드 (폐기)" 행은 잡았으나 이 두 행을 놓침), `STACK.md`(redis 의존 주석 "pg/payment-side EventDedupeStore" — payment 측엔 그 클래스 없음, pg-service 전용), `CONFIRM-FLOW.md`(§14 VT+MDC 절이 EOS 전환에서 완전 삭제된 `StockOutboxImmediateEventHandler` 를 현재형 병기, Task 7 이 §3/4/10/11/13 만 재작성해 §14 는 범위 밖으로 잔존). 3건 모두 Task 7/9 대상 파일이라 이 태스크에서 직접 정정 + DIAGNOSIS.md 해당 섹션(4.1.1/4.2.1/4.2.3)에 발견 기록 추가 + 3파일 헤더 갱신. `./gradlew test` 재실행 861 PASS(문서 전용, 코드 무변경).
- Task 11 완료 — `README.md` §4.3.1 전건(S1 critical 2건 + S1 minor 1건 + S3 2건 + S5 2건 + S2 1건) 종결. 배너 "🚧 진행 중 · Phase 6"→"✅ Phase 6 완료" + "589 PASS"→"단위 861 / 통합 59 PASS"(Task 9 실측값) + "정합이 안 맞을 수 있음" 경고·막연한 "보상 트랜잭션 자동 회복 layer" 예시 삭제 + "다음 Phase 7" 예고에 알람 규칙·Toxiproxy 장애 드릴 인프라 선행 구축 완료 1줄 추가. "주요 해결 과제" 표 "장애 내성 복구 체계" 행 — 폐기 개념(`RecoveryDecision`/`canCompensateStock`/FCG) 대신 현행 사실(PG self-loop 재시도+백오프, DLQ 자동 격리, `PaymentReconciler` 스케줄 복원, `compensateAtomic` Redis Lua 원자 연산)로 전면 재작성, FCG 는 미연결 dead code 라 헤드라인 미포함. "결제 상태 관리" 캡션 "보상 안전 가드 자체는 유지" 삭제 — 섹션을 "Phase 5 시점 스냅샷"으로 명시하고 가드가 `QuarantineCompensationHandler` 단일 종결 체크로 대체됐음을 병기, mermaid GUARD 노드는 스냅샷 전제가 명확해져 원형 보존. Outbox 모델 표에 `FAILED` dead-terminal 각주 반영. 문체(S5) "이상적"·"최적의" 평가 형용사 + "~를 통해" 번역투 정정. Phase 표기(S2)는 plan 결정대로 README 축(1~7) 유지, 내부 로드맵과의 번호 불일치 disambiguation 은 README 에 미반영(`TODOS.md` 분류 룰에 이미 1줄 명시). README diff 의 도메인 사실(S1) 4항목은 `DIAGNOSIS.md` §4.3.3 에 반영 내역 병기(ship domain-expert 대조 입력용). `./gradlew test` 재실행 861 PASS(문서 전용, 코드 무변경).
- Task 12 완료 — `docs/context/PAYMENT-FLOW-GUIDE.md` §4.3.2 S1 클러스터 5건(L105/L107/L214-217/L254-255/L294·L307-308) 종결. CONFIRM-FLOW.md/PAYMENT-FLOW.md 짝문서(Task 7 정정본)와 완전히 같은 사실 오류였음을 소스 재확인(`OutboxRelayService.java:49-78` 단일 `@Transactional`, `OutboxWorker.java:26,38,41`)으로 확정 — "Kafka 발행 실패 → `IN_FLIGHT` 유지 → 워커 타임아웃 폴백" 서술 전건을 "발행 실패 → relay 단일 TX 롤백 → PENDING 즉시 복귀 → `OutboxWorker` 5초 주기 배치 재픽업이 1차 회복 경로, `IN_FLIGHT` 5분 타임아웃 회수는 워커 크래시 등 보조 경로"로 정정(§A 시퀀스 단계14/15 각주, §B-2 PUBREC mermaid, §C 회복 경로 색인 표, §D 마스터 플로우차트 노드·엣지). mermaid 노드 라벨 금지 문자(중괄호·중간점·유니코드 화살표·따옴표) 신규 추가분 준수 확인. 문체(S5)는 Task 4 진단대로 수정 대상 없음 유지(구조화 기술 문서 장르, 보존). 리포트에 없는 항목은 수정하지 않음(§C 나머지 행 등은 4.3.2 "전건 검증 결과 정합" 판정대로 보존). 문서 헤더에 정정 시점 1줄 추가, `DIAGNOSIS.md` §4.3.2 종결 마킹 + 헤더 "최종 갱신" 동기화. `./gradlew test` 대상 아님(문서 전용, 코드 무변경).
- Task 13 완료 — 위키 1차 5페이지(`outbox-pattern`/`outbox-channel-dispatch`/`pg-confirm-flow`/`async-outbox`/`tx-scope`) `DOCS-CONSISTENCY-OVERHAUL-DIAGNOSIS.md` §4.4.1~4.4.5 근거로 로컬 파일 수정 완료(위키 별도 저장소, 커밋은 사용자). `outbox-pattern.md`: 빈 헤더 삭제 + `FAILED` dead-terminal 각주 + topic 문체 기준 예문 실반영(retry 카운트 불릿은 Task 1 재검증 결론 문구). `outbox-channel-dispatch.md`: `worker-count` 누락 행 추가 + 도입 배경 서사(pg-confirm-listener-split). `pg-confirm-flow.md`: FCG "표=살아있는 트리거" vs "향후 확장=검토 중" 모순을 "구현됐으나 미연결"로 통일(PAYMENT-FLOW.md §4.9 와 결론 통일). `async-outbox.md`: `scheduler.outbox-worker.*` default(50)/benchmark(100) 층위 각주(`parallel-enabled` 와 동일 축) + `RecoveryDecision` 전방 참조 2곳 정정 + 말미 서사(msa-transition). `tx-scope.md` 변경 없음(보존). 부수 발견 — 세션 시작 시점에 이미 존재하던 uncommitted 위키 변경(`stored_traceparent` RDB 복원 관련, 3파일)은 이 태스크가 작성하지 않았고 DIAGNOSIS §4.4 항목 밖이라 건드리지 않음(출처 확인은 후속).
- Task 14 완료 — 위키 2차 4페이지(`message-delivery-and-dedupe`/`idempotency`/`compensation-tx`/`stock-cache-recovery`) `DOCS-CONSISTENCY-OVERHAUL-DIAGNOSIS.md` §4.4.6~4.4.9 근거로 로컬 파일 수정 완료(위키 별도 저장소, 커밋은 사용자). `message-delivery-and-dedupe.md`: `payment.events.confirmed.dlq` 두 번째 격리 경로(EOS `commitTransaction` 반복 실패 → `AfterRollbackProcessor`) 행 추가(S1) + 서사(DLQ-REACHABILITY). `idempotency.md`: 변경 없음(보존, 번역투 1건만 문체 교정). `compensation-tx.md`: 배너 사실 오류(재고 복구가 product-service 호출로 진화했다는 서술 — 실제는 payment-service 내부 Redis Lua `compensateAtomic`) 정정(S1 critical) + `retry-recovery.md` 모범 사례 템플릿(배너로 시대 규정 + 본문 보존 + 말미 "이후 변화" 매핑)을 따라 새 "## 이 모델의 이후 변화" 절 신설로 RD/RETRYING/FCG 클러스터를 역사로 격하(stock-compensation-recovery/stock-compensation-other-paths 토픽 인용) + `stock-cache-recovery` 상호 링크 추가. `stock-cache-recovery.md`: `AfterRollbackProcessor` 가 같은 recoverer 로 수렴한다는 1문단 추가(S3) + "원칙의 재적용" 회고 항목 신설. **Task 13 이월 해소** — `outbox-channel-dispatch.md` "Kafka publish 실패 → 4회 초과 시 DLQ" 서술 오류 확정(소스: `PgOutboxRelayService`/`PgOutboxPollingWorker` 에 attempt 카운터 없음, `MAX_ATTEMPTS=4` 는 `PgVendorCallService` 벤더 self-loop 전용) — "무한 재폴링, DLQ 전이 없음"으로 정정 + `DIAGNOSIS.md` §4.4.2 확정 갱신.
- 주의: 사실 판정 근거는 소스 파일:라인만 (문서 상호 인용 불인정). 위키는 파일 수정만, 커밋은 사용자.

## 최근 완료

- **FAULT-INJECTION-RESILIENCE** (서비스·DB·Redis 가용성 알람 + docker stop 완전 다운 정합 거동 실증 — 가용성 사각을 4서비스 `DependencyHealthMetrics` 직접 폴링 게이지(`dependency_up{component}`, 2s 타임아웃 가드, payment redis dedupe/stock 2분리, last-poll staleness)로 메우고 신규 `availability.yml`(ServiceDown/DependencyDown/DependencyHealthStale + `absent()` backstop)로 탐지. 완전 다운 정합은 `@EmbeddedKafka`+전용 MySQL+`@MockitoSpyBean doThrow` 통합테스트로 **DLQ 유실0**(load-bearing, 시간 무관) 고정 — 컨테이너 stop 은 Hikari 30s×5 비결정성으로 금지. **검증이 두 갭 발견**: (1) execute 중 implementer 도메인 변경(`resetToReady`→order NOT_STARTED 복원, EXPIRED 종결 활성화)을 domain-expert **critical**로 롤백 — 설계 전제 "IN_PROGRESS→READY→EXPIRED 2단 마스킹"이 실제론 order EXECUTING 잔류로 `expire()` 차단(EXPIRED 도달 불가·READY 영구 잔류 + 만료 batch poison-pill)임을 실측해 CONCERNS **L-14** 등재(L-10 정책 갭). EXPIRED 종결화는 D7 가드가 TQ-1 복구를 봉쇄해 비종결 READY보다 나쁨이 롤백 근거. (2) ship **라이브 드릴**이 stale jar 배포 갭(bootJar 선행 누락 → 게이지 빈 미생성) + user `@EnableScheduling` 누락(폴러 미실행 → 알람 영구 오발화)을 잡음 — promtool/통합테스트 사각. no-divergence(over-sell 0)는 공허 단정 제외, **신규 복구 로직 없음**(TQ-1/TC-3 위임). 7태스크, payment 단위465+통합42·pg330·product50·user9 PASS + promtool 25케이스 + 라이브 ServiceDown·DependencyDown{db,redis-dedupe} 발화/해소 실측, discuss R3·plan R2(critical 1 reconcile)·execute 도메인 critical 1(롤백)·ship 코드리뷰 R2(critical 1 user scheduler), 2026-06-30, 이슈/브랜치 #118) — `docs/archive/fault-injection-resilience/COMPLETION-BRIEFING.md`
- **ALERTING-RULES-AND-FAULT-DRILL** (Prometheus 알람 규칙 인프라 + Toxiproxy 장애 주입 발화 실증 — rule 평가만 도입(Alertmanager 미도입): `prometheus.yml` rule_files → `observability/prometheus/rules/*.yml` 로드, `/api/v1/{rules,alerts}` 평가까지. 3그룹 7규칙(coordinator/guard-skip/dlq), `promtool test rules` 16케이스 회귀 고정. Toxiproxy latency 전용 드릴 프로파일. **라이브 실증이 promtool 사각 dead branch 발견** — broker 완전 정지 시 kafka_brokers 는 0 아닌 absent → `absent(kafka_brokers)` 3분기 보강 + PITFALLS #24. 단일 broker 구조 한계로 코디네이터/EOS 라이브 발화는 promtool+통합테스트 격하. 애플리케이션 코드 무변경. 7태스크, 18커밋, 2026-06-27, 이슈/브랜치 #116) — `docs/archive/alerting-rules-and-fault-drill/COMPLETION-BRIEFING.md`

전체 이력: `docs/archive/README.md` / 구 STATE 이력: `docs/archive/state-history-2026H1.md`
