# 재개 지점 — TIME-MODEL-AND-EXPIRY execute

> 갱신: 2026-06-02 | 이슈 #83, 브랜치 #83 | stage: execute

## 진행 상황

- ✅ **T1+T2+T4+T5** — 도메인/엔티티/저장소 Instant 전환 + `hibernate.jdbc.time_zone=UTC` + native query 파라미터 Instant. 커밋 `7f9de2fd`. integrationTest 28/28.
- ✅ **T3+T6+T7** — application/infra `LocalDateTimeProvider`→`Clock` 전환 + 만료 임계 외부화(`payment.expiration.ready-timeout-minutes`). 단위 통과·integrationTest 28/28. (13건 실패는 `BaseIntegrationTest` 구조 오류 — productPort/userPort 빈이 TestClock 내부로 들어가 미등록 — 가 원인이었고 복구로 전량 해소.)
- ✅ **T10+T11** — 만료 정책 명문화 테스트. `PaymentEventTest` T10 스펙 메서드 3개(expire 도메인 가드 전수, PARTIAL_CANCELED 회귀 가드), `PaymentReconcilerTest` T11 스펙 2개(2단 연쇄 1단계 verify), `PaymentExpirationServiceImplTest` T11 스펙 1개(2단 연쇄 2단계). 단위 446/446 PASS.

## 남은 태스크 (순서)

1. ✅ **T8** — pg `PgInbox`/`PgOutbox` 도메인 `Instant.now()` 직접 호출 제거 → 인자 주입 + `PgInboxProcessor` Clock 주입. `grep pg/main Instant.now()` 0건. 단위 311/311 PASS.
2. ✅ **T15** — pg 벤더 strategy 시각 정리(`TossPaymentGatewayStrategy.LocalDateTime.now()` 제거 + `NicepayPaymentGatewayStrategy.OffsetDateTime.now()` → `clock` 기반) + approvedAtRaw contract NicepayPaidAtNormalizationTest 기존 커버 확인(F6). `grep LocalDateTime.now()` 주석 외 실질 호출 0건. 단위 311/311 PASS.
3. ✅ **T9** — 만료 스케줄러 프로퍼티 키 정정 + fallback 체인. 커밋 `5048e7eb`.
4. ✅ **T12** — payment `JdbcPaymentEventDedupeStore` raw-JDBC UTC 규약(connectionTimeZone=UTC + UTC Calendar) + Clock 전환. `LocalDateTimeProvider`/`SystemLocalDateTimeProvider` 물리 삭제 + javadoc 정정 → **AC1 grep 0 달성**. payment 단위+통합 30/30. (기존 메타데이터 테스트의 `getTimestamp` 읽기를 UTC Calendar로 맞춰 GREEN.)
5. **T13** — product `Clock` 빈 도입 + `JdbcEventDedupeStore` raw-JDBC UTC 규약 + `DedupeCleanupWorker`/`StockCommitConsumer` Instant.now() 제거. ← **다음**
6. **T14** — payment `parseApprovedAt` offset 정규화(`.toInstant()`) — 정산 앵커 9시간 오차 차단.
7. (verify 단계) **T16** — PITFALLS §13 + 영구 문서 동기화.

## 주의 (PLAN 재확인)

- 시스템 TZ가 Asia/Seoul(KST)이라 raw-JDBC vs ORM 시각 비대칭이 통합테스트에서 드러난다 — D3/D7 검증의 핵심 회귀 가드. testcontainers reuse 켜져 있어 시각 설정 변경 시 `docker ps --filter ancestor=mysql:8.0 -q | xargs -r docker rm -f` 후 재실행.
- T12·T13 AC8: 비-UTC JVM TZ + Testcontainers 라운드트립. build.gradle 변경 필요 시 Rule 2(중단·보고) — 테스트 내 `TimeZone.setDefault` 우회 우선.
- T4·T12가 같은 `application-docker.yml` 편집 — 순차.
- 통합테스트 하네스: `BaseTestConfig`에 가변 `@Primary TestClock` 빈(`setFixedInstant`로 제어). `@Primary LocalDateTimeProvider`(TestLocalDateTimeProvider)는 T12에서 포트 삭제 시 함께 정리.
