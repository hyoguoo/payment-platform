# 재개 지점 — TIME-MODEL-AND-EXPIRY execute (WIP)

> 작성: 2026-06-01 | 이슈 #83, 브랜치 #83 | stage: execute (T1+[T2+T4+T5] 묶음 GREEN 미완성)

## 무슨 일이 있었나

execute 첫 빌드-그린 논리 단위 **T1 + [T2+T4+T5]**를 Implementer가 구현 중 **세션 한도로 중단**됨.
- RED 커밋 완료: `fbc45187 test(payment): T2+T5 Instant 기반 도메인·저장소 테스트 RED 추가`
- 그 후 GREEN 작업(도메인 Instant 전환 + 소비처 전환)을 working tree에 진행했으나 **GREEN 커밋 미완성** 상태로 끊김.
- 이 WIP를 `wip:` 커밋으로 보존함.

## 현재 빌드 상태 (검증 완료)

- `:payment-service:compileJava` + `compileTestJava`: **BUILD SUCCESSFUL** (컴파일 그린).
- `:payment-service:test`(단위): 통과.
- `:payment-service:integrationTest`: **2건 실패** (28 completed, 2 failed):
  1. `PaymentEventRepositoryImplTest > findReadyPaymentsOlderThan(Instant) — cutoff 이전 READY 결제만 반환한다.`
  2. `PaymentSchedulerTest > 스케줄러가 30분 이상 지난 READY 상태의 결제를 EXPIRED로 변경한다`

## 실패 2건 추정 원인 (재개 시 우선 점검)

둘 다 Testcontainers MySQL 기반 통합 테스트. T4(`hibernate.jdbc.time_zone=UTC`) + T5(저장소 cutoff `Instant` 전환)의 round-trip 정합 문제로 보임:
- 테스트 fixture가 아직 `LocalDateTime` 기준으로 cutoff를 만들거나, entity `Instant` 매핑 ↔ DB `DATETIME(6)` 저장/조회 시 TZ 경계가 어긋났을 가능성.
- `hibernate.jdbc.time_zone=UTC`가 Testcontainers 프로파일(`application-test.yml`)에 실제 반영됐는지 확인.
- 이 2건이 바로 D3(UTC 저장 일관)/AC8(round-trip 동일성)의 핵심 검증 지점 — 회귀 버그가 아니라 GREEN 미완성으로 판단.

## 재개 절차

1. `./gradlew :payment-service:integrationTest --rerun` 로 실패 2건 재현.
2. 위 2개 테스트의 fixture·cutoff·entity 매핑을 점검해 GREEN화. (도메인은 `Clock` 미주입, `Instant` 인자만 — D2 유지)
3. GREEN화되면 `feat(payment):` 커밋(PLAN T1/T2/T4/T5 체크박스 + STATE.md 갱신 포함). RED 커밋 `fbc45187`과 짝.
4. **중복 작업 주의**: Implementer가 이미 application/infra 소비처(`PaymentReconciler`, `PaymentCommandUseCase`, `PaymentConfirmResultUseCase`, `PaymentCreateUseCase`, `PaymentLoadUseCase`, `PaymentHealthMetrics`, `PaymentStatusMetricsAspect` 등)를 상당수 `Clock`으로 전환함 (working tree 참조). 즉 T3/T7 범위가 이미 일부 진행됨 → **남은 태스크(T3/T6/T7) 재산정** 필요. 무엇이 남았는지 `grep -rn "LocalDateTimeProvider" payment-service/src/main` 으로 확인.
5. `LocalDateTimeProvider` 포트는 **아직 미삭제** (의도) — 모든 소비처 전환 완료(T7/T12) 후 최종 삭제해야 AC1(grep 0건) 달성. 테스트의 `TestLocalDateTimeProvider`도 정리 대상.
6. 이후 순서: 남은 T3/T6/T7 마무리 → T10 → T8(pg) → T15 → T9 → T11 → T12 → T13 → T14 → (verify에서 T16).

## 주의 (PLAN 재확인)

- T4·T12가 같은 `application-docker.yml` 편집 — 충돌 방지 순차.
- AC8 통합테스트는 비-UTC JVM TZ(`-Duser.timezone=Asia/Seoul`) + Testcontainers. build.gradle 변경이 필요하면 **Rule 2(중단·보고)** — 가능하면 테스트 내 `TimeZone.setDefault`로 우회.
- F6(T15 pg approvedAtRaw 직렬화 contract 불변 회귀 가드) execute에서 확인.
