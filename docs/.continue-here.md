# 재개 지점 — TIME-MODEL-AND-EXPIRY execute

> 갱신: 2026-06-01 | 이슈 #83, 브랜치 #83 | stage: execute

## 완료된 태스크

- **T1** (Clock 빈 등록 + LocalDateTimeProvider/SystemLocalDateTimeProvider 제거) — GREEN
- **T2** (PaymentEvent 도메인 Instant 전환) — GREEN
- **T4** (PaymentEventEntity Instant 전환 + hibernate.jdbc.time_zone=UTC) — GREEN
- **T5** (PaymentEventRepository 포트·구현체 Instant 전환) — GREEN
  - 핵심 수정: JpaPaymentEventRepository.findReadyPaymentsOlderThan 파라미터 LocalDateTime → Instant
  - PaymentEventRepositoryImpl LocalDateTime.ofInstant 변환 제거
  - integrationTest 28/28 PASS

## 현재 빌드 상태

- `:payment-service:test`(단위): 426 PASS
- `:payment-service:integrationTest`: 28 PASS (PaymentEventRepositoryImplTest + PaymentSchedulerTest 포함)

## 다음 태스크: T3 + T6 + T7

T3: application 레이어 LocalDateTimeProvider → Clock 전환
- PaymentCommandUseCase, PaymentLoadUseCase, PaymentCreateUseCase, PaymentOutboxUseCase
- OutboxRelayService, PaymentReconciler, PaymentConfirmResultUseCase (L68 필드, L126, L191)
- 이미 일부 진행됨 — grep으로 잔여 확인: `grep -rn "LocalDateTimeProvider" payment-service/src/main`

T6: 만료 임계 외부화 (@Value 30분 기본값)
T7: infrastructure 레이어 Clock 전환 (metrics/aspect/scheduler)

## 주의

- LocalDateTimeProvider 포트는 아직 미삭제 (의도). 모든 소비처 전환 후 T7 완료 시 삭제.
- T4·T12 같은 application-docker.yml 편집 — 충돌 방지 순차.
- 다음 순서: T3 → T6 → T7 → T10 → T8(pg) → T15 → T9 → T11 → T12 → T13 → T14 → (verify T16)
