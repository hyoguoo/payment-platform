# 재개 지점 — TIME-MODEL-AND-EXPIRY execute 완료

> 갱신: 2026-06-02 | 이슈 #83, 브랜치 #83 | stage: **review**

## 진행 상황 (전 구현 태스크 완료)

- ✅ **T1~T12, T15** — payment 전체 Clock/Instant 전환 + pg 시각 정리. AC1 달성(LocalDateTimeProvider 0건).
- ✅ **T13** — product Clock 빈 도입 + JdbcEventDedupeStore UTC Calendar 바인딩 + DedupeCleanupWorker/StockCommitConsumer clock.instant() 전환. AC2 달성(product Instant.now() 0건).
- ✅ **T14** — parseApprovedAt .toInstant() 정규화 TODO 정리 + AC9 테스트 추가. AC9 달성(.toLocalDateTime() 0건).

## 다음 단계

- (verify 단계) **T16** — PITFALLS §13 + 영구 문서 동기화.

## 달성된 grep 지표

- AC1: `grep -rn "LocalDateTimeProvider" payment-service/src/main/java` → 0건
- AC2: `grep -rn "Instant.now()" product-service/src/main/java` → 0건
- AC9: `grep -rn "toLocalDateTime()" payment-service/src/main/java` parseApprovedAt 경로 → 0건
