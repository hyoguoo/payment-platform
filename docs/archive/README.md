# Archive

> **⚠️ AI 에이전트는 이 디렉토리를 읽지 않는다. (`docs/context/`만 참조할 것)**
> 완료된 작업 뭉치의 기록 보존용 아카이브입니다.
> 예외: 과거 작업 맥락이 필요할 때 해당 폴더의 `COMPLETION-BRIEFING.md`만 읽는다.

각 작업 폴더 구성: `COMPLETION-BRIEFING.md`(작업 요약 SSOT) + PLAN/CONTEXT 산출물 + `rounds/`(구 6단계 워크플로우의 라운드 판정 기록, 2026-06-12 이전 체계).
`state-history-2026H1.md` — 2026-06-12 STATE.md 슬림화 이전의 "직전 봉인" 이력 스냅샷.

---

## 작업 뭉치 목록

| 폴더 | 작업 내용 | 기간 |
|------|---------|------|
| `async-payment-migration/` | 비동기 결제 처리 마이그레이션 (Sync / Outbox / Kafka 전략 구현) | 2026-03-14 ~ 2026-03-18 |
| `checkout-idempotency/` | Checkout 멱등성 구현 (Caffeine 캐시 기반 중복 요청 감지, 201/200 분기) | 2026-03-22 |
| `async-benchmark-improvement/` | k6 벤치마크 개선 — ramping-arrival-rate 전환, cAdvisor/Prometheus remote write 연동, 전략 자동 전환 | 2026-03-27 |
| `k6-benchmark-measurement-improvement/` | k6 측정 개선 — e2e_under_load/e2e_idle 분리, outbox-parallel 케이스 추가, 3케이스 자동화 | 2026-03-27 |
| `k6-benchmark-methodology-fix/` | k6 측정 방법론 수정 — e2e_idle 별도 run 분리, TIMEOUT 명시 집계, confirm_requests Counter, sync 부하 상향 | 2026-03-27 |
| `outbox-immediate-dispatch/` | Outbox 즉시 처리 — TX 커밋 후 @Async+@TransactionalEventListener로 Toss API 즉시 호출, OutboxWorker recovery 전용 전환 | 2026-03-28 |
| `k6-benchmark-redesign/` | k6 벤치마크 재설계 — idle 제거, e2e_completion_ms 메트릭 통일, 부하 단계 100→200→400 통일 | 2026-03-28 |
| `async-payment-cleanup/` | 비동기 결제 클린업 — Kafka 전략 제거, Sync/Outbox 이중 전략 정리, 불필요 UseCase·RecoverService 제거 | 2026-03-29 |
| `async-channel/` | 비동기 채널 — LinkedBlockingQueue + OutboxImmediateWorker(VT/PT)로 HTTP 스레드 블로킹 완전 제거, OutboxProcessingService 공유 처리 로직 추출 | 2026-03-31 |
| `outbox-only-refactor/` | Outbox 단일화 리팩토링 — Sync 전략·PaymentProcess 도메인 제거, UNKNOWN 상태 전면 삭제, RestTemplate→WebClient 전환, Grafana 관측 지표 추가 | 2026-04-06 |
| `client-integration/` | 클라이언트 통합 — 정적 결제 UI(checkout/success/fail) 추가, Thymeleaf 어드민 페이지 리팩토링, Docker 환경변수 및 run.sh 개선 | 2026-04-06 |
| `payment-retry-state/` | 재시도 정교화 — RetryPolicy(FIXED/EXPONENTIAL 백오프), RETRYING 상태 도입, 예외 기반 분기 → PaymentConfirmResultStatus enum 전환, nextRetryAt DB 기록 | 2026-04-07 |
| `payment-double-fault-recovery/` | 이중장애 복구 — RecoveryDecision 값 객체, PG getStatus 선행 조회 복구 사이클, FCG(Final Confirmation Gate), QUARANTINED 상태, D12 재고 가드, isTerminal() SSOT | 2026-04-10 |
| `nicepay-pg-strategy/` | NicePay PG 전략 추가 — 멀티 PG 연동 (Strategy 패턴), gatewayType 결제건별 라우팅, NicePay confirm/cancel/getStatus, 복구 사이클 최초 시도 PG 선조회 제거, 예외 벤더 중립 rename | 2026-04-14 |
| `msa-transition/` | MSA 전환 — 모놀리스 → 4서비스 분해(payment/pg/product/user) + Eureka + Gateway, 4 MySQL 인스턴스 분리(ADR-23), Kafka 양방향 confirm 왕복(ADR-30), Phase 0~3.5 게이트 통과 | 2026-04-24 |
| `pre-phase-4-hardening/` | Pre-Phase-4 하드닝 — 비즈니스 로직 정확성·traceId 추적·코드 작성 수준 3축 19태스크(T-A~T-J + K1~K15), AMOUNT_MISMATCH 양방향 방어, dedupe two-phase lease + DLQ, AFTER_COMMIT stock publish, traceparent 다중 홉 검증 | 2026-04-24 |
| `phase-4-readiness-sweep/` | Phase 4 진입 전 self-loop 정리 + 코드 리팩터링 검수 — 잔재 워크플로우 식별자(T*/K*/D*/S*/TC-K*) · ADR 키워드 · 폐기 클래스명(StockRestore/StockSnapshot/FailureCompensation) 코드/문서 전수 제거, EventType enum dead 12건 정리, dead config / V1 dead column 제거, PaymentEvent.resetToReady invariant 테스트 9건 추가, 메서드 분리 2건 + Optional 패턴 2건 + boolean parameter → enum 1건 | 2026-04-27 |
| `stock-compensation-recovery/` | 결제 결과 보상 silent loss 회복 layer — Lua atomic dedup token (decrement:done / compensation:done SETNX P8D) 도입, 기존 단일 상품 Lua → 결제 단위 N개 atomic 강화 (선차감 부분 차감 race 동시 해소), application 측 try/catch + dedupe lease (markWithLease/extendLease/remove) + 직접 DLQ publisher 통째 폐기, EventDedupeStore + PaymentConfirmDlqPublisher 두 orphan port 제거, handleFailed 호출 순서 뒤집기 (보상 → markPaymentAsFail), Spring Kafka DefaultErrorHandler + DeadLetterPublishingRecoverer + FixedBackOff(1s, 5) 위임, Redis appendfsync=always 강제 (AOF race window 완화) | 2026-05-08 |
| `pg-confirm-listener-split/` | pg-service Kafka listener TX 에서 벤더 호출 분리 — `PgInboxPendingService` (listener TX timeout=5s, INSERT IGNORE + publishEvent) + `PgInboxChannel` (LinkedBlockingQueue cap=1024) + `PgInboxImmediateWorker` (VT 5, status 분기 4분기) + `PgInboxPollingWorker` (60s 통일, PENDING/IN_PROGRESS 두 경로, 새 OTel root span) + `PgInboxStatus` PENDING 추가 + NONE 폐기 + Flyway V2~V3 (PENDING enum + paymentKey/vendorType 컬럼) + 보정 경로 PENDING 우회 룰 (`DuplicateApprovalHandler` `transitDirectToTerminal` / `transitDirectToInProgress`) + native query `FOR UPDATE SKIP LOCKED` (PENDING + IN_PROGRESS 양쪽) + `PgTerminalReemitService` 별 빈 분리 (self-invocation proxy 우회 차단) + `pg_inbox.zombie_recovered_total` / `listener_tx_timeout_total` Counter 신규, 위키 + 영구 문서 동기화 | 2026-05-09 |
| `cleanup-batch-a/` | PR 묶음 A — 코드 청소 4건 (TC-16 + TC-10 + TC-2 + TC-5) — pg-service `PgInboxAmountService` dead service 제거 + pg-service `PgInbox` / `PgOutbox` `@Builder(allArgsBuilder/allArgsBuild) + @AllArgsConstructor(PRIVATE)` 패턴 통일 (factory only 노출, `PgOutbox` Long id dead parameter 제거 호출처 5 파일 정정) + product / user-service Flyway `db/schema/` + `db/seed/` 디렉토리 분리 (`docker` profile 은 schema 만 적용, `FlywayDockerProfileTest` Testcontainers 검증) + payment-service `PaymentExceptionHandler` 의 `ProductService` / `UserService` `RetryableException` → 503 + `Retry-After: 5` 헤더 일괄 매핑, 영구 문서 6개 갱신 (CONFIRM-FLOW / PAYMENT-FLOW dangling 제거, STACK.md missing-migration 3-step 가이드, CONVENTIONS.md Builder 룰 정합, TODOS.md [PR A] 4항목 완료 이전 + [NET-RETRY] / [FLYWAY-USER-SEED-GAP] 신규 등재, STRUCTURE.md db/schema+db/seed 분리 표기) | 2026-05-12 |
| `payment-eos-transition/` | TC-13 payment-service 결제 결과 컨슈머 EOS 전환 (PR #77) — 발행 보장 모델을 outbox → Kafka EOS 로 빅뱅 전환. `PaymentEventDedupeStore` + `JdbcPaymentEventDedupeStore` 신설 (`payment_event_dedupe` Flyway V2 UNIQUE INSERT IGNORE), `KafkaTransactionManager` 빈 + `transactional.id = ${spring.application.name}-${HOSTNAME:local}` + `enable.idempotence=true` 도입, 컨슈머 wiring (KTM + `isolation.level=read_committed`), `PaymentConfirmResultUseCase` 재작성 (D7 진입 가드 `isCompensatableByFailureHandler` + 멱등 마킹 + multi-product loop 안에서 `StockEventUuidDeriver.derive` + 직접 `producer.send(stock-committed)` Kafka tx buffer), `StockOutbox` 묶음 19 파일 일괄 삭제 + `payment_stock_outbox` Flyway V3 DROP, product-service `isolation.level=read_committed` 적용, `StockEventUuidDeriver` 보존 (DR-1 multi-product idempotencyKey 결정성). Testcontainers EOS 통합 5 시나리오 (정상 commit / abort invisibility / 중복 INSERT IGNORE / multi-product / QUARANTINED 가드). Rule 1 SUT 수정 2건 (`JpaTransactionManager @Primary` + `commandsConfirmProducerFactory` 명시 등록). 위키 4개 파일 Phase 6 마커 제거 + 정합. review R1 fix 과정에서 hidden bug 3건 노출 + clean fix (gatewayType=null / CheckoutResult Jackson is-prefix / IdempotencyStoreRedisAdapter race). 영구 문서 8개 갱신, CONCERNS L-1 + CONFIRM-FLOW §5 + TODOS TC-13-FOLLOW-6 에 "위키 line 141 (중복 시 발행 항상 진행) 룰이 EOS atomicity 가 아닌 안전성 SSOT" 명시. test 708/708 + integrationTest 23/23 PASS | 2026-05-17 ~ 2026-05-18 |
| `eos-followup-cleanup/` | EOS 전환 후속 정합 + 결제 비동기 경로 청소 (이슈 #79) — (FOLLOW-6) `PaymentConfirmResultUseCase.handle`에 `@Transactional(transactionManager="transactionManager")` qualifier 명시(위험 지점 1곳만, 나머지 13곳+ 무변경) + deprecated `setTransactionManager`→`setKafkaAwareTransactionManager` 교체 + best-effort 1PC 한계 Javadoc. (FOLLOW-5) `PaymentEventStatus.isCompensatableByFailureHandler` 겸용 메서드를 `canApplyConfirmResult`(결과 반영 가능성) + `canCompensateStock`(보상 정당성) 두 술어로 분리 + 답 동조 교차 불변식 회귀 테스트(`PaymentEventStatusCrossInvariantTest`, D7 침묵 DLQ 드리프트 가드) + 기존 `PaymentEventStatusEosGuardTest` 삭제. (FOLLOW-2/TC-11) payment `payment_event_dedupe` / product `stock_commit_dedupe` 만료행 청소 `DedupeCleanupWorker`(@Scheduled, `deleteExpired` 포트 메서드 추가 + 어댑터 DELETE 구현, TTL P8D > retention 7d라 멱등 무해) + product `SchedulerConfig`(@ConditionalOnProperty), pg_inbox는 종결행이 재배달 멱등 SoT라 범위 제외. (TC-15 항목3) Flyway V4 `pg_inbox.stored_traceparent VARCHAR(64)` + `TraceparentExtractor`(OTel W3C 래핑, infra 격리) — consumer가 `Context.current()` 추출→RDB 보관, `PgInboxPollingWorker`가 폴링 회수 시 새 root span 대신 원본 confirm parent 복원. 14 태스크 31 커밋, `./gradlew test` 746 PASS, 리뷰 critical/major 0 (minor 3 후속 등재). 영구 문서 5개 갱신 (CONFIRM-FLOW / ARCHITECTURE / STRUCTURE / PITFALLS / TODOS) | 2026-05-29 |
| `cleanup-batch-b/` | 빌드·테스트 게이트 위생 (이슈 #81) — (A) spotbugsTest 위반 5건 **전부 코드 정정**(억제 0): NP_NULL 4건은 tearDown redis 체인을 명시 `if (x==null) throw` 가드로(`Objects.requireNonNull`을 SpotBugs 6.0.9가 null-억제자로 인식 못 함), EI_EXPOSE_REP2 1건은 `FakeMessagePublisher` 저장 타입 `Throwable`→`Supplier<? extends Throwable>` 전환. (B) NET-RETRY — `ProductFeignConfig`/`UserFeignConfig` ErrorDecoder에 502/504→`*ServiceRetryableException`(503+Retry-After:5) 승격, 500 비-retryable 유지, 429/503 단일 예외 유지(GET 조회 전용이라 비멱등 재시도 위험 없음). (C) JaCoCo 게이트 실효화 — 설정을 루트 `subprojects` 공통화(payment 개별 블록 제거), `integrationTest` exec 조건부 합산(`tasks.findByName` 가드), `jacocoTestCoverageVerification`에 서비스별 LINE `minimum`(ext `jacoco.lineCoverageMinimum`, element=BUNDLE: payment 0.89/pg 0.91/product 0.40/user 0.0), 측정 대상 정책(infra 제외) 유지. Gradle 8.10→8.14.4(Java 24 호환, 독립 커밋). 6태스크, review major 1(C-2 범위 밖 부채 혼입→커밋 분리)/minor 3, `./gradlew build` 4서비스 GREEN. 영구 문서 4개 갱신(INTEGRATIONS / STACK / TESTING / TODOS) | 2026-05-30 ~ 2026-05-31 |
| `time-model-and-expiry/` | 시간 모델 Clock/Instant 통일 + 결제 만료 정책 명문화 (PR B, 이슈/브랜치 #83) — 4서비스(payment/pg/product/user) 시간 표준을 JDK `Clock` 빈 + `Instant`로 수렴. payment 자체 포트 `LocalDateTimeProvider`/`SystemLocalDateTimeProvider` 폐기(grep 0), 도메인은 `Instant` 인자 주입(now() 직접 호출 0). UTC 저장 일관 — ORM `hibernate.jdbc.time_zone=UTC` + raw-JDBC `connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true` + 명시 UTC Calendar(payment/product dedupe store). payment 도메인 `PaymentEvent`+`PaymentOutbox` 모두 Instant 통일(T17, 경계 ofInstant 변환 6곳 제거). 만료 정책 명문화 — `expire()` READY 가드 + 정합 스캐너 2단 연쇄, 임계 외부화 `payment.expiration.ready-timeout-minutes`(기본30) + 스케줄러 키 `scheduler.payment-expiration.*` 정정(fallback). 벤더 승인 시각 `OffsetDateTime.parse(approvedAtRaw).toInstant()` 정규화(정산 9시간 오차 차단). DM1 `@EnableJpaAuditing(dateTimeProviderRef="clockDateTimeProvider")`로 created_at UTC 일관(만료 cutoff 정합), DM2 product default/docker connectionTimeZone=UTC로 dedupe split-brain 해소. 17태스크 + 가드, `./gradlew test` 846 PASS, 리뷰 critical/major 0. 영구 문서 5개 갱신(PITFALLS / ARCHITECTURE / INTEGRATIONS / CONVENTIONS / TODOS). 후속 등재 [TIME-PRODUCT-NOW-UNIFY] / [TZ-UTC-BACKSTOP] / [BASEENTITY-AUDIT-SOURCE] | 2026-06-01 ~ 2026-06-03 |
| `time-model-followup/` | 시간 모델 잔여 정합 3건 묶음 (이슈/브랜치 #89) — TIME-MODEL-AND-EXPIRY 이연 후속. (1) product 재고 멱등(`JdbcEventDedupeStore`) 만료 판정 DB `NOW()` → 호출자 주입 `Instant` 통일(DB 시계 의존 제거), 포트 `recordIfAbsent`에 `now` 인자 추가 + `existsValid`(라이브 0건)·미사용 `Clock` 필드 전건 제거. (2) 6서비스 TZ backstop 3겹 — Dockerfile `ENV TZ=UTC` + JVM `-Duser.timezone=UTC` + compose `environment.TZ=UTC`(eureka는 infra compose). (3) payment `BaseEntity` audit 컬럼(`created_at/updated_at/deleted_at`) `LocalDateTime` → `Instant` + Flyway V4 `DATETIME` → `DATETIME(6)` 승급 + `clockDateTimeProvider` `Instant` 반환, 엔티티 매핑 경계 수동 `.toInstant(UTC)` 제거. 18태스크, payment·product 단위+통합 857 PASS, 리뷰 critical/major 0(minor 5 처리). V4 Flyway V1→V4 실제 적용 docker 검증 통과. 영구 문서 2개 갱신(PITFALLS §6 / ARCHITECTURE) | 2026-06-06 ~ 2026-06-07 |
| `ci-pipeline-redesign/` | CI 파이프라인 서비스별 fan-out 재설계 + 빌드·게이트 위생 4건 흡수 (이슈/브랜치 #91) — 단일 2-job `ci.yml`을 6서비스(payment/pg/product/user/gateway/eureka) 독립 파이프라인으로 fan-out. (D1/D2) 재사용 워크플로우 `_service-ci.yml`(`workflow_call`, 입력 `service`/`has-integration`) 신규 — `build-test-lint` job(`build -x integrationTest`로 통합 분리) + `integration-test` job(통합 보유 서비스만). (D3) `org.gradle.test-retry` 1.6.5 통합테스트 한정 `maxRetries=2 maxFailures=3` + Testcontainers reuse. (D4) 취합 `report` job이 `report-comment.js`로 6서비스 커버리지/lint 단일 PR 통합 코멘트 조립(`lint-summary.js` 폐기), reviewdog 인라인 서비스별 유지, Discord 제거. (D5) 액션 8종 Node 24 메이저 상향. (D6) Groovy `exceptionFormat "full"`→`= 'full'` 4곳. (D7) 커버리지 게이트 — **review에서 단위 기준으로 재정의**(통합 exec 합산 제거, payment 0.86/pg 0.93/product 0.43/user 0.97, gateway·eureka 0.0). (D8) user `FlywayDockerProfileTest`(product 동형 seed 차단 가드) + `UserQueryUseCaseTest` 신규로 user 통합 보유 전환. 7태스크, 단위 341 + 통합 48 PASS, 리뷰 critical 2/major 2(전건 해소, 게이트 함정·취합 코멘트 파싱 버그·회귀 복원)→재리뷰 pass. 영구 문서 2개 갱신(STACK CI 파이프라인 섹션 / TESTING 게이트 단위 기준) | 2026-06-08 |
| `observability-completion/` | 관측성 완성 — 대시보드 2종 + 로그 기반 추적 진입 + 신규 메트릭 (이슈/브랜치 #94) — 이미 떠 있던 관측 백엔드(Prometheus/Grafana/Tempo/Loki/Promtail/kafka-exporter)의 "마지막 한 칸"을 채워 운영 3대 질문(결제 흐름 정상성/서버별 자원/주문 추적)에 즉답. (D12) 대시보드 2분할 — `business-dashboard.json`(funnel·전이·상태분포·격리·벤더latency·DLQ·outbox·cleanup·코디네이터, 기존 `payment-dashboard.json` 폐기) + `system-dashboard.json`(`$application` 6서비스 JVM/GC/HTTP/Hikari/lag). (D2) 추적 진입을 span 속성 부착이 아닌 **로그 기반**(orderId→Loki→traceId derivedField→Tempo)으로 결정 — 코드 변경 0, discuss 중 사용자 단순화. (D16) `KafkaConsumerConfig` listener observation 1줄로 컨슈머 처리 로그 traceId 연속성 확보(복구/좀비 경로, 라이브 13건 실증). (D15) `KafkaProducerConfig` EOS factory Micrometer 리스너 1줄 → `kafka_producer_txn_*` 노출. (D10/D11) Tempo `metrics_generator`(서비스 그래프·span RED) + exemplar 3점(앱 histogram·Prometheus flag·Grafana 링크). (D3) 샘플링 기본 1.0. (D13/D14) 신규 카운터 — `payment_confirm_guard_skip_total`(eager 6종)·`*_dedupe.cleanup_failed_total`. review에서 funnel 카운터(`payment_event_published/terminal_total`)가 실재하지 않던 죽은 참조임을 발견해 신규 구현(발행=`PaymentCreateUseCase` / 종결=`PaymentStatusMetricsAspect` `isTerminal()` SSOT). T0~T10 11태스크 + review 3라운드(critical/major 0), 512/512 PASS, 라이브 스모크 AC1~AC6 PASS(벤더 latency만 prod 트래픽 의존). 영구 문서 2개 갱신(STACK 관측성 / ARCHITECTURE 메트릭) | 2026-06-10 ~ 2026-06-11 |
| `cleanup-batch-c/` | 코드 레벨 정리 — 미사용 코드 제거 + 워커 보일러플레이트 헬퍼화 + 테스트 헬퍼 위치 통일 (이슈/브랜치 #98) — 동작 불변(behavior-preserving) 단일 PR. (A) 데드 3종 grep 0건 제거: pg `PgVendorCallService.callVendor`(deprecated forRemoval, main 호출 0)·`PgOutbox.incrementAttempt`(전수 스캔 발굴), payment `PaymentHistoryRepository.countTransitionsByStatusWithinWindow`(+QueryDSL import). callVendor 회귀 테스트는 `invokeVendor`+`applyOutcome` 분리 경로로 동등 보존. (B1) pg Immediate 워커 2종 SmartLifecycle 골격 + `runWithContext` 이중 scope를 `AbstractImmediateWorker<J extends ImmediateJob>` 공통 base로 추출(template method, 워커별 차이는 추상 메서드 위임), Polling 2종은 @Scheduled+DB traceparent 별종이라 비대상(PITFALL 12 회피). (B2 제외) 벤더 전략 헬퍼는 진짜 비종속이 상수 4개뿐이라 추출 가치 없음. (C1) 이질 `FakePaymentEventRepository` 2벌을 `payment.mock` 베이스 1벌로 통합(정합성 가드 8개 의존처 보존, mock 변종 order 중복 append 함정 회피). (C2) `paymentplatform.mock`(bounded 밖) 제거 — `FakeIdempotencyStore`→payment.mock, `FakeStockCachePortAtomicTest`→payment.application 이동. 5태스크 5커밋, 단위 GREEN(payment 512/pg 310)+통합 34, EOS 통합 5 시나리오 PASS, 게이트 discuss/plan 각 2R pass·ship findings 0. 영구 문서 4개 갱신(PAYMENT-FLOW / STRUCTURE / ARCHITECTURE / CONCERNS). 관측: 통합테스트 Testcontainers 잔존 Flyway flaky → CONCERNS C-11 | 2026-06-13 |
| `cleanup-batch-d/` | 빌드·테스트 위생 정리 + 상품 서비스 청소 스케줄러 운영 활성화 (이슈/브랜치 #100) — 위생 청소 3건 + discuss 게이트에서 발견한 운영 누락 복구 1건. (C-11) payment 통합테스트 flaky 해소 — create-drop 그룹(`BaseIntegrationTest`, JPA 스키마)과 flyway-on 그룹(`StockCompensationRecovery` + dedupe 3개)이 `withReuse(true)` 재사용 컨테이너의 같은 DB명 `payment-test`를 공유하다 `Found non-empty schema but no history`로 ApplicationContext 로드 실패하던 것을, flyway-on 그룹에 전용 DB명(`payment-scr-test`/`payment-dedupe-{,-roundtrip,-cleanup}-test`) 부여로 DB 레벨 격리(`PaymentEosIntegrationTest`의 `payment-eos-test` 선례 일반화, baseline-on-migrate는 마이그레이션 검증 무력화로 기각). (GROOVY) `build.gradle` 5곳 `events` 공백 할당 → 리스트 할당(Gradle 10 제거 대비). (운영 누락) product `DedupeCleanupWorker`가 `SchedulerConfig`(`@EnableScheduling`+`@ConditionalOnProperty scheduler.enabled`) 게이트인데 product `scheduler.enabled=true`가 어느 표준 배포(docker 포함)에도 없어 `stock_commit_dedupe` 만료행이 운영 무한 누적이던 것을 `application-docker.yml`에 플래그 추가로 정상화(cleanup은 `expires_at<now` 멱등 DELETE, TTL P8D>retention 7d라 도메인 무관). (SCHED-DOC) STACK.md 스케줄러 활성화 정책 절 신설(게이트=SchedulerConfig, 서비스별 매트릭스: payment docker/benchmark·product docker·pg 게이트없이 항상). 4태스크(전부 tdd=false·domain_risk=false), `./gradlew build --rerun-tasks` 전건 GREEN(C-11 재현 0)+린트 통과, ship 리뷰 critical 0/major 2(STACK 사실오류·pg 누락 해소)/minor 1(metrics 보강). 영구 문서 4개 갱신(STACK / TESTING / TODOS / CONCERNS) | 2026-06-14 |
| `k6-async-benchmark/` | 비동기 결제 경로 k6 부하 측정 자산 신규 + 병목 분석 사이클 (이슈/브랜치 #102) — Java 애플리케이션 코드 무변경, 측정 인프라/스크립트만 추가. 신규 자산: `scripts/k6/{helpers,async-payment,run-benchmark,verify-settlement,sweep}` + `scripts/bench-seed-stock.sh` + `docker/docker-compose.benchmark.yml`(payment benchmark profile + JVM heap 상한 + 포트 노출 + Hikari/reconciler env, pg fake gateway 재사용). **baseline**: 저/고지연 2환경(Fake latency)으로 비동기 흡수 입증 — 벤더 지연 5배(100-300→800-1500ms)에도 confirm 동기 응답 p95 38→21ms 불변, e2e 완료만 582ms→1.62s 증가, 양환경 checks 100%·유실 0. **병목 분석**: (사이클1 동기 confirm) Hikari DB 풀(30) 병목 knee~150 req/s(active 30 상한+pending 64~121) → 풀 30→60 처방 → knee 300 이동·p95 65~87%↓·pending 0. (사이클2 비동기 e2e) 파이프라인 처리량 병목 없음(events.confirmed lag·outbox pending·pg lag 모두 ≈0) — 비동기 흡수 효과. 측정 중 reconciler 30s 과단축이 유발한 재confirm cascade(중복 events.confirmed) + DLT 토픽 suffix 갭(`create-topics.sh` `.dlq` vs Spring `DeadLetterPublishingRecoverer` 기본 `-dlt`) → consumer 영구 블로킹(lag 33000 정체) 발견·처방(reconciler 완화+DLT 토픽+offset reset), CONCERNS C-12 등재. e2e 지연 실제 원인은 status 폴링 자가부하(http_reqs 664/s)+단일 인스턴스 CPU. 실환경 디버깅 6건(컨테이너명 동적탐색/`{data:}` 래퍼 파싱/JVM heap·포트 OOM/threshold 허용/bench-seed 멱등/VU·부하곡선 env). 환경 한계 — 로컬 7.65GB 단일 인스턴스 OOM으로 baseline 100→200→400 재현 불가(peak 하향·gateway 우회·관측성 off), 절대 TPS 무의미 상대 비교만 유효. 7태스크+병목 2사이클, 11커밋, Java 무변경(test n/a), discuss R2·plan R2 pass, ship findings critical 0/major 0/minor 3(스킵). 영구 문서 1개 갱신(CONCERNS C-12). 후속(다음 목표): payment scale-out 처리량 측정 / DLT suffix 갭 버그 수정 / status 폴링→push 측정 개선 | 2026-06-15 |
| `capacity-and-scaleout/` | 결제 처리량 부하 측정 2페이즈 + scale-out 검증 (이슈/브랜치 #104) — K6-ASYNC 연장, Java 무변경(DLT resolver 1건 제외). **페이즈 1(단일 인스턴스)**: 동기 confirm 병목=Hikari 풀(풀 30→60→80 = knee 300→350→450, DB 미천장), 비동기 e2e 75req/s 완벽 흡수, 폴링 backoff+지터 win-win(폴링부하 28%↓·체감 27%↓). **페이즈 2(scale-out 1→2)**: `hostname` 라인 제거로 transactional.id 고유화 후 측정 → **scale-out 기각**(confirm 1.0×/e2e 1.3× < 합격 1.6×). 양쪽 Hikari 풀(합 160) 포화해도 처리율 정체 + CPU 5.5/10 여유 → 병목이 인스턴스-로컬 풀에서 **공유 DB 경합**(MySQL lock/IO + Kafka EOS commit 직렬화)으로 이동 = "scale-out 전제는 공유 자원 동반 스케일". fencing 고유화 정상(중복0·분산편차0.7%·rebalance 안전), 의도적 id 충돌은 rebalance overlap 순간만 ProducerFenced(consumer-initiated EOS txn.id=prefix+group+topic+partition → 정상 배타 파티션 무탈). 정상 운영 재고 정합 완벽(redis==RDB), 충돌 시 fencing in-flight 미세 갭(0.1%대 — ship 2-run 실측으로 reconciler cascade·보상누락 둘 다 반증, 실제는 stock-committed EOS abort로 IN_PROGRESS in-flight 비대칭). USL N≤2 한계로 제약식만(confirm Nmax≈1.4). DLT `.dlq` resolver로 C-12 해소(측정 선결). 신규 `scripts/usl-fit.py`. 10태스크, ship critical0/major1(재고 갭 원인 정정)/minor8 전부 반영. 영구 문서 2개 갱신(CONCERNS C-12 해소·TODOS T4-E 등재) | 2026-06-19 |
| `stock-compensation-other-paths/` | 재고 보상 경로 정리: 死 코드 제거 + 확정 진입 보상 폐기 (이슈/브랜치 #106, TQ-7) — 경로 2(실패 보상 가드 `executePaymentFailureCompensationWithOutbox`)가 ADR-04(`OutboxProcessingService` 삭제)로 운영 호출처 0의 死 코드임이 드러나, 형제 outbox 처리 3메서드(Success/Retry/Quarantine WithOutbox) + `canCompensateStock` 가드 + `STOCK_COMPENSATE_*` 이벤트 3종 + `StockCachePort.increment` 포트·어댑터·Fake까지 동반 제거. 경로 1(`OutboxAsyncConfirmService.compensateStock`)은 확정 TX 실패 시 단일 increment swallow(침묵 손실) + `decrement:done` 토큰 미정리로 재확정 시 과매도 잠재 결함 — discuss 게이트에서 '롤백(토큰 DEL)'안이 confirm 동시성 직렬화(Idempotency-Key) 부재로 동시 confirm·롤백실패 과매도를 연다는 domain-expert major 2로 기각, **보상 폐기(재고 차감 유지)**로 전환: 재고·토큰 무접촉으로 과매도 0, 미복구는 `StockRetentionMetrics`/`STOCK_RETENTION_UNRECOVERED` 가시화 + 원본 예외 전파(누수는 redis<RDB 보수적 갭, reconciler TC-3 위임). 통합 테스트 3종(재확정 이중차감 0/동시 confirm 차감 1회/교차 케이스)으로 과매도 0 불변식 회귀 가드. 4태스크, 단위 490+통합 37 PASS, discuss R2·plan R2 pass·ship 리뷰 critical 0(보상 폐기 백본 양쪽 pass, stale 문서·주석·tautology 단언 정정). 영구 문서 4개 갱신(CONFIRM-FLOW/PITFALLS/ARCHITECTURE/TODOS). 후속: TQ-8(RETRYING 상태 머신 死 코드 정리)/TC-3(재고 reconciler) | 2026-06-21 |
| `cleanup-batch-e/` | 비동기 confirm 死 코드 정리 + Fake PG 멱등성 시뮬 (이슈/브랜치 #108, TQ-8+TC-9) — **payment-service**: RETRYING 진입 경로(`markPaymentAsRetrying`) 운영 호출처 0 으로 `RETRYING` enum 케이스 + 상태 머신 가드(`done`/`fail`/`canApplyConfirmResult`/`isTerminal`)의 RETRYING 절 + 동반 死 RETRY_ATTEMPT 이벤트 체인(`action="retry"` 사용처 0) + `PaymentOutbox.toFailed` + 재고 캐시 단건 API 5종(`current` 포함) + `INVALID_STATUS_TO_FAILED` + `stock_decrement.lua` 제거. `retryCount` 필드/`FAILED` enum/`INVALID_STATUS_TO_RETRY` 보존, DB 잔존 row 위험은 진입 호출처 0 + Flyway 시드/제약 0 으로 갈음(RETRY_ATTEMPT 도 동일 논거). **pg-service**: main `FakePgGatewayStrategy` + test mock `FakePgGatewayAdapter` 양쪽에 동일 paymentKey 재호출 시 중복 승인 응답(`DuplicateApprovalDetectedEvent` 발행 + `PgGatewayDuplicateHandledException`, `ConcurrentHashMap` atomic 첫호출 판정) 멱등 시뮬 추가 — 실 벤더 이벤트+예외 이중 신호 재현. self-loop -> `DuplicateApprovalHandler` 흡수 -> APPROVED 유지 + 재고 추가차감 0 통합 테스트(`PgSelfLoopDuplicateAbsorptionIntegrationTest`, 이벤트+예외 양 경로 검증, outbox row 카운트 비의존). 5태스크, payment 450단위+37통합 / pg 316단위+8통합 PASS, discuss R2·plan R2·ship R2 전부 pass(critical 0 / major 3 전부 처리 / minor 0). 영구 문서 6개 갱신(CONFIRM-FLOW/PAYMENT-FLOW/PITFALLS/TESTING/error-logging/TODOS) | 2026-06-21 |
| `retry-metric-cleanup/` | payment 재시도 metric 잔재 정리: 死 metric 전면 제거 (이슈/브랜치 #110, TQ-8 후속) — 비동기 confirm 재시도 주도권이 pg-service self-loop로 이전(PR #106/ADR-04)되며 payment 는 확정 결과(APPROVED/FAILED)만 수신, `payment_event.retry_count` 를 증가시키는 경로가 사라져 항상 0 인 死 값이 됨(CLEANUP-BATCH-E 가 후속 분리로 보존했던 잔재). 3태스크 제거: (1) `max_retry_reached` 게이지 경로(게이지 + `maxRetryCount` @Value + `max-retry-count` 설정키 + `countByRetryCountGreaterThanEqual` 포트·Jpa·Impl·Fake), (2) `payment_event.retry_count` 데이터 경로(V5 컬럼 DROP + 도메인 필드 + 엔티티 매핑 + 응답 DTO 2종 + admin HTML 2종 + 테스트), (3) 재시도 로깅 死 enum 2종(`PAYMENT_RETRY_COUNT_INCREASED`/`PAYMENT_RETRY_START`). 이름만 같은 `payment_outbox.retry_count`(살아있는 Kafka 발행 재시도)·`RetryPolicy`·`stuck_in_progress` 게이지 보존 — `PaymentEvent.retryCount` 필드 제거 시 컴파일러가 PaymentOutbox 빌더와 자동 구분이 핵심 안전장치. V5 는 plain `DROP COLUMN`(MySQL 컬럼 단위 `IF EXISTS` 미지원, flyway-on 통합테스트가 적용 정합 자동 검증). admin retryCount 는 자체 Thymeleaf SSR 이라 외부 호환 무관 전부 제거. 3태스크(전부 tdd=false, Task2 domain_risk), 단위 450+통합 37 PASS + checkstyle/spotbugs 통과, discuss R2·plan R2·ship 1R pass(critical 0/major 0/minor 1 스킵=V5 멱등성 MySQL 제약). 영구 문서 1개 갱신(TODOS) | 2026-06-22 |
| `confirm-approved-resend-gap/` | 비동기 confirm APPROVED 재고 확정 재발행 갭 수정 (이슈/브랜치 #112) — APPROVED 경로에서 RDB DONE 커밋 후 EOS 발행(stock-committed)이 유실되면 재배달이 D7 종결 가드에 막혀 영구 유실 → product 차감 누락 → redis 선차감과 RDB 발산 → 오버셀 가능. 설계 SSOT가 믿던 "affected==0(중복) 시 발행 항상 진행" crash 내성 분기가 **도달 불가 dead branch**임이 드러남(dedupe 마킹 `markIfAbsent`과 종결 전이 `markPaymentAsDone`이 같은 JPA tx 원자 커밋이라 "dedupe됨+비종결" 불가). **수정**: 재배달이 실제 도착하는 D7 종결 가드 분기에서 `status==DONE && message==APPROVED`(=정상 첫 도착엔 없는 재배달 신호)면 `sendStockCommittedEvents` 재발행 + `terminalResendMetrics(DONE)` 계측, product 결정적 키(`derive(orderId,productId)`, message eventUuid 독립) 멱등 흡수로 차감 1회 — under-publish 위험/over-publish 무해 비대칭 이용. affected==0 분기는 단순 skip화. 순서 뒤집기(발행 먼저)는 producer tx buffer라 원자성 경계 불변으로 무효(미채택). **실증**: Toxiproxy는 임베디드 Kafka 하니스라 부적합 → `commitTransaction` 1회 실패 주입 시드(`CommitFailureInjectingProducerPostProcessor`, 운영 코드 무변경)로 JPA inner 선커밋→Kafka outer 실패 윈도우 결정적 재현. #6(복구: 재배달→재발행→차감 1회) 설계대로 증명. #7(지속실패)이 **설계 S2 가설 2중 반증** — commitTransaction 실패는 `DefaultErrorHandler`(DLQ) 아닌 컨테이너 디폴트 `AfterRollbackProcessor`(9회, DLQ 미진입) 경로 + 재발행도 같은 EOS tx라 지속 실패 시 "중복 0"이 아니라 **완전 유실**(payment DONE인데 재고 확정 영구 소실) → TC-13-FOLLOW-7 등재. 3태스크(Task2·3 domain_risk), 단위 457+통합 39 PASS(통합 `--rerun-tasks` 강제)+린트 통과, discuss R2·plan R1·ship 리뷰 pass(critical 0/major 1 doc-sync/minor 3 전부 처리). 영구 문서 5개 갱신(CONFIRM-FLOW/CONCERNS/PITFALLS/TODOS/ARCHITECTURE). 후속: TC-13-FOLLOW-7(EOS 커밋 지속 실패 DLQ 경로) | 2026-06-22 |
