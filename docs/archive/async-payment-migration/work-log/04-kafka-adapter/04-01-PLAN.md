---
phase: 04-kafka-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/compose/docker-compose.yml
  - build.gradle
  - src/main/resources/application.yml
  - src/main/resources/application-docker.yml
  - src/test/java/com/hyoguoo/paymentplatform/core/test/BaseKafkaIntegrationTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerTest.java
autonomous: true
requirements:
  - KAFKA-01

must_haves:
  truths:
    - "Docker Compose에 kafka(bitnami/kafka:3.9 KRaft) 및 kafka-ui(kafbat) 서비스가 정의되어 있다"
    - "build.gradle에 spring-kafka 및 testcontainers:kafka 의존성이 추가되어 있다"
    - "application.yml에 spring.kafka.* 기본 설정이 추가되어 있다"
    - "BaseKafkaIntegrationTest 추상 클래스가 MySQL + Kafka 컨테이너를 정적 필드로 보유한다"
    - "KafkaConfirmAdapterTest 스텁이 컴파일 오류 없이 존재한다 (RED — 구현 전 실패)"
    - "KafkaConfirmListenerTest 스텁이 컴파일 오류 없이 존재한다 (RED — 구현 전 실패)"
  artifacts:
    - path: "docker/compose/docker-compose.yml"
      provides: "kafka + kafka-ui 서비스 정의"
      contains: "bitnami/kafka:3.9"
    - path: "build.gradle"
      provides: "spring-kafka + testcontainers:kafka 의존성"
      contains: "spring-kafka"
    - path: "src/test/java/com/hyoguoo/paymentplatform/core/test/BaseKafkaIntegrationTest.java"
      provides: "Kafka+MySQL 통합 테스트 기반 클래스"
      exports: ["BaseKafkaIntegrationTest"]
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java"
      provides: "KafkaConfirmAdapter 단위 테스트 스텁"
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerTest.java"
      provides: "KafkaConfirmListener 단위 테스트 스텁"
  key_links:
    - from: "docker/compose/docker-compose.yml"
      to: "kafka-ui"
      via: "depends_on kafka"
      pattern: "kafka-ui.*depends_on.*kafka"
    - from: "BaseKafkaIntegrationTest"
      to: "spring.kafka.bootstrap-servers"
      via: "@DynamicPropertySource"
      pattern: "KAFKA_CONTAINER::getBootstrapServers"
---

<objective>
Phase 4 인프라 기반 세팅: Docker Compose에 Kafka(KRaft, bitnami/kafka:3.9) + kafbat/kafka-ui를 추가하고, build.gradle에 spring-kafka + testcontainers kafka 의존성을 추가한다. application.yml에 spring.kafka.* 기본 설정을 추가하고, 통합 테스트 기반 클래스(BaseKafkaIntegrationTest)와 Wave 0 단위 테스트 스텁을 생성한다.

Purpose: Plan 02(KafkaConfirmAdapter), Plan 03(KafkaConfirmListener), Plan 04(통합 테스트)가 컴파일 오류 없이 실행될 수 있는 의존성과 테스트 스캐폴딩을 확보한다.
Output: docker-compose.yml 수정, build.gradle 수정, application.yml 수정, BaseKafkaIntegrationTest.java, KafkaConfirmAdapterTest.java 스텁, KafkaConfirmListenerTest.java 스텁
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kafka-adapter/04-CONTEXT.md
@.planning/phases/04-kafka-adapter/04-RESEARCH.md

<interfaces>
<!-- 기존 BaseIntegrationTest (확장 기반) -->
<!-- src/test/java/com/hyoguoo/paymentplatform/core/test/BaseIntegrationTest.java -->
```java
@SpringBootTest
@ActiveProfiles("test")
@Testcontainers
public abstract class BaseIntegrationTest {
    @Container
    protected static final MySQLContainer<?> MYSQL_CONTAINER = new MySQLContainer<>("mysql:8.0")
            .withDatabaseName("payment-test")
            .withUsername("test")
            .withPassword("test")
            .withCommand("--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci");

    @DynamicPropertySource
    static void properties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", MYSQL_CONTAINER::getJdbcUrl);
        registry.add("spring.datasource.username", MYSQL_CONTAINER::getUsername);
        registry.add("spring.datasource.password", MYSQL_CONTAINER::getPassword);
        registry.add("spring.datasource.driver-class-name", () -> "com.mysql.cj.jdbc.Driver");
        registry.add("scheduler.enabled", () -> "true");
        registry.add("scheduler.payment-status-sync.enabled", () -> "false");
    }
}
```

<!-- OutboxConfirmAdapterTest 패턴 — KafkaConfirmAdapterTest 스텁 참고 -->
```java
// 단위 테스트: Mockito.mock() 사용, @SpringBootTest 없음
class OutboxConfirmAdapterTest {
    private OutboxConfirmAdapter outboxConfirmAdapter;
    private PaymentTransactionCoordinator mockTransactionCoordinator;
    // ...
    @BeforeEach
    void setUp() {
        mockTransactionCoordinator = Mockito.mock(PaymentTransactionCoordinator.class);
        outboxConfirmAdapter = new OutboxConfirmAdapter(mockTransactionCoordinator, ...);
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Docker Compose + build.gradle + application.yml 인프라 세팅</name>
  <files>
    docker/compose/docker-compose.yml,
    build.gradle,
    src/main/resources/application.yml,
    src/main/resources/application-docker.yml
  </files>
  <behavior>
    - build.gradle: `implementation 'org.springframework.kafka:spring-kafka'` 추가 (dependencies 블록, Spring Boot Core 섹션 아래)
    - build.gradle: `testImplementation "org.testcontainers:kafka:${testcontainersVersion}"` 추가
    - application.yml: spring.kafka.* 기본 설정 블록 추가 (bootstrap-servers: localhost:9092, producer/consumer/listener 설정)
    - application-docker.yml: spring.kafka.bootstrap-servers: kafka:9092 추가 (docker 프로파일 전용)
    - docker-compose.yml: kafka 서비스 추가 (bitnami/kafka:3.9, KRaft 단일 노드, 포트 9092, healthcheck 포함)
    - docker-compose.yml: kafka-ui 서비스 추가 (ghcr.io/kafbat/kafka-ui:latest, 포트 8081:8080, kafka 의존)
    - docker-compose.yml: app.depends_on에 kafka: condition: service_healthy 추가
    - docker-compose.yml volumes에 별도 볼륨 불필요 (KRaft 단일 노드는 무상태로 운영)
  </behavior>
  <action>
    build.gradle `dependencies` 블록에 두 줄 추가:
    ```groovy
    // Kafka
    implementation 'org.springframework.kafka:spring-kafka'
    // Test
    testImplementation "org.testcontainers:kafka:${testcontainersVersion}"
    ```

    application.yml에 spring.kafka 블록 추가 (spring.payment 블록 아래):
    ```yaml
    spring:
      kafka:
        bootstrap-servers: localhost:9092
        producer:
          key-serializer: org.apache.kafka.common.serialization.StringSerializer
          value-serializer: org.apache.kafka.common.serialization.StringSerializer
        consumer:
          group-id: payment-confirm-group
          auto-offset-reset: earliest
          key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
          value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
          enable-auto-commit: false
        listener:
          ack-mode: RECORD
    ```

    application-docker.yml의 spring 블록에 kafka 추가:
    ```yaml
      kafka:
        bootstrap-servers: kafka:9092
    ```

    docker-compose.yml services 섹션에 kafka + kafka-ui 추가:
    ```yaml
      kafka:
        image: bitnami/kafka:3.9
        container_name: payment-kafka
        ports:
          - "9092:9092"
        environment:
          KAFKA_CFG_NODE_ID: 1
          KAFKA_CFG_PROCESS_ROLES: broker,controller
          KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
          KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
          KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
          KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
          KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
          KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE: "true"
          KAFKA_CFG_NUM_PARTITIONS: 3
          KAFKA_CFG_DEFAULT_REPLICATION_FACTOR: 1
        networks:
          - payment-network
        healthcheck:
          test: ["CMD-SHELL", "kafka-topics.sh --bootstrap-server localhost:9092 --list"]
          interval: 30s
          timeout: 10s
          retries: 5

      kafka-ui:
        image: ghcr.io/kafbat/kafka-ui:latest
        container_name: payment-kafka-ui
        ports:
          - "8081:8080"
        environment:
          KAFKA_CLUSTERS_0_NAME: payment-local
          KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
        networks:
          - payment-network
        depends_on:
          kafka:
            condition: service_healthy
    ```

    app.depends_on에 추가:
    ```yaml
      kafka:
        condition: service_healthy
    ```
  </action>
  <verify>
    <automated>./gradlew compileJava compileTestJava --no-daemon 2>&1 | tail -5</automated>
  </verify>
  <done>./gradlew compileJava compileTestJava 성공. build.gradle에 spring-kafka + testcontainers:kafka 선언됨. application.yml에 spring.kafka 블록 존재. docker-compose.yml에 kafka + kafka-ui 서비스 정의됨.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BaseKafkaIntegrationTest + Wave 0 테스트 스텁 생성</name>
  <files>
    src/test/java/com/hyoguoo/paymentplatform/core/test/BaseKafkaIntegrationTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerTest.java
  </files>
  <behavior>
    BaseKafkaIntegrationTest:
    - MYSQL_CONTAINER (mysql:8.0) + KAFKA_CONTAINER (confluentinc/cp-kafka:7.4.0 with .withKraft()) 정적 필드
    - @DynamicPropertySource: datasource + bootstrap-servers + spring.payment.async-strategy=kafka 오버라이드
    - scheduler.enabled=true, scheduler.payment-status-sync.enabled=false 유지
    - 임포트: org.testcontainers.containers.KafkaContainer (1.19.8 패키지 경로)

    KafkaConfirmAdapterTest (스텁 — RED):
    - OutboxConfirmAdapterTest 구조 복제, KafkaConfirmAdapter 클래스명으로 변경
    - 테스트 메서드 3개: confirm_CallsExecutePayment_Once(), confirm_CallsExecuteStockDecreaseOnly_Once(), confirm_Returns_Async202()
    - @ConditionalOnProperty havingValue="kafka" 검증 테스트 1개 추가
    - KafkaTemplate mock 주입 포함
    - 스텁 단계: KafkaConfirmAdapter 클래스가 아직 없으므로 컴파일 오류 발생 예정 → Plan 02에서 해소

    KafkaConfirmListenerTest (스텁 — RED):
    - @Slf4j, @Component, @RequiredArgsConstructor 패턴 확인 테스트 포함
    - consume() 성공 케이스: consume_CallsConfirmPaymentWithGateway_Once()
    - consume() 실패 케이스: consume_WhenNonRetryable_DoesNotThrow() (DLT로 라우팅)
    - handleDlt() 케이스: handleDlt_CallsFailureCompensation_Once()
    - @RetryableTopic attempts="6" 검증: retryableTopic_HasAttempts6()
    - 스텁 단계: KafkaConfirmListener 클래스 없으므로 컴파일 오류 발생 예정 → Plan 03에서 해소
  </behavior>
  <action>
    BaseKafkaIntegrationTest.java 생성:
    ```java
    package com.hyoguoo.paymentplatform.core.test;

    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.test.context.ActiveProfiles;
    import org.springframework.test.context.DynamicPropertyRegistry;
    import org.springframework.test.context.DynamicPropertySource;
    import org.testcontainers.containers.KafkaContainer;
    import org.testcontainers.containers.MySQLContainer;
    import org.testcontainers.junit.jupiter.Container;
    import org.testcontainers.junit.jupiter.Testcontainers;
    import org.testcontainers.utility.DockerImageName;

    @SpringBootTest
    @ActiveProfiles("test")
    @Testcontainers
    public abstract class BaseKafkaIntegrationTest {

        @Container
        protected static final MySQLContainer<?> MYSQL_CONTAINER = new MySQLContainer<>("mysql:8.0")
                .withDatabaseName("payment-test")
                .withUsername("test")
                .withPassword("test")
                .withCommand("--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci");

        @Container
        protected static final KafkaContainer KAFKA_CONTAINER =
                new KafkaContainer(DockerImageName.parse("confluentinc/cp-kafka:7.4.0"))
                        .withKraft();

        @DynamicPropertySource
        static void properties(DynamicPropertyRegistry registry) {
            registry.add("spring.datasource.url", MYSQL_CONTAINER::getJdbcUrl);
            registry.add("spring.datasource.username", MYSQL_CONTAINER::getUsername);
            registry.add("spring.datasource.password", MYSQL_CONTAINER::getPassword);
            registry.add("spring.datasource.driver-class-name", () -> "com.mysql.cj.jdbc.Driver");
            registry.add("spring.kafka.bootstrap-servers", KAFKA_CONTAINER::getBootstrapServers);
            registry.add("spring.payment.async-strategy", () -> "kafka");
            registry.add("scheduler.enabled", () -> "true");
            registry.add("scheduler.payment-status-sync.enabled", () -> "false");
        }
    }
    ```

    KafkaConfirmAdapterTest.java 스텁 생성 (OutboxConfirmAdapterTest 패턴 준수):
    - 패키지: com.hyoguoo.paymentplatform.payment.infrastructure.adapter
    - @DisplayName("KafkaConfirmAdapter 테스트")
    - 필드: mockTransactionCoordinator, mockPaymentLoadUseCase, mockPaymentCommandUseCase, mockKafkaTemplate
    - KafkaTemplate<String, String> mockKafkaTemplate = Mockito.mock(KafkaTemplate.class)
    - 각 테스트는 `given(...).willReturn(...)` 세팅 후 adapter.confirm(command) 호출 → then(...).should(times(1))...
    - KafkaConfirmAdapter 클래스가 없으므로 이 파일은 컴파일 오류 상태(RED). Plan 02에서 해소됨.

    KafkaConfirmListenerTest.java 스텁 생성:
    - 패키지: com.hyoguoo.paymentplatform.payment.listener
    - @DisplayName("KafkaConfirmListener 테스트")
    - 필드: mockPaymentLoadUseCase, mockPaymentCommandUseCase, mockTransactionCoordinator
    - 테스트 구조: OutboxWorkerTest 패턴 참고
    - KafkaConfirmListener 클래스가 없으므로 컴파일 오류 상태(RED). Plan 03에서 해소됨.
  </action>
  <verify>
    <automated>MISSING — KafkaConfirmAdapter, KafkaConfirmListener 클래스가 Plan 02/03에서 생성된 후 ./gradlew test --tests "*.KafkaConfirmAdapterTest" --tests "*.KafkaConfirmListenerTest" 실행 가능</automated>
  </verify>
  <done>BaseKafkaIntegrationTest.java 파일 존재 및 KafkaContainer 정적 필드 선언됨. KafkaConfirmAdapterTest.java, KafkaConfirmListenerTest.java 파일이 올바른 패키지에 존재하며 테스트 메서드 스텁이 작성되어 있다 (구현 클래스 없어 RED 상태 허용).</done>
</task>

</tasks>

<verification>
- `./gradlew compileJava` 성공 (spring-kafka 의존성 해소)
- `docker/compose/docker-compose.yml`에 kafka, kafka-ui 서비스 존재
- `src/test/java/.../core/test/BaseKafkaIntegrationTest.java` 존재 및 KafkaContainer 선언
- `src/test/java/.../payment/infrastructure/adapter/KafkaConfirmAdapterTest.java` 존재
- `src/test/java/.../payment/listener/KafkaConfirmListenerTest.java` 존재
</verification>

<success_criteria>
- Docker Compose에 kafka(bitnami/kafka:3.9 KRaft) + kafka-ui(kafbat, 8081) 서비스 정의 완료
- build.gradle에 spring-kafka + testcontainers:kafka 의존성 추가 완료
- application.yml에 spring.kafka.* 설정 블록 추가 완료
- BaseKafkaIntegrationTest 추상 클래스 생성: MySQL + Kafka 컨테이너, @DynamicPropertySource
- Wave 0 테스트 스텁 2개 생성 (RED 상태 허용 — 구현 클래스는 Plan 02/03에서 생성)
</success_criteria>

<output>
After completion, create `.planning/phases/04-kafka-adapter/04-01-SUMMARY.md`
</output>
