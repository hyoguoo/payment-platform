---
phase: 04-kafka-adapter
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - KAFKA-02
  - KAFKA-05

must_haves:
  truths:
    - "REQUIREMENTS.md KAFKA-02가 실제 구현 토픽명 'payment-confirm'을 반영한다"
    - "REQUIREMENTS.md KAFKA-05가 Toss 멱등키 위임 방식을 반영한다"
    - "REQUIREMENTS.md와 구현 코드 사이의 문서 불일치가 해소된다"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "KAFKA-02, KAFKA-05 요구사항 — 실제 구현과 일치하는 명세"
      contains: "payment-confirm"
  key_links:
    - from: ".planning/REQUIREMENTS.md (KAFKA-02)"
      to: "KafkaConfirmAdapter.TOPIC"
      via: "토픽명 일치"
      pattern: "payment-confirm"
    - from: ".planning/REQUIREMENTS.md (KAFKA-05)"
      to: "KafkaConfirmListener (Toss 멱등키 위임)"
      via: "멱등성 방식 명세 일치"
      pattern: "Toss 멱등키"
---

<objective>
REQUIREMENTS.md의 KAFKA-02, KAFKA-05 명세를 실제 구현과 일치하도록 업데이트한다.

두 갭 모두 코드 결함이 아닌 문서 불일치다. 구현 결정은 CONTEXT.md와 STATE.md에 이미 공식 기록되어 있으며, REQUIREMENTS.md만 미업데이트 상태다.

Purpose: REQUIREMENTS.md가 단일 진실 원천(single source of truth)으로 기능하도록 실제 구현과 명세를 동기화한다.
Output: 갱신된 .planning/REQUIREMENTS.md (KAFKA-02, KAFKA-05 항목)
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-kafka-adapter/04-CONTEXT.md
@.planning/phases/04-kafka-adapter/04-VERIFICATION.md

<interfaces>
<!-- 갭 근거: CONTEXT.md의 공식 결정 -->

KAFKA-02 결정 (STATE.md 기록):
  "[Phase 04-kafka-adapter]: KafkaConfirmAdapter.TOPIC = payment-confirm (기존 payment-confirm-requests 변경)
   — DLT 토픽 payment-confirm-dlq로 KAFKA-06 충족"

KAFKA-05 결정 (CONTEXT.md 기록):
  "별도 existsByOrderId 가드 없음 — Toss 멱등키에 위임
  컨슈머는 orderId로 PaymentEvent 조회 후 Toss API 호출,
  이미 DONE 상태라면 Toss가 동일 응답 반환
  PaymentEvent DONE 재기록은 멱등하게 동작하도록 처리"

실제 구현 상태:
  KafkaConfirmAdapter.java: TOPIC = "payment-confirm"
  KafkaConfirmListener.java: @RetryableTopic(topics="payment-confirm", dltTopicSuffix="-dlq")
                              → DLT = "payment-confirm-dlq" (KAFKA-06 충족)
  KafkaConfirmListener.java: existsByOrderId 가드 없음, Toss API 직접 호출
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md KAFKA-02, KAFKA-05 명세 업데이트</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
REQUIREMENTS.md의 KAFKA 섹션에서 두 항목을 수정한다.

**KAFKA-02 변경:**
현재: "`KafkaConfirmAdapter`가 confirm 요청을 `payment-confirm-requests` 토픽에 발행하고 202를 반환한다"
변경: "`KafkaConfirmAdapter`가 confirm 요청을 `payment-confirm` 토픽에 발행하고 202를 반환한다 (DLT 토픽 `payment-confirm-dlq` 충족을 위해 Plan 03에서 변경)"

**KAFKA-05 변경:**
현재: "`existsByOrderId` 가드로 중복 컨슘에 대한 멱등성을 보장한다"
변경: "Toss 결제 API의 멱등키(orderId 기반)에 위임해 중복 컨슘에 대한 멱등성을 보장한다 — 별도 `existsByOrderId` 가드 없음 (CONTEXT.md 결정)"

파일 하단 `Last updated` 주석도 오늘 날짜(2026-03-15)로 갱신한다.
  </action>
  <verify>
    <automated>grep -n "payment-confirm" .planning/REQUIREMENTS.md | grep "KAFKA-02\|payment-confirm-requests\|payment-confirm "
grep -n "Toss" .planning/REQUIREMENTS.md | grep "KAFKA-05\|멱등"</automated>
  </verify>
  <done>
- KAFKA-02 항목에 'payment-confirm-requests' 문자열이 없고 'payment-confirm' 토픽명이 명시되어 있다
- KAFKA-05 항목에 'existsByOrderId 가드' 문자열이 없고 'Toss 멱등키' 위임 방식이 명시되어 있다
- 나머지 요구사항 항목은 변경되지 않는다
  </done>
</task>

</tasks>

<verification>
변경 후 다음을 확인한다:

```bash
# KAFKA-02: 구 토픽명이 제거되었는지
grep "payment-confirm-requests" .planning/REQUIREMENTS.md
# 출력 없음이 정상 (KAFKA-02 항목에서 완전히 제거됨)

# KAFKA-02: 신규 토픽명이 반영되었는지
grep "KAFKA-02" .planning/REQUIREMENTS.md

# KAFKA-05: existsByOrderId 가드 구문이 제거되었는지
grep "existsByOrderId 가드" .planning/REQUIREMENTS.md
# 출력 없음이 정상

# KAFKA-05: Toss 멱등키 위임 방식이 반영되었는지
grep "KAFKA-05" .planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
- REQUIREMENTS.md KAFKA-02: 토픽명이 'payment-confirm'으로 명시되고, 변경 이유(KAFKA-06 DLT 충족)가 기록된다
- REQUIREMENTS.md KAFKA-05: Toss 멱등키 위임 방식이 명시되고, 별도 가드가 없음이 문서화된다
- REQUIREMENTS.md 내 다른 항목은 수정되지 않는다
- 04-VERIFICATION.md에 기록된 두 가지 갭이 해소된다
</success_criteria>

<output>
완료 후 `.planning/phases/04-kafka-adapter/04-05-SUMMARY.md`를 생성한다.
</output>
