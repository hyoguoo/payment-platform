---
phase: 04-kafka-adapter
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerIntegrationTest.java
autonomous: true
requirements:
  - KAFKA-07

must_haves:
  truths:
    - "KafkaConfirmListenerIntegrationTest가 Testcontainers Kafka를 사용해 실제 메시지 발행/소비 흐름을 검증한다"
    - "kafkaTemplate.send() 이후 Awaitility polling으로 PaymentEvent 상태가 DONE으로 전환됨을 확인한다"
    - "중복 메시지(동일 orderId 재발행) 처리 시 Toss 멱등키에 의해 안전하게 처리된다 (DONE 상태 유지)"
    - "통합 테스트가 BaseKafkaIntegrationTest를 상속하여 MySQL + Kafka 컨테이너를 재사용한다"
    - "KafkaConfirmListenerIntegrationTest가 GREEN이다"
  artifacts:
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerIntegrationTest.java"
      provides: "Testcontainers Kafka 통합 테스트"
      exports: ["KafkaConfirmListenerIntegrationTest"]
  key_links:
    - from: "KafkaConfirmListenerIntegrationTest"
      to: "BaseKafkaIntegrationTest"
      via: "extends BaseKafkaIntegrationTest"
      pattern: "extends BaseKafkaIntegrationTest"
    - from: "kafkaTemplate.send"
      to: "KafkaConfirmListener.consume"
      via: "Testcontainers Kafka broker"
      pattern: "await.*PaymentEventStatus.DONE"
---

<objective>
Testcontainers Kafka 통합 테스트 작성: BaseKafkaIntegrationTest를 상속하여 실제 Kafka 브로커에 메시지를 발행하고 KafkaConfirmListener가 소비해 PaymentEvent 상태가 DONE으로 전환되는 E2E 흐름을 검증한다. Awaitility로 비동기 처리 완료를 대기한다.

Purpose: KAFKA-07 요구사항 충족 — Testcontainers Kafka 통합 테스트.
Output: KafkaConfirmListenerIntegrationTest GREEN
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kafka-adapter/04-CONTEXT.md
@.planning/phases/04-kafka-adapter/04-RESEARCH.md
@.planning/phases/04-kafka-adapter/04-02-SUMMARY.md
@.planning/phases/04-kafka-adapter/04-03-SUMMARY.md

<interfaces>
<!-- BaseKafkaIntegrationTest (Plan 01에서 생성) -->
```java
@SpringBootTest
@ActiveProfiles("test")
@Testcontainers
public abstract class BaseKafkaIntegrationTest {
    @Container
    protected static final MySQLContainer<?> MYSQL_CONTAINER = ...;
    @Container
    protected static final KafkaContainer KAFKA_CONTAINER =
            new KafkaContainer(DockerImageName.parse("confluentinc/cp-kafka:7.4.0")).withKraft();

    @DynamicPropertySource
    static void properties(DynamicPropertyRegistry registry) {
        // spring.datasource.* + spring.kafka.bootstrap-servers
        // spring.payment.async-strategy=kafka
    }
}
```

<!-- PaymentEvent 상태 조회를 위한 PaymentLoadUseCase -->
```java
// @Autowired PaymentLoadUseCase paymentLoadUseCase;
// PaymentEvent event = paymentLoadUseCase.getPaymentEventByOrderId(orderId);
// event.getStatus() → PaymentEventStatus.DONE
```

<!-- 기존 통합 테스트 참고 (OutboxWorker 패턴) -->
// BaseIntegrationTest 상속 + @Autowired KafkaTemplate + @Autowired PaymentLoadUseCase
// KafkaTemplate.send("payment-confirm", orderId, orderId)
// Awaitility.await().atMost(30, SECONDS).until(...)

<!-- 테스트 데이터 준비 -->
// OutboxWorker 통합 테스트에서 PaymentEvent 초기화 방식 참고
// 데이터 준비 방법: @Sql 또는 @Autowired 서비스로 직접 INSERT
// PaymentEvent: READY → IN_PROGRESS (executePayment 필요) → 컨슈머가 IN_PROGRESS에서 처리
// 주의: KafkaConfirmAdapter.confirm()이 executePayment()를 호출하므로 통합 테스트에서는
//       PaymentEvent를 READY 상태로 준비 후 KafkaConfirmAdapter를 통해 confirm() 직접 호출하거나
//       수동으로 IN_PROGRESS 상태로 세팅 필요
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KafkaConfirmListenerIntegrationTest 구현 + GREEN</name>
  <files>
    src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerIntegrationTest.java
  </files>
  <behavior>
    KafkaConfirmListenerIntegrationTest:
    - BaseKafkaIntegrationTest 상속
    - @Autowired KafkaTemplate<String, String> kafkaTemplate
    - @Autowired PaymentLoadUseCase paymentLoadUseCase (결과 검증용)
    - @Autowired PaymentConfirmService paymentConfirmService (KafkaConfirmAdapter 빈 — spring.payment.async-strategy=kafka로 활성화됨)
    - @Autowired 필요한 데이터 준비 서비스 (기존 통합 테스트에서 사용된 서비스 확인 필요)

    테스트 1 — 정상 흐름:
    - kafkaConfirmListener_WhenMessagePublished_PaymentEventBecomeDone():
      - DB에 테스트 PaymentEvent 생성 (READY 상태, 테스트 데이터)
      - paymentConfirmService.confirm(command) 호출 → KafkaConfirmAdapter가 kafka 발행 + 202 반환
      - Awaitility.await().atMost(30, TimeUnit.SECONDS).pollInterval(500, MILLISECONDS)
        .until(() -> paymentLoadUseCase.getPaymentEventByOrderId(orderId).getStatus() == PaymentEventStatus.DONE)

    테스트 2 — 중복 발행 멱등성:
    - kafkaConfirmListener_WhenDuplicateMessage_IdempotentProcessing():
      - DONE 상태의 PaymentEvent에 대해 kafkaTemplate.send("payment-confirm", orderId, orderId) 직접 발행
      - Awaitility로 일정 시간 대기 후 PaymentEvent 상태가 여전히 DONE임을 확인
      - (Toss 멱등키 위임: 동일 orderId 재처리 시 Toss가 동일 응답 반환, DONE 재기록은 멱등)

    Awaitility 사용: `org.awaitility:awaitility`는 spring-boot-starter-test에 포함됨 (별도 의존성 불필요)
    - `import static org.awaitility.Awaitility.await;`

    테스트 데이터 준비 방법:
    - 기존 OutboxWorker 통합 테스트(있다면) 또는 BaseIntegrationTest 패턴 참고
    - @Autowired로 사용 가능한 UseCase 직접 호출하여 DB 준비
    - @Transactional(Propagation.NOT_SUPPORTED) 또는 @DirtiesContext 없이 상태 독립성 유지

    주의사항:
    - KafkaConfirmListener.consume() 메서드에 @Transactional이 없음을 확인 (Plan 03 결과)
    - Toss API Mock 처리: 통합 테스트에서 Toss HTTP 호출은 WireMock 또는 MockBean으로 처리 필요
      → 기존 통합 테스트 패턴 확인 후 동일 방식 적용 (@MockBean TossOperator 또는 WireMock 서버)
    - PaymentEvent를 IN_PROGRESS 상태로 설정해야 컨슈머가 Toss API 호출 가능
      (KafkaConfirmAdapter.confirm() 내 executePayment()가 READY→IN_PROGRESS 전환)
  </behavior>
  <action>
    1. 기존 통합 테스트 파일에서 데이터 준비 패턴과 Toss mock 처리 방식을 확인:
       - `find src/test -name "*IntegrationTest*.java" -o -name "*Integration*Test.java"` 실행
       - 기존 통합 테스트의 @MockBean/@WireMock 패턴 확인

    2. KafkaConfirmListenerIntegrationTest.java 생성:
       ```java
       package com.hyoguoo.paymentplatform.payment.listener;

       import com.hyoguoo.paymentplatform.core.test.BaseKafkaIntegrationTest;
       import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentLoadUseCase;
       import com.hyoguoo.paymentplatform.payment.domain.enums.PaymentEventStatus;
       import com.hyoguoo.paymentplatform.payment.presentation.port.PaymentConfirmService;
       import java.util.concurrent.TimeUnit;
       import org.awaitility.Awaitility;
       import org.junit.jupiter.api.DisplayName;
       import org.junit.jupiter.api.Test;
       import org.springframework.beans.factory.annotation.Autowired;
       import org.springframework.kafka.core.KafkaTemplate;
       // ... (데이터 준비 의존성 import)

       @DisplayName("KafkaConfirmListener 통합 테스트")
       class KafkaConfirmListenerIntegrationTest extends BaseKafkaIntegrationTest {

           @Autowired
           private KafkaTemplate<String, String> kafkaTemplate;

           @Autowired
           private PaymentLoadUseCase paymentLoadUseCase;

           @Autowired
           private PaymentConfirmService paymentConfirmService; // KafkaConfirmAdapter

           // Toss API mock: 기존 통합 테스트 패턴에 맞게 적용

           @Test
           @DisplayName("Kafka 메시지 발행 후 컨슈머가 PaymentEvent를 DONE으로 전환한다")
           void kafkaConfirmListener_WhenMessagePublished_PaymentEventBecomeDone() {
               // given: DB에 PaymentEvent 준비 (테스트 데이터)
               // when: confirm() 또는 직접 kafkaTemplate.send()
               // then: Awaitility polling으로 DONE 상태 확인
               Awaitility.await()
                       .atMost(30, TimeUnit.SECONDS)
                       .pollInterval(500, TimeUnit.MILLISECONDS)
                       .until(() -> paymentLoadUseCase.getPaymentEventByOrderId(/* orderId */)
                               .getStatus() == PaymentEventStatus.DONE);
           }
       }
       ```

    3. 구체적인 테스트 데이터 준비와 Toss mock 구현은 기존 통합 테스트 패턴을 정확히 확인 후 동일하게 적용.
       기존 OutboxWorker, OutboxConfirmAdapter 통합 테스트가 있다면 그 패턴을 그대로 재사용.

    4. `./gradlew test --tests "*.KafkaConfirmListenerIntegrationTest"` 실행 — GREEN 확인
  </action>
  <verify>
    <automated>./gradlew test --tests "*.KafkaConfirmListenerIntegrationTest" --no-daemon 2>&1 | tail -15</automated>
  </verify>
  <done>KafkaConfirmListenerIntegrationTest GREEN. Testcontainers Kafka를 통해 메시지 발행 후 컨슈머가 PaymentEvent를 DONE으로 전환하는 E2E 흐름 자동화 검증 완료. `./gradlew test` 전체 GREEN.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "*.KafkaConfirmListenerIntegrationTest"` GREEN
- `./gradlew test` 전체 GREEN (회귀 없음)
- Testcontainers Kafka (confluentinc/cp-kafka:7.4.0) 브로커를 실제 사용
- 비동기 처리 완료를 Awaitility polling으로 검증
</verification>

<success_criteria>
- KafkaConfirmListenerIntegrationTest 존재 및 GREEN
- KAFKA-07(Testcontainers Kafka 통합 테스트) 요구사항 충족
- Phase 4 전체 요구사항 KAFKA-01~07 단위/통합 테스트로 검증됨
- `./gradlew test` 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-kafka-adapter/04-04-SUMMARY.md`
</output>
