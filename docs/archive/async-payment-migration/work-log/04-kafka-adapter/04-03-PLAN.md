---
phase: 04-kafka-adapter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListener.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerTest.java
autonomous: true
requirements:
  - KAFKA-04
  - KAFKA-05
  - KAFKA-06

must_haves:
  truths:
    - "KafkaConfirmListener.consume()가 orderId로 PaymentEvent를 조회하고 Toss API를 호출한다"
    - "@RetryableTopic(attempts='6')이 설정되어 최초 1회 + 5회 재시도(RETRYABLE_LIMIT=5와 동일)가 가능하다"
    - "PaymentTossNonRetryableException 발생 시 즉시 DLT로 라우팅된다 (exclude 설정)"
    - "handleDlt()가 호출되면 executePaymentFailureCompensation()이 실행된다"
    - "컨슈머 메서드에 @Transactional이 없다 (transactionCoordinator가 트랜잭션 경계 담당)"
    - "Toss 멱등키 위임 방식: 별도 existsByOrderId 가드 없음 — CONTEXT.md 결정"
    - "KafkaConfirmListenerTest 테스트가 GREEN이다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListener.java"
      provides: "Kafka 컨슈머 + DLT 핸들러"
      exports: ["KafkaConfirmListener"]
      contains: "@RetryableTopic"
  key_links:
    - from: "KafkaConfirmListener.consume"
      to: "PaymentTransactionCoordinator.executePaymentSuccessCompletion"
      via: "transactionCoordinator.executePaymentSuccessCompletion(orderId, paymentEvent, approvedAt)"
      pattern: "executePaymentSuccessCompletion"
    - from: "KafkaConfirmListener.handleDlt"
      to: "PaymentTransactionCoordinator.executePaymentFailureCompensation"
      via: "transactionCoordinator.executePaymentFailureCompensation(...)"
      pattern: "executePaymentFailureCompensation"
    - from: "@RetryableTopic"
      to: "payment-confirm-dlq"
      via: "dltTopicSuffix = \"-dlq\""
      pattern: "dltTopicSuffix.*-dlq"
---

<objective>
KafkaConfirmListener 구현: @RetryableTopic(attempts="6") + @DltHandler로 non-blocking 재시도와 DLT 라우팅을 선언적으로 구성한다. consume()은 orderId로 PaymentEvent를 조회해 Toss API 호출 후 상태 업데이트하고, handleDlt()는 보상 트랜잭션을 실행한다.

Purpose: KAFKA-04(컨슈머 처리), KAFKA-05(멱등성 — Toss 위임), KAFKA-06(DLT 라우팅) 요구사항 충족.
Output: KafkaConfirmListener.java, KafkaConfirmListenerTest GREEN
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kafka-adapter/04-CONTEXT.md
@.planning/phases/04-kafka-adapter/04-RESEARCH.md

<interfaces>
<!-- OutboxWorker.processRecord() — KafkaConfirmListener.consume() 로직 참고 -->
```java
// src/main/java/.../payment/scheduler/OutboxWorker.java (processRecord 발췌)
private void processRecord(PaymentOutbox outbox) {
    String orderId = outbox.getOrderId();
    PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(orderId);

    try {
        PaymentConfirmCommand command = PaymentConfirmCommand.builder()
                .userId(paymentEvent.getBuyerId())
                .orderId(orderId)
                .paymentKey(paymentEvent.getPaymentKey())
                .amount(paymentEvent.getTotalAmount())
                .build();
        PaymentGatewayInfo gatewayInfo = paymentCommandUseCase.confirmPaymentWithGateway(command);
        transactionCoordinator.executePaymentSuccessCompletion(
                orderId, paymentEvent, gatewayInfo.getPaymentDetails().getApprovedAt());
        // markDone은 Kafka 어댑터에서 불필요 — PaymentEvent 상태만 업데이트

    } catch (PaymentTossNonRetryableException e) {
        LogFmt.error(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
        transactionCoordinator.executePaymentFailureCompensation(
                orderId, paymentEvent, paymentEvent.getPaymentOrderList(), e.getMessage());

    } catch (PaymentTossRetryableException e) {
        // Kafka: @RetryableTopic이 재시도를 담당하므로 여기서 직접 카운트 관리 불필요
        // PaymentTossRetryableException을 @RetryableTopic include에 등록하여 retry 토픽으로 라우팅
        LogFmt.warn(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
        throw e; // re-throw → @RetryableTopic이 retry 토픽으로 라우팅
    }
}
```

<!-- PaymentCommandUseCase.confirmPaymentWithGateway 시그니처 -->
```java
public PaymentGatewayInfo confirmPaymentWithGateway(PaymentConfirmCommand command);
// PaymentGatewayInfo.getPaymentDetails().getApprovedAt() → LocalDateTime
```

<!-- @RetryableTopic 중요 설정값 -->
// attempts = "6" → 최초 1회 + 재시도 5회 = RETRYABLE_LIMIT=5 동일
// dltTopicSuffix = "-dlq" → payment-confirm-requests-dlq 생성
// KAFKA-06 요구사항: payment-confirm-dlq
// 해결책: @KafkaListener topics = "payment-confirm" + dltTopicSuffix = "-dlq"
//         → DLT = "payment-confirm-dlq" (KAFKA-06 충족)
// 따라서 프로듀서 토픽명도 "payment-confirm"으로 변경 필요
// → KafkaConfirmAdapter.TOPIC = "payment-confirm" (Plan 02에서 "payment-confirm-requests" 사용 시 조율 필요)
//
// 대안: 요구사항 명세를 "payment-confirm-requests-dlq"로 수용하고 TOPIC = "payment-confirm-requests" 유지
// RESEARCH.md 권장: 토픽명을 "payment-confirm"으로 변경하여 DLT = "payment-confirm-dlq" 달성
// → 이 플랜에서는 KAFKA-06 명세("payment-confirm-dlq")를 충족하기 위해 TOPIC = "payment-confirm" 사용
// → KafkaConfirmAdapter.TOPIC도 "payment-confirm"으로 수정

<!-- LogFmt 패턴 -->
```java
LogFmt.error(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
LogFmt.warn(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KafkaConfirmListenerTest 완성 (RED) + KafkaConfirmListener 구현 (GREEN)</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListener.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/listener/KafkaConfirmListenerTest.java
  </files>
  <behavior>
    KafkaConfirmListenerTest (Plan 01 스텁 완성 — GREEN):
    - consume_Success_CallsExecutePaymentSuccessCompletion_Once(): 정상 흐름 — confirmPaymentWithGateway → executePaymentSuccessCompletion 1회 호출
    - consume_Success_DoesNotCallFailureCompensation(): 정상 흐름 — executePaymentFailureCompensation never() 호출
    - consume_WhenNonRetryable_CallsFailureCompensation(): PaymentTossNonRetryableException 발생 시 executePaymentFailureCompensation 1회 호출
    - consume_WhenRetryable_ThrowsException(): PaymentTossRetryableException 발생 시 예외가 re-throw됨 (@RetryableTopic이 처리)
    - handleDlt_CallsExecutePaymentFailureCompensation_Once(): DLT 도달 시 executePaymentFailureCompensation 1회 호출
    - retryableTopic_HasAttempts6(): @RetryableTopic annotation의 attempts="6" 검증
    - retryableTopic_HasDltSuffix_Dlq(): dltTopicSuffix="-dlq" 검증
    - kafkaListener_TopicIsPaymentConfirm(): @KafkaListener topics="payment-confirm" 검증

    KafkaConfirmListener 구현:
    - 패키지: com.hyoguoo.paymentplatform.payment.listener (PaymentHistoryEventListener와 동일)
    - @RetryableTopic(attempts="6", backoff=@Backoff(delay=1000, multiplier=2.0, maxDelay=30000), dltTopicSuffix="-dlq", include={PaymentTossRetryableException.class}, exclude={PaymentTossNonRetryableException.class}, autoCreateTopics="true")
    - @KafkaListener(topics="payment-confirm", groupId="${spring.kafka.consumer.group-id:payment-confirm-group}")
    - consume(String orderId): getPaymentEventByOrderId → confirmPaymentWithGateway → executePaymentSuccessCompletion / 예외 분기
    - PaymentTossNonRetryableException: catch 후 LogFmt.error + executePaymentFailureCompensation 호출 (DLT 라우팅 불필요, 바로 보상)
    - PaymentTossRetryableException: catch 후 LogFmt.warn + throw (re-throw → @RetryableTopic retry 토픽 라우팅)
    - @DltHandler handleDlt(String orderId): LogFmt.error + executePaymentFailureCompensation 호출
    - consume() 메서드에 @Transactional 없음 (Anti-pattern 회피)

    KafkaConfirmAdapter.TOPIC 수정:
    - "payment-confirm-requests" → "payment-confirm" (DLT 토픽명 KAFKA-06 충족)
    - KafkaConfirmAdapter.java의 TOPIC 상수를 "payment-confirm"으로 변경
    - KafkaConfirmAdapterTest의 send() 검증에서 토픽명도 "payment-confirm"으로 업데이트
  </behavior>
  <action>
    KafkaConfirmListener.java 신규 생성:
    ```java
    package com.hyoguoo.paymentplatform.payment.listener;

    import com.hyoguoo.paymentplatform.core.common.log.EventType;
    import com.hyoguoo.paymentplatform.core.common.log.LogDomain;
    import com.hyoguoo.paymentplatform.core.common.log.LogFmt;
    import com.hyoguoo.paymentplatform.payment.application.dto.request.PaymentConfirmCommand;
    import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentCommandUseCase;
    import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentLoadUseCase;
    import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentTransactionCoordinator;
    import com.hyoguoo.paymentplatform.payment.domain.PaymentEvent;
    import com.hyoguoo.paymentplatform.payment.domain.dto.PaymentGatewayInfo;
    import com.hyoguoo.paymentplatform.payment.exception.PaymentTossNonRetryableException;
    import com.hyoguoo.paymentplatform.payment.exception.PaymentTossRetryableException;
    import lombok.RequiredArgsConstructor;
    import lombok.extern.slf4j.Slf4j;
    import org.springframework.kafka.annotation.DltHandler;
    import org.springframework.kafka.annotation.KafkaListener;
    import org.springframework.kafka.annotation.RetryableTopic;
    import org.springframework.retry.annotation.Backoff;
    import org.springframework.stereotype.Component;

    @Slf4j
    @Component
    @RequiredArgsConstructor
    public class KafkaConfirmListener {

        private final PaymentLoadUseCase paymentLoadUseCase;
        private final PaymentCommandUseCase paymentCommandUseCase;
        private final PaymentTransactionCoordinator transactionCoordinator;

        @RetryableTopic(
                attempts = "6",
                backoff = @Backoff(delay = 1000, multiplier = 2.0, maxDelay = 30000),
                dltTopicSuffix = "-dlq",
                include = {PaymentTossRetryableException.class},
                exclude = {PaymentTossNonRetryableException.class},
                autoCreateTopics = "true"
        )
        @KafkaListener(
                topics = "payment-confirm",
                groupId = "${spring.kafka.consumer.group-id:payment-confirm-group}"
        )
        public void consume(String orderId) {
            PaymentEvent paymentEvent =
                    paymentLoadUseCase.getPaymentEventByOrderId(orderId);

            PaymentConfirmCommand command = PaymentConfirmCommand.builder()
                    .userId(paymentEvent.getBuyerId())
                    .orderId(orderId)
                    .paymentKey(paymentEvent.getPaymentKey())
                    .amount(paymentEvent.getTotalAmount())
                    .build();

            try {
                PaymentGatewayInfo gatewayInfo =
                        paymentCommandUseCase.confirmPaymentWithGateway(command);

                transactionCoordinator.executePaymentSuccessCompletion(
                        orderId, paymentEvent,
                        gatewayInfo.getPaymentDetails().getApprovedAt()
                );

            } catch (PaymentTossNonRetryableException e) {
                LogFmt.error(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
                transactionCoordinator.executePaymentFailureCompensation(
                        orderId, paymentEvent, paymentEvent.getPaymentOrderList(), e.getMessage()
                );

            } catch (PaymentTossRetryableException e) {
                LogFmt.warn(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
                throw e;
            }
        }

        @DltHandler
        public void handleDlt(String orderId) {
            LogFmt.error(log, LogDomain.PAYMENT, EventType.EXCEPTION,
                    () -> "DLT reached for orderId=" + orderId);
            PaymentEvent paymentEvent =
                    paymentLoadUseCase.getPaymentEventByOrderId(orderId);
            transactionCoordinator.executePaymentFailureCompensation(
                    orderId, paymentEvent, paymentEvent.getPaymentOrderList(),
                    "kafka-dlt-exhausted"
            );
        }
    }
    ```

    KafkaConfirmListener 구현 후 KafkaConfirmListenerTest.java (Plan 01 스텁) 완성:
    - 각 테스트 메서드에서 given(...).willReturn(...) 세팅
    - PaymentGatewayInfo mock: mockPaymentGatewayInfo.getPaymentDetails().getApprovedAt() 체인 mock 필요
    - NonRetryable 케이스: given(mockPaymentCommandUseCase.confirmPaymentWithGateway(any())).willThrow(new PaymentTossNonRetryableException(...))
    - Retryable 케이스: willThrow(new PaymentTossRetryableException(...)) → assertThatThrownBy(...).isInstanceOf(PaymentTossRetryableException.class)
    - Annotation 검증: KafkaConfirmListener.class.getMethod("consume", String.class).getAnnotation(RetryableTopic.class)

    KafkaConfirmAdapter.java의 TOPIC 상수를 "payment-confirm"으로 수정:
    ```java
    private static final String TOPIC = "payment-confirm";
    ```
    KafkaConfirmAdapterTest.java의 send() 검증 토픽명도 "payment-confirm"으로 업데이트.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.KafkaConfirmListenerTest" --tests "*.KafkaConfirmAdapterTest" --no-daemon 2>&1 | tail -15</automated>
  </verify>
  <done>KafkaConfirmListenerTest + KafkaConfirmAdapterTest 모든 테스트 GREEN. @RetryableTopic attempts="6", dltTopicSuffix="-dlq", topics="payment-confirm" 검증됨. DLT 핸들러 보상 트랜잭션 호출 검증됨.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "*.KafkaConfirmListenerTest" --tests "*.KafkaConfirmAdapterTest"` 전부 GREEN
- KafkaConfirmListener에 @Transactional 없음
- @RetryableTopic attempts="6" (1+5회), dltTopicSuffix="-dlq" 설정됨
- DLT 토픽: "payment-confirm-dlq" (KAFKA-06 충족)
- Retryable 예외: re-throw로 @RetryableTopic에게 위임
- NonRetryable 예외: catch → 보상 트랜잭션 즉시 실행
</verification>

<success_criteria>
- KafkaConfirmListener.java 존재 및 @RetryableTopic + @DltHandler 선언
- KafkaConfirmListenerTest 8개 이상 테스트 GREEN
- KAFKA-04(컨슈머 처리), KAFKA-05(Toss 멱등키 위임), KAFKA-06(DLT 라우팅) 단위 테스트로 검증됨
- `./gradlew test` 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-kafka-adapter/04-03-SUMMARY.md`
</output>
