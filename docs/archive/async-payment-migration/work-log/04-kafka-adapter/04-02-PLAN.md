---
phase: 04-kafka-adapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapter.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinatorTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java
autonomous: true
requirements:
  - KAFKA-02
  - KAFKA-03

must_haves:
  truths:
    - "PaymentTransactionCoordinator에 executeStockDecreaseOnly() 메서드가 존재하며 Outbox 생성 없이 재고 감소만 수행한다"
    - "KafkaConfirmAdapter.confirm() 호출 시 executePayment(), executeStockDecreaseOnly(), kafkaTemplate.send() 순서로 실행된다"
    - "KafkaConfirmAdapter.confirm()은 ResponseType.ASYNC_202를 반환한다"
    - "KafkaConfirmAdapter는 @ConditionalOnProperty(havingValue='kafka', matchIfMissing=false)를 가진다"
    - "Kafka 발행은 트랜잭션 커밋 이후 (executeStockDecreaseOnly() 리턴 후) 호출된다"
    - "KafkaConfirmAdapterTest 테스트가 GREEN이다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java"
      provides: "executeStockDecreaseOnly() 추가"
      contains: "executeStockDecreaseOnly"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapter.java"
      provides: "PaymentConfirmService Kafka 구현체"
      exports: ["KafkaConfirmAdapter"]
  key_links:
    - from: "KafkaConfirmAdapter"
      to: "PaymentTransactionCoordinator.executeStockDecreaseOnly"
      via: "transactionCoordinator.executeStockDecreaseOnly(orderId, paymentOrderList)"
      pattern: "executeStockDecreaseOnly"
    - from: "KafkaConfirmAdapter"
      to: "kafkaTemplate"
      via: "kafkaTemplate.send(TOPIC, orderId, orderId)"
      pattern: "kafkaTemplate\\.send"
---

<objective>
KafkaConfirmAdapter 구현: PaymentTransactionCoordinator에 executeStockDecreaseOnly() 메서드를 추가하고, KafkaConfirmAdapter가 재고 감소 → Kafka 발행 → 202 반환 흐름을 구현한다. CONTEXT.md 결정에 따라 Kafka 발행은 DB 트랜잭션 커밋 이후 호출된다.

Purpose: KAFKA-02(어댑터 발행 + 202), KAFKA-03(재고 감소 동기 완료) 요구사항 충족.
Output: KafkaConfirmAdapter.java, PaymentTransactionCoordinator executeStockDecreaseOnly() 메서드 추가, KafkaConfirmAdapterTest GREEN
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kafka-adapter/04-CONTEXT.md
@.planning/phases/04-kafka-adapter/04-RESEARCH.md

<interfaces>
<!-- OutboxConfirmAdapter — KafkaConfirmAdapter 구조 참고 -->
```java
// src/main/java/.../payment/infrastructure/adapter/OutboxConfirmAdapter.java
@Slf4j
@Service
@RequiredArgsConstructor
@ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "outbox")
public class OutboxConfirmAdapter implements PaymentConfirmService {
    private final PaymentTransactionCoordinator transactionCoordinator;
    private final PaymentLoadUseCase paymentLoadUseCase;
    private final PaymentCommandUseCase paymentCommandUseCase;

    @Override
    public PaymentConfirmAsyncResult confirm(PaymentConfirmCommand command)
            throws PaymentOrderedProductStockException {
        PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(command.getOrderId());
        paymentCommandUseCase.executePayment(paymentEvent, command.getPaymentKey());
        transactionCoordinator.executeStockDecreaseWithOutboxCreation(
                command.getOrderId(), paymentEvent.getPaymentOrderList());
        return PaymentConfirmAsyncResult.builder()
                .responseType(ResponseType.ASYNC_202)
                .orderId(command.getOrderId())
                .amount(command.getAmount())
                .build();
    }
}
```

<!-- PaymentTransactionCoordinator 기존 메서드 -->
```java
// @Transactional(rollbackFor = PaymentOrderedProductStockException.class)
// public PaymentOutbox executeStockDecreaseWithOutboxCreation(String orderId, List<PaymentOrder> paymentOrderList)
// → Outbox 생성 포함. Kafka 어댑터는 이 메서드 대신 신규 executeStockDecreaseOnly() 사용

// 추가할 메서드 시그니처:
@Transactional(rollbackFor = PaymentOrderedProductStockException.class)
public void executeStockDecreaseOnly(String orderId, List<PaymentOrder> paymentOrderList)
        throws PaymentOrderedProductStockException {
    orderedProductUseCase.decreaseStockForOrders(paymentOrderList);
    // Kafka 어댑터: Outbox 레코드 생성 없이 재고 감소만 수행
}
```

<!-- PaymentConfirmAsyncResult -->
```java
// ResponseType.ASYNC_202 — 컨트롤러가 202 반환에 사용
public enum ResponseType { SYNC_200, ASYNC_202 }
```

<!-- KafkaTemplate 사용 패턴 -->
```java
// kafkaTemplate.send(topic, key, value)
// topic = "payment-confirm-requests"
// key = orderId (파티션 친화성)
// value = orderId (페이로드 최소화 — CONTEXT.md 결정)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentTransactionCoordinator.executeStockDecreaseOnly() 추가 + 테스트 GREEN</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinatorTest.java
  </files>
  <behavior>
    - PaymentTransactionCoordinatorTest에 테스트 먼저 작성 (RED):
      - executeStockDecreaseOnly_CallsDecreaseStockForOrders_Once(): decreaseStockForOrders가 1회 호출됨을 검증
      - executeStockDecreaseOnly_DoesNotCallCreatePendingRecord(): paymentOutboxUseCase.createPendingRecord가 호출되지 않음을 검증 (Outbox 생성 없음)
    - PaymentTransactionCoordinator에 executeStockDecreaseOnly() 구현 (GREEN):
      - @Transactional(rollbackFor = PaymentOrderedProductStockException.class)
      - orderedProductUseCase.decreaseStockForOrders(paymentOrderList) 만 호출
      - paymentOutboxUseCase.createPendingRecord() 호출 없음
    - 기존 테스트가 계속 GREEN인지 확인
  </behavior>
  <action>
    PaymentTransactionCoordinatorTest에 executeStockDecreaseOnly 관련 테스트 추가:
    - `@Nested @DisplayName("executeStockDecreaseOnly() 메서드 테스트") class ExecuteStockDecreaseOnlyTest`
    - 성공 케이스: orderedProductUseCase.decreaseStockForOrders가 1회 호출됨
    - 격리 케이스: paymentOutboxUseCase.createPendingRecord가 never() 호출됨 (Outbox 생성 없음)

    PaymentTransactionCoordinator에 메서드 추가:
    ```java
    @Transactional(rollbackFor = PaymentOrderedProductStockException.class)
    public void executeStockDecreaseOnly(
            String orderId,
            List<PaymentOrder> paymentOrderList
    ) throws PaymentOrderedProductStockException {
        orderedProductUseCase.decreaseStockForOrders(paymentOrderList);
        // Kafka 어댑터 전용: Outbox 레코드 생성 없이 재고 감소만 수행
    }
    ```

    주의: orderId 파라미터는 서명에 포함하되 현재 메서드에서 직접 사용하지 않음 (확장성 유지 목적). MINIMAL CHANGE 원칙 — PaymentTransactionCoordinator의 다른 메서드는 수정하지 않는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentTransactionCoordinatorTest" --no-daemon 2>&1 | tail -10</automated>
  </verify>
  <done>PaymentTransactionCoordinatorTest GREEN. executeStockDecreaseOnly() 메서드 존재하며 decreaseStockForOrders만 호출하고 Outbox 생성 없음 검증 완료.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KafkaConfirmAdapter 구현 + KafkaConfirmAdapterTest GREEN</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapter.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java
  </files>
  <behavior>
    KafkaConfirmAdapterTest (Plan 01 스텁 완성 — GREEN):
    - confirm_CallsExecutePayment_Once(): executePayment가 1회 호출됨
    - confirm_CallsExecuteStockDecreaseOnly_Once(): executeStockDecreaseOnly가 1회 호출됨 (executeStockDecreaseWithOutboxCreation 아님)
    - confirm_Returns_Async202(): responseType=ASYNC_202, orderId/amount가 command와 동일
    - kafkaConfirmAdapter_HasConditionalOnPropertyAnnotation(): @ConditionalOnProperty name/havingValue="kafka"/matchIfMissing=false 검증
    - kafkaTemplate.send() 호출 검증: then(mockKafkaTemplate).should(times(1)).send("payment-confirm-requests", orderId, orderId)

    KafkaConfirmAdapter 구현:
    - @ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "kafka")
    - KafkaTemplate<String, String> kafkaTemplate final 필드 (생성자 주입)
    - confirm() 순서: getPaymentEventByOrderId → executePayment → executeStockDecreaseOnly → kafkaTemplate.send → return ASYNC_202
    - TOPIC 상수: private static final String TOPIC = "payment-confirm-requests"
    - Anti-pattern 주의: kafkaTemplate.send()는 executeStockDecreaseOnly() 외부 (트랜잭션 커밋 이후) 에서 호출 — 메서드 순서가 트랜잭션 격리를 보장
    - KafkaException 발생 시 예외 전파 (별도 try-catch 불필요 — Spring Boot 전역 예외 핸들러 처리)
  </behavior>
  <action>
    KafkaConfirmAdapterTest.java (Plan 01에서 생성된 스텁) 완성:
    - KafkaTemplate import 추가: `import org.springframework.kafka.core.KafkaTemplate;`
    - `@SuppressWarnings("unchecked")` + `mockKafkaTemplate = (KafkaTemplate<String, String>) Mockito.mock(KafkaTemplate.class)`
    - setUp()에서 KafkaConfirmAdapter 생성자에 mockKafkaTemplate 추가
    - then(mockKafkaTemplate).should(times(1)).send(eq("payment-confirm-requests"), eq(orderId), eq(orderId)) 검증

    KafkaConfirmAdapter.java 신규 생성:
    ```java
    package com.hyoguoo.paymentplatform.payment.infrastructure.adapter;

    import com.hyoguoo.paymentplatform.payment.application.dto.request.PaymentConfirmCommand;
    import com.hyoguoo.paymentplatform.payment.application.dto.response.PaymentConfirmAsyncResult;
    import com.hyoguoo.paymentplatform.payment.application.dto.response.PaymentConfirmAsyncResult.ResponseType;
    import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentCommandUseCase;
    import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentLoadUseCase;
    import com.hyoguoo.paymentplatform.payment.application.usecase.PaymentTransactionCoordinator;
    import com.hyoguoo.paymentplatform.payment.domain.PaymentEvent;
    import com.hyoguoo.paymentplatform.payment.exception.PaymentOrderedProductStockException;
    import com.hyoguoo.paymentplatform.payment.presentation.port.PaymentConfirmService;
    import lombok.RequiredArgsConstructor;
    import lombok.extern.slf4j.Slf4j;
    import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
    import org.springframework.kafka.core.KafkaTemplate;
    import org.springframework.stereotype.Service;

    @Slf4j
    @Service
    @RequiredArgsConstructor
    @ConditionalOnProperty(
            name = "spring.payment.async-strategy",
            havingValue = "kafka"
    )
    public class KafkaConfirmAdapter implements PaymentConfirmService {

        private static final String TOPIC = "payment-confirm-requests";

        private final PaymentTransactionCoordinator transactionCoordinator;
        private final PaymentLoadUseCase paymentLoadUseCase;
        private final PaymentCommandUseCase paymentCommandUseCase;
        private final KafkaTemplate<String, String> kafkaTemplate;

        @Override
        public PaymentConfirmAsyncResult confirm(PaymentConfirmCommand command)
                throws PaymentOrderedProductStockException {
            PaymentEvent paymentEvent =
                    paymentLoadUseCase.getPaymentEventByOrderId(command.getOrderId());

            // PaymentEvent READY → IN_PROGRESS 전환 + paymentKey 기록
            // 컨슈머가 나중에 paymentEvent.getPaymentKey()를 조회하기 위해 반드시 선행 호출
            paymentCommandUseCase.executePayment(paymentEvent, command.getPaymentKey());

            // 재고 감소 (트랜잭션 내, Outbox 레코드 생성 없음)
            transactionCoordinator.executeStockDecreaseOnly(
                    command.getOrderId(), paymentEvent.getPaymentOrderList()
            );

            // Kafka 발행 — 트랜잭션 커밋 이후 호출로 소비자 타이밍 레이스 방지
            // 실패 시 KafkaException 전파 → 상위 컨트롤러의 예외 핸들러 처리
            kafkaTemplate.send(TOPIC, command.getOrderId(), command.getOrderId());

            return PaymentConfirmAsyncResult.builder()
                    .responseType(ResponseType.ASYNC_202)
                    .orderId(command.getOrderId())
                    .amount(command.getAmount())
                    .build();
        }
    }
    ```
  </action>
  <verify>
    <automated>./gradlew test --tests "*.KafkaConfirmAdapterTest" --no-daemon 2>&1 | tail -10</automated>
  </verify>
  <done>KafkaConfirmAdapterTest 모든 테스트 GREEN. KafkaConfirmAdapter.java 존재, @ConditionalOnProperty(havingValue="kafka") 선언됨. confirm() 메서드가 executePayment → executeStockDecreaseOnly → kafkaTemplate.send 순서로 실행됨.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "*.PaymentTransactionCoordinatorTest" --tests "*.KafkaConfirmAdapterTest"` 전부 GREEN
- KafkaConfirmAdapter.java에 `@ConditionalOnProperty(havingValue = "kafka")` 선언됨
- executeStockDecreaseOnly()는 orderedProductUseCase.decreaseStockForOrders만 호출하며 Outbox 생성 없음
- kafkaTemplate.send() 호출이 executeStockDecreaseOnly() 리턴 이후 위치함
</verification>

<success_criteria>
- PaymentTransactionCoordinator.executeStockDecreaseOnly() 추가 및 테스트 GREEN
- KafkaConfirmAdapter 구현 완료 및 KafkaConfirmAdapterTest 5개 이상 테스트 GREEN
- KAFKA-02 (발행 + 202 반환), KAFKA-03 (재고 감소 동기 완료) 단위 테스트로 검증됨
- `./gradlew test` 기존 테스트 포함 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-kafka-adapter/04-02-SUMMARY.md`
</output>
