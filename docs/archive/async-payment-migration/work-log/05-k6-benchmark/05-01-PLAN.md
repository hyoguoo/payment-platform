---
phase: 05-k6-benchmark
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/k6/helpers.js
  - scripts/k6/sync.js
  - scripts/k6/outbox.js
  - scripts/k6/kafka.js
  - scripts/k6/run-benchmark.sh
  - scripts/k6/README.md
autonomous: true
requirements:
  - BENCH-01
  - BENCH-02
  - BENCH-04

must_haves:
  truths:
    - "sync.js / outbox.js / kafka.js 세 파일이 scripts/k6/ 디렉토리에 존재한다"
    - "outbox.js와 kafka.js는 202 응답 후 DONE/FAILED까지 폴링 루프를 포함한다"
    - "세 스크립트 모두 동일한 stages(50→100→200 VU, 60s씩)를 사용한다"
    - "run-benchmark.sh가 전략 전환 안내 후 세 스크립트를 순서대로 실행한다"
    - "helpers.js에서 setup()이 1,000개 orderId를 사전 생성하고 SharedArray로 공유한다"
  artifacts:
    - path: "scripts/k6/helpers.js"
      provides: "setup(), pollStatus(), BASE_URL, POLL_INTERVAL, ORDER_POOL_SIZE, getOrderIndex()"
    - path: "scripts/k6/sync.js"
      provides: "Sync 전략 k6 스크립트 (200 OK 기대)"
    - path: "scripts/k6/outbox.js"
      provides: "Outbox 전략 k6 스크립트 (202 → 폴링)"
    - path: "scripts/k6/kafka.js"
      provides: "Kafka 전략 k6 스크립트 (202 → 폴링)"
    - path: "scripts/k6/run-benchmark.sh"
      provides: "세 전략 순서 실행 자동화 스크립트"
    - path: "scripts/k6/README.md"
      provides: "Docker 실행법, 전략 전환법, 결과 해석법"
  key_links:
    - from: "scripts/k6/outbox.js"
      to: "helpers.js#pollStatus()"
      via: "import { pollStatus } from './helpers.js'"
      pattern: "pollStatus"
    - from: "scripts/k6/run-benchmark.sh"
      to: "scripts/common.sh"
      via: "source ../common.sh"
      pattern: "source.*common.sh"
---

<objective>
k6 부하 테스트 스크립트 3종(sync.js / outbox.js / kafka.js)과 공통 헬퍼, 자동화 실행 스크립트, README를 작성한다.

Purpose: 세 어댑터 전략을 동일 부하 조건(50→100→200 VU, 60s씩)에서 공정하게 비교할 수 있는 측정 인프라를 구축한다.
Output: scripts/k6/ 디렉토리 — helpers.js, sync.js, outbox.js, kafka.js, run-benchmark.sh, README.md
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-k6-benchmark/05-CONTEXT.md
@.planning/phases/05-k6-benchmark/05-RESEARCH.md
@scripts/common.sh

<interfaces>
<!-- k6 스크립트가 호출하는 API 엔드포인트 (05-RESEARCH.md 확인됨) -->

POST /api/v1/payments/checkout
  Body: { userId: Long, orderedProductList: [{ productId: Long, quantity: Int }] }
  Response 200: { orderId: String, totalAmount: BigDecimal }

POST /api/v1/payments/confirm
  Body: { userId: Long, orderId: String, amount: BigDecimal, paymentKey: String }
  Response 200 (sync): { orderId, amount }
  Response 202 (async): { orderId, amount }

GET /api/v1/payments/{orderId}/status
  Response 200: { orderId: String, status: "PENDING"|"PROCESSING"|"DONE"|"FAILED", approvedAt: LocalDateTime|null }

<!-- 테스트 데이터 (data.sql 확인됨) -->
VALID_USER_ID = 1
VALID_PRODUCT_ID = 1  (price: 50000)
FAKE_PAYMENT_KEY = 임의 문자열 허용 (Fake 환경)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: helpers.js — 공통 상수, setup(), pollStatus(), 인덱스 할당</name>
  <files>scripts/k6/helpers.js</files>
  <action>
scripts/k6/ 디렉토리를 생성하고 helpers.js를 작성한다. k6의 ES6 모듈 문법(import/export)을 사용한다. Node.js require()는 사용 불가.

helpers.js 내용:
1. **공통 상수**
   ```javascript
   export const BASE_URL = __ENV.BASE_URL || 'http://host.docker.internal:8080';
   export const ORDER_POOL_SIZE = 1000;
   export const POLL_INTERVAL_S = 0.5;   // sleep() 는 초 단위 — 500ms
   export const POLL_TIMEOUT_MS = 30000;  // 30초
   export const FAKE_PAYMENT_KEY = 'benchmark-payment-key';
   ```

2. **공통 stages** (세 스크립트에서 동일하게 import)
   ```javascript
   export const BENCHMARK_STAGES = [
     { duration: '60s', target: 50 },
     { duration: '60s', target: 100 },
     { duration: '60s', target: 200 },
   ];
   ```

3. **setup()** — 측정 트래픽과 분리된 사전 데이터 준비
   ```javascript
   import http from 'k6/http';
   import { SharedArray } from 'k6/data';

   export function setup() {
     const orderIds = [];
     for (let i = 0; i < ORDER_POOL_SIZE; i++) {
       const res = http.post(
         `${BASE_URL}/api/v1/payments/checkout`,
         JSON.stringify({ userId: 1, orderedProductList: [{ productId: 1, quantity: 1 }] }),
         { headers: { 'Content-Type': 'application/json' } }
       );
       if (res.status === 200) {
         orderIds.push(JSON.parse(res.body).orderId);
       }
     }
     return { orderIds };
   }
   ```

4. **getOrderIndex()** — VU별 중복 없는 orderId 인덱스 계산
   ```javascript
   export function getOrderIndex(poolSize) {
     // __VU 1-based, __ITER 0-based
     // 단순 패턴: (VU번호 * 소수 + ITER) % 풀크기 로 충돌 최소화
     return ((__VU - 1) * 97 + __ITER) % poolSize;
   }
   ```

5. **pollStatus()** — 비동기 전략의 end-to-end 완료 대기
   ```javascript
   import { sleep } from 'k6';

   export function pollStatus(orderId) {
     const startTime = Date.now();
     while (Date.now() - startTime < POLL_TIMEOUT_MS) {
       sleep(POLL_INTERVAL_S);
       const res = http.get(`${BASE_URL}/api/v1/payments/${orderId}/status`);
       if (res.status === 200) {
         const body = JSON.parse(res.body);
         if (body.status === 'DONE' || body.status === 'FAILED') {
           return body.status;
         }
       }
     }
     return 'TIMEOUT';
   }
   ```

파일 상단에 모든 import를 모아서 작성한다. export const와 export function만 사용.
  </action>
  <verify>
    <automated>ls /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/scripts/k6/helpers.js</automated>
  </verify>
  <done>scripts/k6/helpers.js 파일 존재. setup(), pollStatus(), BENCHMARK_STAGES, getOrderIndex() 모두 export됨.</done>
</task>

<task type="auto">
  <name>Task 2: sync.js / outbox.js / kafka.js — 세 전략 측정 스크립트</name>
  <files>scripts/k6/sync.js, scripts/k6/outbox.js, scripts/k6/kafka.js</files>
  <action>
세 스크립트를 작성한다. 모두 동일한 BENCHMARK_STAGES를 import해 비교 공정성을 확보한다.

**sync.js** — Sync 전략 (200 OK 기대):
```javascript
import http from 'k6/http';
import { check } from 'k6';
import { setup, BASE_URL, BENCHMARK_STAGES, FAKE_PAYMENT_KEY, getOrderIndex, ORDER_POOL_SIZE } from './helpers.js';

export { setup };

export const options = {
  stages: BENCHMARK_STAGES,
};

export default function(data) {
  const idx = getOrderIndex(data.orderIds.length);
  const orderId = data.orderIds[idx];

  const res = http.post(
    `${BASE_URL}/api/v1/payments/confirm`,
    JSON.stringify({
      userId: 1,
      orderId: orderId,
      amount: 50000,
      paymentKey: FAKE_PAYMENT_KEY,
    }),
    { headers: { 'Content-Type': 'application/json' } }
  );

  check(res, {
    'status is 200': (r) => r.status === 200,
  });
}
```

**outbox.js** — Outbox 전략 (202 → 폴링 → DONE):
```javascript
import http from 'k6/http';
import { check } from 'k6';
import { Trend } from 'k6/metrics';
import { setup, BASE_URL, BENCHMARK_STAGES, FAKE_PAYMENT_KEY, getOrderIndex, pollStatus } from './helpers.js';

export { setup };

const e2eLatency = new Trend('e2e_latency_ms', true);

export const options = {
  stages: BENCHMARK_STAGES,
};

export default function(data) {
  const start = Date.now();
  const idx = getOrderIndex(data.orderIds.length);
  const orderId = data.orderIds[idx];

  const confirmRes = http.post(
    `${BASE_URL}/api/v1/payments/confirm`,
    JSON.stringify({
      userId: 1,
      orderId: orderId,
      amount: 50000,
      paymentKey: FAKE_PAYMENT_KEY,
    }),
    { headers: { 'Content-Type': 'application/json' } }
  );

  check(confirmRes, { 'confirm accepted (202)': (r) => r.status === 202 });

  if (confirmRes.status === 202) {
    const finalStatus = pollStatus(orderId);
    e2eLatency.add(Date.now() - start);
    check(finalStatus, { 'completed (DONE)': (s) => s === 'DONE' });
  }
}
```

**kafka.js** — Kafka 전략 (202 → 폴링 → DONE): outbox.js와 동일한 구조. 파일명과 주석만 kafka로 변경. e2eLatency Trend 변수명도 동일하게 사용(스크립트별 독립 파일이므로 충돌 없음).

세 스크립트 모두:
- `export { setup }` 으로 helpers.js의 setup() 재사용
- `export const options = { stages: BENCHMARK_STAGES }` 로 동일 부하 조건 보장
- Thresholds 없음 (순수 측정)
  </action>
  <verify>
    <automated>ls /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/scripts/k6/sync.js /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/scripts/k6/outbox.js /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/scripts/k6/kafka.js</automated>
  </verify>
  <done>세 스크립트 파일 존재. sync.js는 check(200), outbox.js/kafka.js는 pollStatus() 호출 후 check(DONE)을 포함함.</done>
</task>

<task type="auto">
  <name>Task 3: run-benchmark.sh + README.md</name>
  <files>scripts/k6/run-benchmark.sh, scripts/k6/README.md</files>
  <action>
**run-benchmark.sh** — 세 전략 순서 실행 자동화:
기존 scripts/common.sh 컨벤션을 그대로 따른다(print_section, print_warning, print_info 함수 사용).

```bash
#!/bin/bash
set -e
source "$(dirname "$0")/../common.sh"

SCRIPT_DIR="$(dirname "$(realpath "$0")")"
BASE_URL="${BASE_URL:-http://host.docker.internal:8080}"

print_section "=== Phase 5: k6 Benchmark ==="
print_warning "전제 조건:"
echo "  1. Docker가 실행 중이어야 합니다"
echo "  2. 서버가 benchmark 프로파일로 실행 중이어야 합니다"
echo "     ./gradlew bootRun --args='--spring.profiles.active=benchmark'"
echo "  3. MySQL이 실행 중이어야 합니다 (docker/compose)"
echo ""

run_strategy() {
  local strategy=$1
  local script=$2
  echo ""
  print_section "--- $strategy 전략 측정 ---"
  print_warning "application.yml 에서 spring.payment.async-strategy=$strategy 로 변경 후 서버를 재기동하세요."
  if [ "$strategy" == "kafka" ]; then
    print_warning "Kafka 전략은 docker/compose/docker-compose.yml 의 Kafka 서비스도 실행 중이어야 합니다."
  fi
  read -p "준비됐으면 Enter 키를 누르세요 (Ctrl+C로 중단)..."

  docker run --rm \
    -v "${SCRIPT_DIR}:/scripts" \
    -e BASE_URL="${BASE_URL}" \
    grafana/k6 run "/scripts/${script}"

  print_info "$strategy 전략 측정 완료."
}

run_strategy "sync" "sync.js"
run_strategy "outbox" "outbox.js"
run_strategy "kafka" "kafka.js"

echo ""
print_info "모든 전략 측정 완료."
print_warning "k6 summary 수치를 BENCHMARK.md 에 기록하세요."
```

파일에 실행 권한 추가: `chmod +x scripts/k6/run-benchmark.sh`

**README.md** — Docker 실행 명령어, 전략 전환법, 결과 해석법:
아래 섹션을 포함한다:
1. 사전 조건 (Docker, 서버, MySQL/Kafka)
2. 개별 스크립트 실행 (macOS / Linux 명령어 각각)
3. 전체 자동 실행 (run-benchmark.sh)
4. 전략 전환 방법 (application.yml 수정 + 재기동)
5. k6 결과 읽는 법 (http_req_duration p50/p95/p99, http_reqs/s, checks 에러율)
6. BENCHMARK.md 수치 기입 안내

macOS 개별 실행 예시:
```bash
docker run --rm \
  -v $(pwd)/scripts/k6:/scripts \
  -e BASE_URL=http://host.docker.internal:8080 \
  grafana/k6 run /scripts/sync.js
```

Linux 개별 실행 예시:
```bash
docker run --rm \
  --network host \
  -v $(pwd)/scripts/k6:/scripts \
  grafana/k6 run /scripts/sync.js
```
  </action>
  <verify>
    <automated>ls /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/scripts/k6/run-benchmark.sh /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/scripts/k6/README.md</automated>
  </verify>
  <done>run-benchmark.sh 존재하고 실행 권한 있음(chmod +x). README.md 존재하고 macOS/Linux 실행 명령어 두 가지 포함.</done>
</task>

</tasks>

<verification>
1. `ls scripts/k6/` — helpers.js, sync.js, outbox.js, kafka.js, run-benchmark.sh, README.md 모두 존재
2. sync.js 내 `check(res, { 'status is 200' })` 패턴 존재
3. outbox.js / kafka.js 내 `pollStatus(orderId)` 호출 존재
4. 세 스크립트 모두 `BENCHMARK_STAGES` import 존재 (동일 VU 조건 보장)
5. `./gradlew test` 통과 (Java 코드 무변경이므로 회귀 없어야 함)
</verification>

<success_criteria>
- scripts/k6/ 디렉토리에 6개 파일 모두 존재
- sync.js는 200 OK 검증, outbox.js/kafka.js는 202 → pollStatus() → DONE 검증
- 세 스크립트의 stages가 BENCHMARK_STAGES로 동일
- run-benchmark.sh는 common.sh 색상 함수 사용, 전략별 user prompt 포함
- `./gradlew test` 회귀 없음
</success_criteria>

<output>
완료 후 `.planning/phases/05-k6-benchmark/05-01-SUMMARY.md` 를 생성하세요.
</output>
