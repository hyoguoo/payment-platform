---
phase: 05-k6-benchmark
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/mock/FakeTossHttpOperator.java
  - src/main/java/com/hyoguoo/paymentplatform/mock/BenchmarkConfig.java
  - src/main/resources/application-benchmark.yml
autonomous: true
requirements:
  - BENCH-01
  - BENCH-03
  - BENCH-04

must_haves:
  truths:
    - "spring.profiles.active=benchmark 으로 애플리케이션을 기동하면 정상 시작된다"
    - "benchmark 프로파일에서 FakeTossHttpOperator가 실제 TossHttpOperator 대신 활성화된다"
    - "application-benchmark.yml에 stock 충분 확보를 위한 data-benchmark.sql 참조가 설정된다"
    - "기존 테스트(./gradlew test)가 모두 통과한다 — FakeTossHttpOperator 이동으로 회귀 없음"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/mock/FakeTossHttpOperator.java"
      provides: "benchmark 프로파일용 Fake Toss HTTP 오퍼레이터 (src/main에서 활성화)"
    - path: "src/main/java/com/hyoguoo/paymentplatform/mock/BenchmarkConfig.java"
      provides: "@Profile(\"benchmark\") + @Primary FakeTossHttpOperator 빈 등록"
    - path: "src/main/resources/application-benchmark.yml"
      provides: "benchmark 프로파일 설정 (FakeToss 활성화, async-strategy 기본값, pool 설정)"
  key_links:
    - from: "src/main/java/com/hyoguoo/paymentplatform/mock/BenchmarkConfig.java"
      to: "FakeTossHttpOperator"
      via: "@Bean @Primary"
      pattern: "@Primary"
    - from: "src/main/resources/application-benchmark.yml"
      to: "spring.payment.async-strategy"
      via: "Spring profile activation"
      pattern: "async-strategy"
---

<objective>
FakeTossHttpOperator를 src/main/java로 이동하고 @Profile("benchmark") BenchmarkConfig로 등록한다. application-benchmark.yml을 신규 생성해 benchmark 프로파일을 정의한다.

Purpose: 부하 테스트 시 실제 Toss API 호출 없이 일관된 응답을 반환하도록 Fake 빈을 프로덕션 컨텍스트에서 안전하게 활성화한다. stock 부족 문제(data.sql stock=59)를 해결해 1,000개 orderId 사전 생성을 보장한다.
Output: src/main/java/mock/FakeTossHttpOperator.java, src/main/java/mock/BenchmarkConfig.java, src/main/resources/application-benchmark.yml
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-k6-benchmark/05-CONTEXT.md
@.planning/phases/05-k6-benchmark/05-RESEARCH.md
@src/main/resources/application.yml
@src/main/resources/application-docker.yml

<interfaces>
<!-- FakeTossHttpOperator 현재 위치: src/test/java/com/hyoguoo/paymentplatform/mock/FakeTossHttpOperator.java -->
<!-- HttpOperator 인터페이스: src/main/java/com/hyoguoo/paymentplatform/core/common/infrastructure/http/HttpOperator.java -->

<!-- application-docker.yml 패턴 참고 (profile 구조) -->
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://mysql:3306/payment_db?...

<!-- 기존 ConditionalOnProperty 어댑터 패턴 (SyncConfirmAdapter 예시) -->
@Configuration
@ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "sync", matchIfMissing = true)
public class SyncConfirmAdapter implements PaymentConfirmAsyncPort { ... }

<!-- data.sql 확인됨: stock = 59 (product 1, 2 각각) — 1,000개 checkout 불가 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FakeTossHttpOperator를 src/main으로 이동 + BenchmarkConfig 생성</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/mock/FakeTossHttpOperator.java,
    src/main/java/com/hyoguoo/paymentplatform/mock/BenchmarkConfig.java
  </files>
  <action>
**Step 1: FakeTossHttpOperator 이동**

`src/test/java/com/hyoguoo/paymentplatform/mock/FakeTossHttpOperator.java` 파일을 그대로 복사해 `src/main/java/com/hyoguoo/paymentplatform/mock/FakeTossHttpOperator.java` 에 생성한다. 패키지 선언은 `com.hyoguoo.paymentplatform.mock` 그대로 유지. 내용 수정 없음.

기존 test 소스의 FakeTossHttpOperator.java는 삭제하지 않는다 — 테스트 코드가 이미 test 소스의 클래스를 참조하고 있으므로 양쪽에 동일한 파일이 존재해도 패키지가 같으면 컴파일 시 충돌이 발생한다.

따라서: src/test/java의 기존 파일을 삭제하고 src/main/java에 새로 생성한다. 테스트에서 FakeTossHttpOperator를 import할 경우 src/main의 클래스를 참조하게 되므로 테스트 회귀가 없다.

**Step 2: BenchmarkConfig 생성**

`src/main/java/com/hyoguoo/paymentplatform/mock/BenchmarkConfig.java` 를 신규 생성:

```java
package com.hyoguoo.paymentplatform.mock;

import com.hyoguoo.paymentplatform.core.common.infrastructure.http.HttpOperator;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Primary;
import org.springframework.context.annotation.Profile;

@Configuration
@Profile("benchmark")
public class BenchmarkConfig {

    @Bean
    @Primary
    public HttpOperator httpOperator() {
        return new FakeTossHttpOperator();
    }
}
```

`@Primary` 를 사용해 기존 실제 TossHttpOperator 빈보다 우선순위를 높인다. `@Profile("benchmark")` 으로 benchmark 프로파일에서만 활성화된다 — 프로덕션/테스트 환경에서는 동작하지 않는다.

Lombok을 사용하지 않는다 — BenchmarkConfig는 단순 @Bean 메서드만 포함하므로 Lombok 불필요.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>./gradlew test 통과. src/main/java/com/hyoguoo/paymentplatform/mock/ 에 FakeTossHttpOperator.java와 BenchmarkConfig.java 모두 존재.</done>
</task>

<task type="auto">
  <name>Task 2: application-benchmark.yml 생성 + data-benchmark.sql</name>
  <files>
    src/main/resources/application-benchmark.yml,
    src/main/resources/data-benchmark.sql
  </files>
  <action>
**application-benchmark.yml 생성:**

```yaml
spring:
  config:
    activate:
      on-profile: benchmark

  payment:
    async-strategy: sync   # 기본값: 각 전략 측정 시 변경 후 재기동

  datasource:
    hikari:
      maximum-pool-size: 30   # 200 VU 부하 대응 (기본값 10보다 높임)

  sql:
    init:
      data-locations: classpath:data.sql, classpath:data-benchmark.sql
```

`spring.sql.init.data-locations` 로 기본 data.sql 이후 data-benchmark.sql을 추가 실행해 stock을 재설정한다.

**data-benchmark.sql 생성:**

```sql
-- benchmark 프로파일 전용 stock 초기화
-- setup()에서 1,000개 checkout 생성을 위해 충분한 stock 확보
UPDATE product SET stock = 5000 WHERE id = 1;
UPDATE product SET stock = 5000 WHERE id = 2;
```

stock = 5000 으로 설정해 1,000개 checkout + 실제 부하 테스트 이터레이션을 충분히 처리할 수 있도록 한다(data.sql의 stock=59로는 60번째 checkout부터 실패).
  </action>
  <verify>
    <automated>ls /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/src/main/resources/application-benchmark.yml /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/src/main/resources/data-benchmark.sql</automated>
  </verify>
  <done>application-benchmark.yml과 data-benchmark.sql 모두 존재. yml에 spring.payment.async-strategy, hikari pool size, data-benchmark.sql 참조가 포함됨.</done>
</task>

</tasks>

<verification>
1. `./gradlew test` 통과 — FakeTossHttpOperator test→main 이동으로 기존 테스트 회귀 없음
2. `ls src/main/java/com/hyoguoo/paymentplatform/mock/` — BenchmarkConfig.java, FakeTossHttpOperator.java 존재
3. `ls src/main/resources/application-benchmark.yml` — 파일 존재
4. application-benchmark.yml에 `spring.payment.async-strategy` 설정 포함 확인
5. data-benchmark.sql에 `UPDATE product SET stock = 5000` 구문 존재
</verification>

<success_criteria>
- ./gradlew test 전체 통과
- BenchmarkConfig @Profile("benchmark") + @Primary @Bean HttpOperator 구조로 작성됨
- application-benchmark.yml에 hikari pool-size 30, data-benchmark.sql 참조 포함
- data-benchmark.sql로 stock=5000 설정 (1,000개 setup() checkout 보장)
</success_criteria>

<output>
완료 후 `.planning/phases/05-k6-benchmark/05-02-SUMMARY.md` 를 생성하세요.
</output>
