---
phase: 05-k6-benchmark
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - BENCHMARK.md
autonomous: true
requirements:
  - BENCH-03
  - BENCH-05

must_haves:
  truths:
    - "BENCHMARK.md가 프로젝트 루트에 존재한다"
    - "TPS / p50 / p95 / p99 / 에러율 비교 표가 포함된다 (수치는 자리표시자)"
    - "전략별 특성 해석 섹션이 포함된다 (Sync 동기 병목 / Outbox 배치 지연 / Kafka 컨슈머 병렬성)"
    - "어댑터 선택 가이드가 포함된다 (고TPS / 안정성 / 단순성 시나리오)"
    - "측정 환경 및 조건이 명시된다 (VU 수, 단계별 시간, Fake Toss 사용)"
  artifacts:
    - path: "BENCHMARK.md"
      provides: "k6 벤치마크 결과 문서 (수치 자리표시자 포함 템플릿)"
      contains: "## 비교 결과"
  key_links:
    - from: "BENCHMARK.md"
      to: "scripts/k6/run-benchmark.sh"
      via: "문서 내 실행 명령어 참조"
      pattern: "run-benchmark.sh"
---

<objective>
프로젝트 루트에 BENCHMARK.md를 작성한다. 실제 k6 실행 결과를 채워 넣을 수 있는 템플릿 구조로, 측정 환경, 비교 표(수치 자리표시자), 전략별 해석, 어댑터 선택 가이드를 포함한다.

Purpose: 포트폴리오 리뷰어가 세 전략의 성능 차이를 한눈에 비교하고 "언제 어떤 전략을 쓸지"를 즉시 이해할 수 있는 결과 문서를 제공한다.
Output: BENCHMARK.md (프로젝트 루트)
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-k6-benchmark/05-CONTEXT.md
@.planning/phases/05-k6-benchmark/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BENCHMARK.md 작성 — 템플릿 구조</name>
  <files>BENCHMARK.md</files>
  <action>
프로젝트 루트에 BENCHMARK.md를 작성한다. 아래 섹션 구조를 준수한다.

**섹션 1: 개요**
- 프로젝트 목적 한 줄 요약
- 측정 목표: 세 전략(Sync / DB Outbox / Kafka)의 TPS·레이턴시·에러율 정량 비교

**섹션 2: 측정 환경 및 조건**
표 형식:
| 항목 | 값 |
|------|-----|
| k6 실행 방식 | Docker (grafana/k6) |
| VU 단계 | 50 → 100 → 200 VU |
| 단계별 지속 시간 | 60초 (전체 3분) |
| 부하 패턴 | Constant load (ramping 없음) |
| Toss API | FakeTossHttpOperator (실제 API 미사용) |
| 전략 전환 방법 | spring.payment.async-strategy 값 변경 + 재기동 |
| 테스트 데이터 | setup()에서 1,000개 orderId 사전 생성 |
| 폴링 방식 (비동기) | 500ms 간격, 30초 타임아웃 |

비고 섹션에 "비동기 전략(Outbox, Kafka)은 202 반환 후 /status 폴링으로 DONE 확인까지의 end-to-end 시간을 측정한다. 단순 HTTP 응답 시간이 아닌 처리 완료까지의 시간이므로 Sync와의 직접 비교 시 이 점을 고려해야 한다." 를 포함한다.

**섹션 3: 비교 결과**

VU 단계별(50 / 100 / 200) 소제목으로 구분하고, 각 단계에 아래 표를 배치한다:

| 전략 | TPS (req/s) | p50 (ms) | p95 (ms) | p99 (ms) | 에러율 | e2e p50 (ms) | e2e p95 (ms) |
|------|-------------|----------|----------|----------|--------|--------------|--------------|
| Sync | - | - | - | - | - | N/A | N/A |
| DB Outbox | - | - | - | - | - | - | - |
| Kafka | - | - | - | - | - | - | - |

수치 자리표시자는 `-` 로 표기. e2e 열은 비동기 전략 전용 (Sync는 N/A).

하단에 "위 수치는 k6 실행 후 채워 넣는다. run-benchmark.sh 실행 방법은 scripts/k6/README.md를 참고하세요." 안내 문구 포함.

**섹션 4: 전략별 특성 해석**

각 전략에 대해 소제목(###)으로 구분:

**Sync (동기 처리)**
- HTTP 요청-응답 사이클 전체가 단일 스레드에서 처리됨
- Toss API 호출 시간이 직접 클라이언트 응답 지연에 반영됨 (Fake 환경에서는 제거됨)
- VU 증가 시 스레드 풀 경합으로 TPS가 선형보다 빠르게 감소할 가능성
- 장점: 단순성, 즉각적 결과, 에러 처리 명확

**DB Outbox (배치 배달)**
- confirm은 DB 쓰기만 수행해 빠르게 202 반환, Toss API는 백그라운드 워커가 처리
- 워커의 fixedDelay 스케줄링으로 처리 지연이 배치 단위로 발생 (e2e 레이턴시 증가)
- DB가 병목 — 동시 PENDING 레코드 증가 시 워커 처리율이 제한될 수 있음
- 장점: 외부 API 장애 시 재시도 보장, 확실한 내구성

**Kafka (이벤트 스트리밍)**
- confirm은 Kafka 발행 후 202 반환, 컨슈머 그룹이 병렬 처리
- 컨슈머 파티션 병렬성으로 Outbox보다 처리 throughput 향상 기대
- 네트워크 홉(브로커 경유)이 추가되어 초저지연은 Sync보다 불리
- 장점: 수평 확장성, 컨슈머 독립 배포, DLT 기반 실패 격리

**섹션 5: 어댑터 선택 가이드**

| 시나리오 | 추천 전략 | 이유 |
|----------|-----------|------|
| 응답 즉시성이 최우선, 외부 API 안정적 | Sync | 가장 단순, 클라이언트에 즉각 결과 전달 |
| 외부 API 장애 허용·재시도 내구성 필요, 단일 인스턴스 | DB Outbox | DB 트랜잭션으로 내구성 보장, Kafka 인프라 불필요 |
| 높은 TPS + 수평 확장 + 마이크로서비스 | Kafka | 컨슈머 파티션 병렬성, 서비스 분리 가능 |

**섹션 6: 실행 방법 요약**

scripts/k6/README.md 링크와 간단한 실행 명령어 예시(macOS)를 포함한다.
  </action>
  <verify>
    <automated>ls /Users/hyoguoo/Repositories/hyoguoo/projects/payment-platform/BENCHMARK.md</automated>
  </verify>
  <done>BENCHMARK.md 프로젝트 루트에 존재. VU 단계별(50/100/200) 비교 표, 전략별 해석, 어댑터 선택 가이드 모두 포함됨.</done>
</task>

</tasks>

<verification>
1. `ls BENCHMARK.md` — 파일 존재
2. 파일 내 "## 비교 결과" 섹션 존재
3. 파일 내 "e2e" 열 존재 (비동기 end-to-end 레이턴시)
4. 파일 내 "어댑터 선택 가이드" 또는 동등한 섹션 존재
5. 수치 자리표시자 `-` 포함 (아직 k6 미실행 상태)
</verification>

<success_criteria>
- BENCHMARK.md 프로젝트 루트에 존재
- VU 50/100/200 단계별 TPS/p50/p95/p99/에러율/e2e 비교 표 포함
- 세 전략(Sync/Outbox/Kafka) 특성 해석 섹션 포함
- 어댑터 선택 가이드 표 포함 (시나리오 → 추천 전략 → 이유)
- 측정 환경(VU 단계, 기간, FakeToss 사용) 명시
</success_criteria>

<output>
완료 후 `.planning/phases/05-k6-benchmark/05-03-SUMMARY.md` 를 생성하세요.
</output>
