---
phase: 01-port-contract-status-endpoint
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/dto/response/PaymentStatusApiResponse.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/dto/response/PaymentStatusResponse.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerTest.java
autonomous: true
requirements:
  - STATUS-01
  - STATUS-02
  - STATUS-03

must_haves:
  truths:
    - "GET /api/v1/payments/{orderId}/status 엔드포인트가 200과 함께 orderId, status, approvedAt을 반환한다"
    - "존재하지 않는 orderId 조회 시 404가 반환된다"
    - "PaymentEventStatus.DONE → PaymentStatusResponse.DONE, FAILED → FAILED, 나머지 → PROCESSING 매핑이 적용된다"
    - "통합 테스트가 PaymentControllerTest에 추가되어 통과한다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/presentation/dto/response/PaymentStatusApiResponse.java"
      provides: "Status 엔드포인트 응답 DTO"
      contains: "orderId, status(PaymentStatusResponse), approvedAt"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/presentation/dto/response/PaymentStatusResponse.java"
      provides: "상태 조회 enum"
      contains: "PENDING, PROCESSING, DONE, FAILED"
  key_links:
    - from: "PaymentController.getPaymentStatus()"
      to: "PaymentLoadUseCase.getPaymentEventByOrderId()"
      via: "paymentLoadUseCase DI"
      pattern: "paymentLoadUseCase\\.getPaymentEventByOrderId"
    - from: "PaymentPresentationMapper.toPaymentStatusApiResponse()"
      to: "PaymentStatusResponse"
      via: "switch expression — PaymentEventStatus → PaymentStatusResponse"
      pattern: "mapToPaymentStatusResponse"
---

<objective>
비동기 클라이언트가 결제 완료 여부를 확인하는 Status 엔드포인트를 추가한다.

Purpose: Phase 3/4 비동기 어댑터 도입 시 클라이언트가 폴링할 수 있는 엔드포인트. Phase 1에서 미리 정의해 후속 Phase의 클라이언트 인터페이스를 확정한다.
Output: PaymentStatusApiResponse.java(신규), PaymentStatusResponse.java(신규 enum), PaymentController의 status 엔드포인트 추가, PaymentControllerTest에 STATUS 시나리오 추가
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-port-contract-status-endpoint/01-CONTEXT.md
@.planning/phases/01-port-contract-status-endpoint/01-RESEARCH.md
@.planning/phases/01-port-contract-status-endpoint/01-02-SUMMARY.md

<interfaces>
<!-- executor가 직접 참조하는 기존 코드. 별도 탐색 불필요. -->

PaymentLoadUseCase.getPaymentEventByOrderId() — 재사용:
```java
// payment/application/usecase/PaymentLoadUseCase.java
public PaymentEvent getPaymentEventByOrderId(String orderId) {
    return paymentEventRepository
            .findByOrderId(orderId)
            .orElseThrow(() -> PaymentFoundException.of(PaymentErrorCode.PAYMENT_EVENT_NOT_FOUND));
}
```

PaymentEvent 주요 필드 (Status 응답에 사용):
```java
// payment/domain/PaymentEvent.java
private String orderId;
private PaymentEventStatus status;  // enum: READY, IN_PROGRESS, DONE, FAILED, CANCELED, PARTIAL_CANCELED, EXPIRED, UNKNOWN
private LocalDateTime approvedAt;   // nullable — DONE 전까지 null
```

PaymentEventStatus enum 전체 값:
```java
READY, IN_PROGRESS, DONE, FAILED, CANCELED, PARTIAL_CANCELED, EXPIRED, UNKNOWN
```

PaymentFoundException → PaymentExceptionHandler → HTTP 404 (이미 처리됨):
```java
@ExceptionHandler(PaymentFoundException.class)
public ResponseEntity<ErrorResponse> catchRuntimeException(PaymentFoundException e) {
    return ResponseEntity.status(HttpStatus.NOT_FOUND).body(ErrorResponse.of(e.getCode(), e.getMessage()));
}
```

PaymentController 현재 DI 필드 (Plan 02 수정 후):
```java
private final PaymentCheckoutService paymentCheckoutService;
private final PaymentConfirmService paymentConfirmService;
// paymentLoadUseCase 없음 — 이 Plan에서 추가
```

PaymentPresentationMapper 현재 메서드 (Plan 02 수정 후):
```java
// 기존 메서드는 유지, 이 Plan에서 신규 메서드 추가
public static PaymentStatusApiResponse toPaymentStatusApiResponse(PaymentEvent event) { ... }
private static PaymentStatusResponse mapToPaymentStatusResponse(PaymentEventStatus status) { ... }
```

IntegrationTest 베이스 클래스:
```java
// @SpringBootTest + @AutoConfigureMockMvc + @Sql("/data-test.sql")
// PaymentControllerTest extends IntegrationTest
// MockMvc 주입 가능: @Autowired MockMvc mockMvc
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaymentStatusResponse enum + PaymentStatusApiResponse DTO 생성</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/dto/response/PaymentStatusResponse.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/dto/response/PaymentStatusApiResponse.java
  </files>
  <action>
    1. `PaymentStatusResponse.java` 신규 생성:
       - 패키지: `com.hyoguoo.paymentplatform.payment.presentation.dto.response`
       - 단순 enum: `public enum PaymentStatusResponse { PENDING, PROCESSING, DONE, FAILED }`
       - 추가 어노테이션 없음 — 단순 응답 분류용 enum

    2. `PaymentStatusApiResponse.java` 신규 생성:
       - 패키지: `com.hyoguoo.paymentplatform.payment.presentation.dto.response`
       - 어노테이션: `@Getter @Builder`
       - 필드:
         - `private final String orderId`
         - `private final PaymentStatusResponse status`
         - `private final LocalDateTime approvedAt`  (nullable — DONE 전까지 null, 직렬화 시 null 포함)
       - Lombok Builder로 생성자 대체

    `approvedAt` null 처리: `@JsonInclude` 없이 null 그대로 직렬화 — STATUS-02에서 approvedAt 포함을 요구하며, null 값도 JSON에 포함된다.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>PaymentStatusResponse.java와 PaymentStatusApiResponse.java 파일 존재, 컴파일 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 매퍼 메서드 추가 + 컨트롤러 엔드포인트 추가 + 통합 테스트</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerTest.java
  </files>
  <behavior>
    - GET /api/v1/payments/{known-orderId}/status → 200 OK, body에 orderId·status·approvedAt 포함
    - GET /api/v1/payments/{unknown-orderId}/status → 404 Not Found
    - DONE 상태 PaymentEvent 조회 시 status == "DONE", approvedAt non-null
    - READY/IN_PROGRESS 상태 PaymentEvent 조회 시 status == "PROCESSING"
  </behavior>
  <action>
    1. `PaymentPresentationMapper.java`에 신규 메서드 추가 (기존 메서드 수정 없음):
       ```java
       public static PaymentStatusApiResponse toPaymentStatusApiResponse(PaymentEvent event) {
           return PaymentStatusApiResponse.builder()
                   .orderId(event.getOrderId())
                   .status(mapToPaymentStatusResponse(event.getStatus()))
                   .approvedAt(event.getApprovedAt())
                   .build();
       }

       private static PaymentStatusResponse mapToPaymentStatusResponse(PaymentEventStatus status) {
           return switch (status) {
               case DONE -> PaymentStatusResponse.DONE;
               case FAILED -> PaymentStatusResponse.FAILED;
               default -> PaymentStatusResponse.PROCESSING;
           };
       }
       ```
       import 추가: `PaymentEvent`, `PaymentEventStatus`, `PaymentStatusApiResponse`, `PaymentStatusResponse`

    2. `PaymentController.java` 수정:
       - `paymentLoadUseCase` DI 필드 추가: `private final PaymentLoadUseCase paymentLoadUseCase`
       - 신규 엔드포인트 추가:
         ```java
         @GetMapping("/api/v1/payments/{orderId}/status")
         public ResponseEntity<PaymentStatusApiResponse> getPaymentStatus(
                 @PathVariable String orderId) {
             PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(orderId);
             return ResponseEntity.ok(PaymentPresentationMapper.toPaymentStatusApiResponse(paymentEvent));
         }
         ```
       - import 추가: `PaymentLoadUseCase`, `PaymentStatusApiResponse`, `GetMapping`, `PathVariable`

    3. `PaymentControllerTest.java`에 테스트 메서드 3개 추가 (기존 테스트 수정 없음):

       a. `getPaymentStatus_Done_Success`:
          - 전제: DONE 상태 PaymentEvent를 PAYMENT_EVENT_INSERT_SQL로 삽입 (approved_at 포함)
          - `GET /api/v1/payments/{orderId}/status` 호출
          - 기대: 200 OK, body.orderId == TEST_ORDER_ID, body.status == "DONE", body.approvedAt non-null
          - 응답 역직렬화: `objectMapper.readValue(...)` — `BasicResponse<PaymentStatusApiResponse>` 타입
          - `PaymentStatusApiResponse`에 Jackson mixin이 필요한지 확인: `@Getter @Builder` 사용 시 역직렬화를 위해 `@JsonCreator` 또는 mixin 필요. 기존 패턴(PaymentConfirmResponseMixin)을 참고해 `PaymentStatusApiResponseMixin`을 추가하거나, `objectMapper.addMixIn` 적용.

       b. `getPaymentStatus_Processing_Success`:
          - 전제: READY 상태 PaymentEvent 삽입 (approved_at=null)
          - 기대: 200 OK, body.status == "PROCESSING", body.approvedAt == null

       c. `getPaymentStatus_NotFound`:
          - 존재하지 않는 orderId로 GET 요청
          - 기대: 404 Not Found

    테스트 작성 후 `./gradlew test --tests "*.PaymentControllerTest"` 실행하여 기존 테스트 포함 전체 통과 확인.

    `@Sql` 정책: `PaymentControllerTest`의 `@BeforeEach`에서 `jpaPaymentEventRepository.deleteAllInBatch()`로 초기화 — 각 테스트 메서드가 자체 데이터 삽입.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentControllerTest"</automated>
  </verify>
  <done>GET /api/v1/payments/{orderId}/status 엔드포인트 동작, 3개 신규 테스트 통과, 기존 PaymentControllerTest 회귀 없음</done>
</task>

</tasks>

<verification>
- `./gradlew test` 전체 통과 (Phase 1 최종 게이트)
- `GET /api/v1/payments/{orderId}/status` 200 반환 + orderId, status, approvedAt 포함
- 존재하지 않는 orderId 조회 시 404 반환
- PaymentEventStatus.DONE → "DONE", 나머지 → "PROCESSING" 매핑 확인
- application.yml에 `spring.payment.async-strategy: sync` 존재 확인
</verification>

<success_criteria>
- Status 엔드포인트가 동작하고 PaymentControllerTest의 STATUS 시나리오 3개가 모두 통과한다
- `./gradlew test` 전체 그린 — Phase 1 게이트 통과
- Phase 1의 모든 요구사항(PORT-01~04, STATUS-01~03)이 코드와 테스트로 구현되었다
</success_criteria>

<output>
완료 후 `.planning/phases/01-port-contract-status-endpoint/01-03-SUMMARY.md` 파일을 생성한다.
</output>
