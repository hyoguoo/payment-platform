---
phase: 01-port-contract-status-endpoint
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/port/PaymentConfirmService.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/dto/response/PaymentConfirmAsyncResult.java
autonomous: true
requirements:
  - PORT-01
  - PORT-02

must_haves:
  truths:
    - "PaymentConfirmService 인터페이스의 confirm() 반환 타입이 PaymentConfirmAsyncResult로 변경되어 컴파일된다"
    - "PaymentConfirmAsyncResult는 ResponseType enum(SYNC_200, ASYNC_202)과 orderId, amount(nullable) 필드를 갖는다"
    - "컨트롤러가 ResponseType 값만으로 200/202를 결정할 수 있는 구조가 성립한다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/dto/response/PaymentConfirmAsyncResult.java"
      provides: "포트 반환 DTO — ResponseType enum 내장"
      contains: "ResponseType, SYNC_200, ASYNC_202, orderId, amount"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/presentation/port/PaymentConfirmService.java"
      provides: "포트 인터페이스 — 반환 타입 변경"
      contains: "PaymentConfirmAsyncResult confirm(PaymentConfirmCommand)"
  key_links:
    - from: "PaymentConfirmService.confirm()"
      to: "PaymentConfirmAsyncResult"
      via: "interface return type"
      pattern: "PaymentConfirmAsyncResult confirm"
---

<objective>
세 어댑터(Sync/Outbox/Kafka) 모두의 컴파일 의존점이 되는 포트 인터페이스와 결과 DTO를 정의한다.

Purpose: 이 Plan의 산출물이 Wave 2 이후 모든 작업의 컴파일 계약이 된다. 인터페이스 먼저 확정해야 어댑터와 컨트롤러 수정이 독립적으로 진행 가능하다.
Output: PaymentConfirmAsyncResult.java (신규), PaymentConfirmService.java (반환 타입 변경)
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-port-contract-status-endpoint/01-CONTEXT.md
@.planning/phases/01-port-contract-status-endpoint/01-RESEARCH.md

<interfaces>
<!-- 수정 대상 파일의 현재 코드. executor는 이 계약을 기반으로 변경한다. -->

현재 PaymentConfirmService.java:
```java
package com.hyoguoo.paymentplatform.payment.presentation.port;

import com.hyoguoo.paymentplatform.payment.application.dto.request.PaymentConfirmCommand;
import com.hyoguoo.paymentplatform.payment.application.dto.response.PaymentConfirmResult;

public interface PaymentConfirmService {
    PaymentConfirmResult confirm(PaymentConfirmCommand paymentConfirmCommand);
}
```

현재 PaymentConfirmResult.java (내부 DTO — 삭제 금지):
```java
package com.hyoguoo.paymentplatform.payment.application.dto.response;

import java.math.BigDecimal;
import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
public class PaymentConfirmResult {
    private final String orderId;
    private final BigDecimal amount;
}
```

PaymentConfirmCommand.java (파라미터 타입 — 변경 없음):
```java
// payment/application/dto/request/PaymentConfirmCommand.java
// 기존 필드: userId(Long), orderId(String), paymentKey(String), amount(BigDecimal)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentConfirmAsyncResult DTO 신규 생성</name>
  <files>src/main/java/com/hyoguoo/paymentplatform/payment/application/dto/response/PaymentConfirmAsyncResult.java</files>
  <behavior>
    - ResponseType.SYNC_200으로 빌드 시 responseType == SYNC_200
    - ResponseType.ASYNC_202으로 빌드 시 responseType == ASYNC_202
    - amount 필드는 null 가능 (BigDecimal, nullable)
    - orderId 필드는 non-null String
    - @Getter + @Builder Lombok 패턴 적용 (코드베이스 전체 규칙)
  </behavior>
  <action>
    `src/main/java/com/hyoguoo/paymentplatform/payment/application/dto/response/PaymentConfirmAsyncResult.java` 파일을 신규 생성한다.

    구현 사양:
    - 패키지: `com.hyoguoo.paymentplatform.payment.application.dto.response`
    - 클래스 어노테이션: `@Getter @Builder`
    - ResponseType을 내부 enum으로 정의: `public enum ResponseType { SYNC_200, ASYNC_202 }`
    - 필드: `private final ResponseType responseType`, `private final String orderId`, `private final BigDecimal amount` (amount는 nullable)
    - 코드베이스 전체 Lombok 패턴 준수: `private final` 필드 + `@Getter @Builder`

    기존 `PaymentConfirmResult.java`는 수정하지 않는다 — `PaymentConfirmServiceImpl`의 내부 반환 타입으로 계속 사용됨.

    테스트 파일을 먼저 작성(RED), 그 다음 구현(GREEN) 순서로 진행:
    - 테스트 위치: `src/test/java/com/hyoguoo/paymentplatform/payment/application/dto/response/PaymentConfirmAsyncResultTest.java`
    - ResponseType.SYNC_200 / ASYNC_202 빌더 생성 검증, amount null 허용 검증
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentConfirmAsyncResultTest" -x integrationTest 2>/dev/null || ./gradlew test --tests "*.PaymentConfirmAsyncResultTest"</automated>
  </verify>
  <done>PaymentConfirmAsyncResult.java 파일 존재, ResponseType enum 포함, 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: PaymentConfirmService 인터페이스 반환 타입 변경</name>
  <files>src/main/java/com/hyoguoo/paymentplatform/payment/presentation/port/PaymentConfirmService.java</files>
  <action>
    `PaymentConfirmService.java`의 `confirm()` 메서드 반환 타입을 `PaymentConfirmResult` → `PaymentConfirmAsyncResult`로 변경한다.

    변경 전:
    ```java
    PaymentConfirmResult confirm(PaymentConfirmCommand paymentConfirmCommand);
    ```

    변경 후:
    ```java
    PaymentConfirmAsyncResult confirm(PaymentConfirmCommand paymentConfirmCommand);
    ```

    import도 `PaymentConfirmResult` → `PaymentConfirmAsyncResult`로 교체한다.

    이 변경 후 `PaymentConfirmServiceImpl`이 `implements PaymentConfirmService`를 유지하면 컴파일 오류가 발생한다. 이는 예상된 상황이며 Wave 2(Plan 02)에서 `implements` 제거와 `SyncConfirmAdapter` 추가로 해결된다.

    컴파일 확인:
    ```
    ./gradlew compileJava
    ```
    이 시점에서 `PaymentConfirmServiceImpl`의 컴파일 오류는 예상 동작이다. Plan 02에서 수정한다.
  </action>
  <verify>
    <automated>./gradlew compileJava 2>&1 | grep -E "PaymentConfirmService\.java|error:" | head -5; echo "interface file updated"</automated>
  </verify>
  <done>
    PaymentConfirmService.java의 confirm() 반환 타입이 PaymentConfirmAsyncResult로 변경됨.

    [CLAUDE.md 예외] 이 태스크는 의도적 컴파일 불완전 상태를 만든다 — PaymentConfirmServiceImpl이
    아직 implements PaymentConfirmService를 유지하므로 ./gradlew test가 실패한다. CLAUDE.md의
    "Verify: Run ./gradlew test after every task" 규칙은 이 태스크 한정으로 면제된다.
    전체 컴파일 복구는 Plan 02 Task 1(SyncConfirmAdapter 생성 + implements 제거)에서 이루어진다.
    Plan 02 완료 후 ./gradlew test를 반드시 실행한다.
  </done>
</task>

</tasks>

<verification>
- PaymentConfirmAsyncResult.java 존재, ResponseType enum(SYNC_200, ASYNC_202) 포함
- PaymentConfirmService.java의 confirm() 시그니처: `PaymentConfirmAsyncResult confirm(PaymentConfirmCommand)`
- PaymentConfirmResult.java 삭제되지 않음 (기존 내부 DTO 유지)
- PaymentConfirmAsyncResultTest 테스트 통과
</verification>

<success_criteria>
- PaymentConfirmAsyncResult가 빌드되고 ResponseType enum이 외부에서 참조 가능하다
- PaymentConfirmService 인터페이스가 PaymentConfirmAsyncResult를 반환 타입으로 선언한다
- 기존 PaymentConfirmResult.java는 삭제되지 않았다
</success_criteria>

<output>
완료 후 `.planning/phases/01-port-contract-status-endpoint/01-01-SUMMARY.md` 파일을 생성한다.
</output>
