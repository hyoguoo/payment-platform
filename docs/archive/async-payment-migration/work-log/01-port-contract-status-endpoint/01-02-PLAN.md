---
phase: 01-port-contract-status-endpoint
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImpl.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapter.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java
  - src/main/resources/application.yml
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapterTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerTest.java
autonomous: true
requirements:
  - PORT-02
  - PORT-03
  - PORT-04

must_haves:
  truths:
    - "SyncConfirmAdapter가 @ConditionalOnProperty(sync, matchIfMissing=true)로 등록되고, spring.payment.async-strategy 미설정 시 자동 활성화된다"
    - "PaymentConfirmServiceImpl에서 implements PaymentConfirmService가 제거되어 Bean 충돌이 없다"
    - "컨트롤러 confirm()이 ResponseType.SYNC_200 시 200 OK, ASYNC_202 시 202 Accepted를 반환한다"
    - "spring.payment.async-strategy=sync 설정 시 SyncConfirmAdapter가 유일한 PaymentConfirmService Bean으로 등록된다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapter.java"
      provides: "PaymentConfirmService 구현체 — SyncConfirmAdapter"
      contains: "@ConditionalOnProperty, implements PaymentConfirmService, paymentConfirmServiceImpl 위임"
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapterTest.java"
      provides: "PORT-03, PORT-04 커버 단위 테스트"
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerTest.java"
      provides: "PORT-02 커버 — SYNC_200 → HTTP 200 검증"
  key_links:
    - from: "SyncConfirmAdapter.confirm()"
      to: "PaymentConfirmServiceImpl.confirm()"
      via: "DI 위임"
      pattern: "paymentConfirmServiceImpl\\.confirm"
    - from: "PaymentController.confirm()"
      to: "ResponseEntity"
      via: "ResponseType enum 분기"
      pattern: "ResponseType\\.ASYNC_202"
---

<objective>
SyncConfirmAdapter를 생성해 PaymentConfirmService 포트를 구현하고, 컨트롤러를 ResponseEntity 반환으로 수정해 200/202 분기를 처리한다. 앱이 이 Plan 완료 후 정상 부트 가능해야 한다.

Purpose: Phase 1 완료 기준(앱 부트 가능)을 달성하는 핵심 Plan. Sync 어댑터를 기본값으로 등록해 Spring Bean 교체 메커니즘을 처음으로 검증한다.
Output: SyncConfirmAdapter.java(신규), PaymentConfirmServiceImpl.java(implements 제거), PaymentController.java(confirm 반환 타입 변경), SyncConfirmAdapterTest.java(신규), PaymentControllerTest.java(SYNC_200→200 테스트 추가)
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-port-contract-status-endpoint/01-CONTEXT.md
@.planning/phases/01-port-contract-status-endpoint/01-RESEARCH.md
@.planning/phases/01-port-contract-status-endpoint/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 확정된 계약. executor는 이 타입을 직접 사용한다. -->

PaymentConfirmAsyncResult.java (Plan 01 산출물):
```java
package com.hyoguoo.paymentplatform.payment.application.dto.response;

import java.math.BigDecimal;
import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
public class PaymentConfirmAsyncResult {

    private final ResponseType responseType;
    private final String orderId;
    private final BigDecimal amount;  // nullable (async 어댑터 시)

    public enum ResponseType {
        SYNC_200,
        ASYNC_202
    }
}
```

PaymentConfirmService.java (Plan 01 수정 후):
```java
public interface PaymentConfirmService {
    PaymentConfirmAsyncResult confirm(PaymentConfirmCommand paymentConfirmCommand);
}
```

PaymentConfirmServiceImpl.confirm() 반환 타입 (변경 전):
```java
// 현재: implements PaymentConfirmService → Plan 02에서 implements 제거
// confirm()은 PaymentConfirmResult (application/dto/response)를 반환함 — 내부 DTO 유지
return PaymentConfirmResult.builder()
        .amount(completedPayment.getTotalAmount())
        .orderId(completedPayment.getOrderId())
        .build();
```

PaymentConfirmResponse.java (컨트롤러 응답 DTO — 변경 없음):
```java
@Getter @Builder
public class PaymentConfirmResponse {
    private final String orderId;
    private final BigDecimal amount;
}
```

PaymentController.java confirm() 현재 코드:
```java
@PostMapping("/api/v1/payments/confirm")
public PaymentConfirmResponse confirm(@RequestBody PaymentConfirmRequest paymentConfirmRequest) {
    PaymentConfirmCommand paymentConfirmCommand = PaymentPresentationMapper.toPaymentConfirmCommand(paymentConfirmRequest);
    PaymentConfirmResult paymentConfirmResult = paymentConfirmService.confirm(paymentConfirmCommand);
    return PaymentPresentationMapper.toPaymentConfirmResponse(paymentConfirmResult);
}
```

PaymentPresentationMapper.toPaymentConfirmResponse() 현재 코드:
```java
public static PaymentConfirmResponse toPaymentConfirmResponse(PaymentConfirmResult paymentConfirmResult) {
    return PaymentConfirmResponse.builder()
            .orderId(paymentConfirmResult.getOrderId())
            .amount(paymentConfirmResult.getAmount())
            .build();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SyncConfirmAdapter 생성 + PaymentConfirmServiceImpl implements 제거</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapter.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImpl.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapterTest.java
  </files>
  <behavior>
    - SyncConfirmAdapter.confirm()은 paymentConfirmServiceImpl.confirm(command)를 호출하고 결과를 감싼다
    - 반환값의 responseType은 ResponseType.SYNC_200
    - 반환값의 orderId는 PaymentConfirmResult.getOrderId()와 동일
    - 반환값의 amount는 PaymentConfirmResult.getAmount()와 동일
    - @ConditionalOnProperty(name="spring.payment.async-strategy", havingValue="sync", matchIfMissing=true) 적용
    - PaymentConfirmServiceImpl에서 implements PaymentConfirmService 제거 후 동일한 Bean으로 등록 (NoUniqueBeanDefinitionException 없음)
  </behavior>
  <action>
    1. `infrastructure/adapter/` 디렉토리가 없으면 생성한다.

    2. `SyncConfirmAdapter.java` 신규 생성:
       - 패키지: `com.hyoguoo.paymentplatform.payment.infrastructure.adapter`
       - 어노테이션: `@Slf4j @Service @RequiredArgsConstructor`
       - `@ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "sync", matchIfMissing = true)`
       - `implements PaymentConfirmService`
       - DI 필드: `private final PaymentConfirmServiceImpl paymentConfirmServiceImpl`
       - `confirm()` 구현: `paymentConfirmServiceImpl.confirm(command)`를 호출하고 `PaymentConfirmAsyncResult.builder().responseType(ResponseType.SYNC_200).orderId(result.getOrderId()).amount(result.getAmount()).build()` 반환
       - 로깅: LogFmt 패턴 — `@Slf4j` + `LogFmt.info(log, LogDomain.PAYMENT, EventType.PAYMENT_CONFIRM_START, ...)` 불필요 (PaymentConfirmServiceImpl 내부에서 이미 로깅)

    3. `PaymentConfirmServiceImpl.java` 수정:
       - `implements PaymentConfirmService` 제거
       - `@Override` 어노테이션 제거
       - import에서 `PaymentConfirmService` 제거
       - 메서드 시그니처를 `public PaymentConfirmResult confirm(PaymentConfirmCommand paymentConfirmCommand)`로 유지 (반환 타입은 PaymentConfirmResult — 내부 DTO 유지)
       - 내부 로직은 일절 수정하지 않는다 (SYNC-03 선행 준수)

    4. 테스트 작성 (`SyncConfirmAdapterTest.java`):
       - `PaymentConfirmServiceImpl`을 Mockito.mock()으로 생성
       - `SyncConfirmAdapter` 직접 생성 (Spring context 없이)
       - 테스트 시나리오:
         a. `confirm_success`: mock이 `PaymentConfirmResult`를 반환할 때, adapter가 `ResponseType.SYNC_200`과 동일한 orderId/amount를 반환하는지 검증
         b. `conditional_property`: `@ConditionalOnProperty` 어노테이션이 name="spring.payment.async-strategy", havingValue="sync", matchIfMissing=true로 선언되어 있는지 리플렉션으로 검증

    주의: SyncConfirmAdapter는 infrastructure/adapter 패키지에 배치 — 헥사고날 원칙 준수 (어댑터는 인프라스트럭처 계층)
  </action>
  <verify>
    <automated>./gradlew test --tests "*.SyncConfirmAdapterTest"</automated>
  </verify>
  <done>SyncConfirmAdapter.java 존재, SyncConfirmAdapterTest 통과, PaymentConfirmServiceImpl에서 implements PaymentConfirmService 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: 컨트롤러 confirm() ResponseEntity 분기 + 매퍼 수정 + application.yml 업데이트 + PORT-02 테스트</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java,
    src/main/resources/application.yml,
    src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerTest.java
  </files>
  <action>
    1. `PaymentPresentationMapper.java` 수정:
       - `toPaymentConfirmResponse()` 파라미터 타입을 `PaymentConfirmResult` → `PaymentConfirmAsyncResult`로 변경
       - 구현: `PaymentConfirmResponse.builder().orderId(result.getOrderId()).amount(result.getAmount()).build()`
       - import: `PaymentConfirmResult` 제거, `PaymentConfirmAsyncResult` 추가

    2. `PaymentController.java` 수정:
       - `confirm()` 반환 타입을 `PaymentConfirmResponse` → `ResponseEntity<PaymentConfirmResponse>`로 변경
       - `paymentConfirmService.confirm(command)` 결과를 `PaymentConfirmAsyncResult`로 받음
       - 분기 로직 추가:
         ```java
         if (result.getResponseType() == PaymentConfirmAsyncResult.ResponseType.ASYNC_202) {
             return ResponseEntity.accepted().body(PaymentPresentationMapper.toPaymentConfirmResponse(result));
         }
         return ResponseEntity.ok(PaymentPresentationMapper.toPaymentConfirmResponse(result));
         ```
       - 필요한 import 추가: `ResponseEntity`, `PaymentConfirmAsyncResult`

    3. `application.yml` 수정:
       - `spring:` 하위에 `payment.async-strategy: sync` 항목 추가
       - 주석 포함: `# 활성 어댑터: sync | outbox | kafka`
       - `@ConditionalOnProperty.name`과 정확히 일치해야 함: `spring.payment.async-strategy`
       - yml 경로: `spring.payment.async-strategy` → yml에서는 `spring:` 하위 `payment:` 하위 `async-strategy: sync`

    4. `PaymentControllerTest.java` — PORT-02 커버 테스트 추가 (기존 테스트 보존):
       - 기존 테스트(`confirmPayment_Success` 등)는 수정하지 않는다. 반환 타입이 ResponseEntity로 바뀌면
         기존 테스트의 `status().isOk()` 기대는 그대로 유지되므로 회귀 없음을 확인한다.
       - 신규 테스트 메서드 `confirmPayment_SyncAdapter_Returns200` 추가:
         - MockMvc + `@WebMvcTest` 또는 기존 테스트 클래스 컨벤션을 그대로 따른다.
         - `paymentConfirmService`를 `@MockBean`으로 선언하고 `PaymentConfirmAsyncResult.builder()
           .responseType(ResponseType.SYNC_200).orderId("order-1").amount(BigDecimal.valueOf(1000)).build()`
           를 반환하도록 stubbing한다.
         - `mockMvc.perform(post("/api/v1/payments/confirm").contentType(APPLICATION_JSON).content(...))
           .andExpect(status().isOk())` 로 HTTP 200을 단언한다.
         - 이 테스트가 PORT-02("ResponseType.SYNC_200 → HTTP 200 OK")의 자동 검증 근거가 된다.

    이 변경 후 `./gradlew compileJava`가 오류 없이 통과해야 한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentControllerTest"</automated>
  </verify>
  <done>PaymentController.confirm()이 ResponseEntity를 반환, application.yml에 spring.payment.async-strategy=sync 존재, PaymentControllerTest 전체 통과 (SYNC_200→HTTP 200 신규 테스트 포함)</done>
</task>

</tasks>

<verification>
- `./gradlew compileJava` 오류 없음
- `./gradlew test --tests "*.SyncConfirmAdapterTest"` 통과
- `./gradlew test --tests "*.PaymentControllerTest"` 통과 (기존 테스트 회귀 없음 + SYNC_200→200 신규 테스트 포함)
- PaymentConfirmServiceImpl.java에 `implements PaymentConfirmService` 없음
- SyncConfirmAdapter.java에 `@ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "sync", matchIfMissing = true)` 존재
</verification>

<success_criteria>
- 앱이 `spring.payment.async-strategy=sync` 설정으로 정상 부트된다 (Bean 충돌 없음)
- `spring.payment.async-strategy` 미설정 시 SyncConfirmAdapter가 기본 활성화된다 (matchIfMissing=true)
- SyncConfirmAdapter 단위 테스트가 통과한다
- 기존 PaymentControllerTest가 모두 통과한다 (회귀 없음)
- PaymentControllerTest에 `ResponseType.SYNC_200 → HTTP 200` 신규 테스트가 추가되어 PORT-02가 자동 검증된다
</success_criteria>

<output>
완료 후 `.planning/phases/01-port-contract-status-endpoint/01-02-SUMMARY.md` 파일을 생성한다.
</output>
