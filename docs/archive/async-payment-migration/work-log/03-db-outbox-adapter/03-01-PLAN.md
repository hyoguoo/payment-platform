---
phase: 03-db-outbox-adapter
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/domain/enums/PaymentOutboxStatus.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/domain/PaymentOutbox.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/port/PaymentOutboxRepository.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/entity/PaymentOutboxEntity.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/repository/JpaPaymentOutboxRepository.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/repository/PaymentOutboxRepositoryImpl.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/PaymentInfrastructureMapper.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapter.java
  - src/main/resources/application.yml
  - src/test/java/com/hyoguoo/paymentplatform/payment/domain/PaymentOutboxTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapterTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinatorTest.java
autonomous: true
requirements:
  - OUTBOX-01
  - OUTBOX-02

must_haves:
  truths:
    - "spring.payment.async-strategy=outbox 설정 시 OutboxConfirmAdapter가 활성화되고 202를 반환한다"
    - "재고 감소와 payment_outbox PENDING 레코드 저장이 같은 트랜잭션으로 원자적으로 실행된다"
    - "PaymentOutbox 도메인 엔티티가 PENDING/IN_FLIGHT/DONE/FAILED 상태 전이를 강제하고 잘못된 전이 시 예외를 던진다"
    - "OutboxConfirmAdapter가 confirm() 내에서 paymentCommandUseCase.executePayment()를 호출해 PaymentEvent에 paymentKey를 기록한다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/domain/PaymentOutbox.java"
      provides: "Outbox 도메인 엔티티 — createPending(), toInFlight(), toDone(), toFailed(), isRetryable(), incrementRetryCount()"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/port/PaymentOutboxRepository.java"
      provides: "Outbox 포트 인터페이스 — save, findByOrderId, findPendingBatch, findTimedOutInFlight, existsByOrderId"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapter.java"
      provides: "PaymentConfirmService 구현체 (@ConditionalOnProperty havingValue=outbox)"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java"
      provides: "executeStockDecreaseWithOutboxCreation() 신규 메서드 추가"
  key_links:
    - from: "OutboxConfirmAdapter.confirm()"
      to: "PaymentCommandUseCase.executePayment()"
      via: "paymentCommandUseCase 주입 — paymentKey를 PaymentEvent에 기록"
      pattern: "executePayment"
    - from: "OutboxConfirmAdapter.confirm()"
      to: "PaymentTransactionCoordinator.executeStockDecreaseWithOutboxCreation()"
      via: "transactionCoordinator 주입 — executePayment() 호출 후 실행"
      pattern: "executeStockDecreaseWithOutboxCreation"
    - from: "PaymentTransactionCoordinator.executeStockDecreaseWithOutboxCreation()"
      to: "PaymentOutboxRepository.save()"
      via: "PaymentOutboxUseCase.createPendingRecord() — Plan 02에서 구현, Plan 01에서는 스텁 시그니처만 정의"
      pattern: "createPendingRecord"
---

<objective>
Outbox 도메인 계층과 어댑터를 구축한다.

Purpose: DB Outbox 패턴의 핵심 계약(도메인 엔티티, 포트 인터페이스, JPA 인프라, 어댑터)을 정의해 Plan 02의 UseCase/Worker 구현이 명확한 타입 계약 위에서 실행될 수 있게 한다.
Output: PaymentOutbox 도메인 엔티티, PaymentOutboxRepository 포트, JPA 엔티티/레포지토리, OutboxConfirmAdapter, PaymentTransactionCoordinator 신규 메서드 시그니처
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-outbox-adapter/03-CONTEXT.md
@.planning/phases/03-db-outbox-adapter/03-RESEARCH.md

<interfaces>
<!-- 실행자가 참조해야 하는 기존 계약. 탐색 불필요. -->

From src/main/java/com/hyoguoo/paymentplatform/payment/domain/PaymentProcess.java:
```java
// Outbox 도메인 엔티티 설계의 직접 참조 패턴
@Builder(builderMethodName = "allArgsBuilder", buildMethodName = "allArgsBuild")
@AllArgsConstructor(access = AccessLevel.PRIVATE)
public class PaymentProcess {
    public static PaymentProcess createProcessing(String orderId) { ... }
    public void complete(LocalDateTime completedAt) { ... }  // 멱등 (COMPLETED이면 no-op)
    public void fail(LocalDateTime failedAt, String failureReason) { ... }  // 멱등 (FAILED이면 no-op)
}
```

From src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/entity/PaymentProcessEntity.java:
```java
// JPA 엔티티 패턴 참조
@Entity @Table(name = "payment_process")
@Builder @NoArgsConstructor(access = AccessLevel.PROTECTED) @AllArgsConstructor(access = AccessLevel.PRIVATE)
public class PaymentProcessEntity extends BaseEntity {
    public static PaymentProcessEntity from(PaymentProcess paymentProcess) { ... }
    public PaymentProcess toDomain() { ... }
}
```

From src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapter.java:
```java
// OutboxConfirmAdapter가 따라야 할 정확한 패턴
@ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "sync", matchIfMissing = true)
public class SyncConfirmAdapter implements PaymentConfirmService {
    public PaymentConfirmAsyncResult confirm(PaymentConfirmCommand command) {
        return PaymentConfirmAsyncResult.builder()
                .responseType(ResponseType.SYNC_200)  // OutboxAdapter는 ASYNC_202 사용
                .orderId(...).amount(...).build();
    }
}
```

From src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java:
```java
// 신규 메서드가 추가될 클래스. 기존 메서드는 변경하지 않는다.
@Transactional(rollbackFor = PaymentOrderedProductStockException.class)
public PaymentProcess executeStockDecreaseWithJobCreation(String orderId, List<PaymentOrder> paymentOrderList) { ... }

@Transactional
public PaymentEvent executePaymentSuccessCompletion(...) { ... }

@Transactional
public PaymentEvent executePaymentFailureCompensation(...) { ... }
```

From src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentCommandUseCase.java:
```java
// OutboxConfirmAdapter가 confirm() 내에서 호출해야 하는 메서드
// PaymentEvent READY → IN_PROGRESS 전환 + paymentKey를 PaymentEvent에 기록
public PaymentEvent executePayment(PaymentEvent paymentEvent, String paymentKey);
```

From src/main/java/com/hyoguoo/paymentplatform/payment/exception/common/PaymentErrorCode.java:
```java
// 신규 에러 코드를 추가해야 함
INVALID_STATUS_TO_COMPLETE("E03018", ...),
INVENTORY_JOB_NOT_FOUND("E03019", ...),
PAYMENT_PROCESS_NOT_FOUND("E03020", ...),
// 다음 가용 코드: E03022부터 사용 (E03021이 마지막)
```

DDL (application-test.yml이 ddl-auto: create-drop이므로 JPA 엔티티에서 자동 생성됨):
```sql
-- PaymentOutboxEntity에서 생성될 테이블 구조
CREATE TABLE payment_outbox (
    id           BIGINT       NOT NULL AUTO_INCREMENT,
    order_id     VARCHAR(100) NOT NULL UNIQUE,
    status       VARCHAR(20)  NOT NULL,
    retry_count  INT          NOT NULL DEFAULT 0,
    in_flight_at DATETIME(6),
    created_at   DATETIME(6)  NOT NULL,
    updated_at   DATETIME(6)  NOT NULL,
    PRIMARY KEY (id),
    INDEX idx_payment_outbox_status_created (status, created_at)
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentOutbox 도메인 엔티티 + 포트 인터페이스 (RED → GREEN)</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/domain/enums/PaymentOutboxStatus.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/domain/PaymentOutbox.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/application/port/PaymentOutboxRepository.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/exception/common/PaymentErrorCode.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/domain/PaymentOutboxTest.java
  </files>
  <behavior>
    - Test 1: PaymentOutbox.createPending("order-1")은 status=PENDING, retryCount=0, inFlightAt=null을 가진다
    - Test 2 (@ParameterizedTest @EnumSource PENDING만): toInFlight(now) 호출 시 status=IN_FLIGHT, inFlightAt=now로 전환된다
    - Test 3 (@ParameterizedTest @EnumSource IN_FLIGHT, DONE, FAILED): toInFlight() 호출 시 PaymentStatusException 발생
    - Test 4: isRetryable() — retryCount=4이면 true, retryCount=5이면 false (RETRYABLE_LIMIT=5)
    - Test 5: incrementRetryCount() 호출 시 retryCount+1, status=PENDING으로 복귀 (재시도를 위한 복귀)
    - Test 6: toDone() 호출 후 status=DONE, toFailed() 호출 후 status=FAILED
  </behavior>
  <action>
    1. PaymentOutboxStatus enum 생성: PENDING, IN_FLIGHT, DONE, FAILED (각각 String value 필드, @RequiredArgsConstructor)

    2. PaymentOutbox 도메인 엔티티 생성 (PaymentProcess 패턴 참조):
       - @Getter, @Builder(builderMethodName="allArgsBuilder", buildMethodName="allArgsBuild"), @AllArgsConstructor(access=PRIVATE)
       - 필드: Long id, String orderId, PaymentOutboxStatus status, int retryCount, LocalDateTime inFlightAt, LocalDateTime createdAt, LocalDateTime updatedAt
       - public static final int RETRYABLE_LIMIT = 5
       - static factory: createPending(String orderId) — status=PENDING, retryCount=0
       - toInFlight(LocalDateTime inFlightAt): status != PENDING이면 PaymentStatusException.of(INVALID_STATUS_TO_IN_FLIGHT) 발생; 아니면 status=IN_FLIGHT, this.inFlightAt=inFlightAt
       - toDone(): this.status = DONE
       - toFailed(): this.status = FAILED
       - isRetryable(): retryCount < RETRYABLE_LIMIT
       - incrementRetryCount(): retryCount++; status=PENDING (재시도 대기로 복귀)

    3. PaymentErrorCode에 새 코드 추가:
       - INVALID_STATUS_TO_IN_FLIGHT("E03022", "IN_FLIGHT 상태로 변경할 수 없는 상태입니다.")
       - PAYMENT_OUTBOX_NOT_FOUND("E03023", "존재하지 않는 Outbox 레코드입니다.")

    4. PaymentOutboxRepository 포트 인터페이스 생성:
       ```java
       public interface PaymentOutboxRepository {
           PaymentOutbox save(PaymentOutbox paymentOutbox);
           Optional<PaymentOutbox> findByOrderId(String orderId);
           List<PaymentOutbox> findPendingBatch(int limit);       // FIFO, createdAt ASC
           List<PaymentOutbox> findTimedOutInFlight(LocalDateTime before); // in_flight_at < before AND status = IN_FLIGHT
           boolean existsByOrderId(String orderId);
       }
       ```

    5. PaymentOutboxTest 작성 — 위 behavior 케이스를 @ParameterizedTest/@EnumSource로 커버. @DisplayName에 한글 설명.

    6. 테스트 먼저 실행(RED): `./gradlew test --tests "*.PaymentOutboxTest"` → 컴파일 오류 또는 실패 확인
    7. 구현 작성 후 GREEN 확인
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentOutboxTest"</automated>
  </verify>
  <done>PaymentOutboxTest 전체 통과, PaymentOutboxStatus/PaymentOutbox/PaymentOutboxRepository 파일 존재</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JPA 인프라 + executeStockDecreaseWithOutboxCreation() 스텁 + OutboxConfirmAdapter (RED → GREEN)</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/entity/PaymentOutboxEntity.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/repository/JpaPaymentOutboxRepository.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/repository/PaymentOutboxRepositoryImpl.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/PaymentInfrastructureMapper.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinator.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapter.java,
    src/main/resources/application.yml,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapterTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentTransactionCoordinatorTest.java
  </files>
  <behavior>
    OutboxConfirmAdapterTest:
    - Test 1: confirm() 호출 시 paymentCommandUseCase.executePayment()를 1회 호출한다 (paymentKey 기록 필수)
    - Test 2: confirm() 호출 시 transactionCoordinator.executeStockDecreaseWithOutboxCreation()를 1회 호출한다
    - Test 3: confirm() 결과의 responseType은 ASYNC_202, orderId와 amount가 command와 동일하다
    - Test 4: @ConditionalOnProperty(name="spring.payment.async-strategy", havingValue="outbox", matchIfMissing=false) 어노테이션 검증 (reflection)

    PaymentTransactionCoordinatorTest (신규 nested class 추가 — executeStockDecreaseWithOutboxCreation):
    - Test 5: executeStockDecreaseWithOutboxCreation() 호출 시 orderedProductUseCase.decreaseStockForOrders()와 paymentOutboxUseCase.createPendingRecord()를 각 1회 호출한다
    - Test 6: 재고 부족 예외 시 createPendingRecord()가 호출되지 않는다 (rollbackFor=PaymentOrderedProductStockException)

    주의: PaymentTransactionCoordinatorTest의 신규 케이스(Test 5, 6)는 이 Plan의 verify에서 실행하지 않는다.
    PaymentOutboxUseCase 클래스가 Plan 02 이전까지 존재하지 않으므로 PaymentTransactionCoordinator가 컴파일되지 않는다.
    PaymentTransactionCoordinatorTest 신규 케이스는 Plan 02 verify(`./gradlew test --tests "*.PaymentTransactionCoordinatorTest"`)에서 실행된다.
  </behavior>
  <action>
    1. PaymentOutboxEntity 생성 (PaymentProcessEntity 패턴 참조):
       - @Entity, @Table(name = "payment_outbox")
       - @Builder, @NoArgsConstructor(access=PROTECTED), @AllArgsConstructor(access=PRIVATE)
       - extends BaseEntity (createdAt/updatedAt 자동 관리)
       - 필드: @Id @GeneratedValue Long id, @Column(unique=true) String orderId, @Enumerated(STRING) PaymentOutboxStatus status, int retryCount, LocalDateTime inFlightAt
       - static PaymentOutboxEntity from(PaymentOutbox): builder로 변환
       - PaymentOutbox toDomain(): allArgsBuilder로 도메인 객체 반환

    2. JpaPaymentOutboxRepository (Spring Data JPA interface):
       ```java
       public interface JpaPaymentOutboxRepository extends JpaRepository<PaymentOutboxEntity, Long> {
           Optional<PaymentOutboxEntity> findByOrderId(String orderId);
           boolean existsByOrderId(String orderId);
           // PENDING 배치 조회: JPQL 사용
           @Query("SELECT e FROM PaymentOutboxEntity e WHERE e.status = 'PENDING' ORDER BY e.createdAt ASC")
           List<PaymentOutboxEntity> findPendingBatch(Pageable pageable);
           // IN_FLIGHT 타임아웃 복구
           @Query("SELECT e FROM PaymentOutboxEntity e WHERE e.status = 'IN_FLIGHT' AND e.inFlightAt < :before")
           List<PaymentOutboxEntity> findTimedOutInFlight(@Param("before") LocalDateTime before);
       }
       ```

    3. PaymentOutboxRepositoryImpl (PaymentOutboxRepository 구현):
       - @Repository, @RequiredArgsConstructor
       - save(): entity.from(domain) → jpaRepo.save() → toDomain()
       - findByOrderId(): jpaRepo.findByOrderId() → map toDomain()
       - findPendingBatch(int limit): PageRequest.of(0, limit, Sort.unsorted())으로 JPQL 호출
       - findTimedOutInFlight(LocalDateTime before): JPQL 결과 stream().map(toDomain).toList()
       - existsByOrderId(): jpaRepo.existsByOrderId()

    4. PaymentInfrastructureMapper에 toPaymentOutbox(entity) 변환 메서드 추가 (이미 PaymentProcess 변환 패턴 있음 — 같은 파일에 추가)

    5. PaymentOutboxUseCase 스텁 생성 (컴파일 통과 목적):
       PaymentTransactionCoordinator가 PaymentOutboxUseCase를 참조하므로, Plan 02에서 완성되기 전에 컴파일이 통과되려면
       최소 시그니처를 담은 스텁 클래스가 필요하다.
       src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentOutboxUseCase.java를 생성하고,
       createPendingRecord(String orderId) 메서드 시그니처만 선언한다 (throws UnsupportedOperationException 구현).
       Plan 02에서 이 파일을 완성한다.
       ```java
       @Service
       @RequiredArgsConstructor
       public class PaymentOutboxUseCase {
           private final PaymentOutboxRepository paymentOutboxRepository;

           @Transactional
           public PaymentOutbox createPendingRecord(String orderId) {
               throw new UnsupportedOperationException("Plan 02에서 구현");
           }
       }
       ```
       이 스텁은 files_modified에 포함되어 있으며, Plan 02 Task 1에서 전체 구현으로 대체된다.

    6. PaymentTransactionCoordinator에 신규 메서드 추가 (기존 메서드는 변경하지 않음):
       ```java
       // 신규 필드 추가
       private final PaymentOutboxUseCase paymentOutboxUseCase;

       @Transactional(rollbackFor = PaymentOrderedProductStockException.class)
       public PaymentOutbox executeStockDecreaseWithOutboxCreation(
               String orderId,
               List<PaymentOrder> paymentOrderList
       ) throws PaymentOrderedProductStockException {
           orderedProductUseCase.decreaseStockForOrders(paymentOrderList);
           return paymentOutboxUseCase.createPendingRecord(orderId);
       }
       ```

    7. OutboxConfirmAdapter 생성 (SyncConfirmAdapter 패턴 참조):
       PaymentCommandUseCase 의존성을 반드시 추가하고, confirm() 내에서
       executeStockDecreaseWithOutboxCreation() 호출 전에 executePayment()를 먼저 호출한다.
       이렇게 해야 워커가 paymentEvent.getPaymentKey()를 조회할 때 null이 아니며,
       PaymentEvent가 READY 상태 그대로 남아 워커의 toDone() 호출 시 상태 전이 예외가 발생하지 않는다.
       ```java
       @Slf4j @Service @RequiredArgsConstructor
       @ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "outbox")
       public class OutboxConfirmAdapter implements PaymentConfirmService {
           private final PaymentTransactionCoordinator transactionCoordinator;
           private final PaymentLoadUseCase paymentLoadUseCase;
           private final PaymentCommandUseCase paymentCommandUseCase;  // paymentKey 기록 필수

           @Override
           public PaymentConfirmAsyncResult confirm(PaymentConfirmCommand command) {
               PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(command.getOrderId());

               // PaymentEvent READY → IN_PROGRESS 전환 + paymentKey 기록
               // 워커가 나중에 paymentEvent.getPaymentKey()를 조회하기 위해 반드시 선행 호출
               paymentCommandUseCase.executePayment(paymentEvent, command.getPaymentKey());

               transactionCoordinator.executeStockDecreaseWithOutboxCreation(
                       command.getOrderId(), paymentEvent.getPaymentOrderList()
               );
               return PaymentConfirmAsyncResult.builder()
                       .responseType(ResponseType.ASYNC_202)
                       .orderId(command.getOrderId())
                       .amount(command.getAmount())
                       .build();
           }
       }
       ```

    8. application.yml에 outbox worker 기본 설정 추가:
       ```yaml
       scheduler:
         outbox-worker:
           fixed-delay-ms: 1000
           batch-size: 10
           parallel-enabled: false
           in-flight-timeout-minutes: 5
       ```

    9. OutboxConfirmAdapterTest 작성 (위 behavior Test 1~4):
       - executePayment() 1회 호출 검증을 반드시 포함한다 (Test 1)
       - executeStockDecreaseWithOutboxCreation() 1회 호출 검증 (Test 2)
       - ASYNC_202 반환 검증 (Test 3)
       - @ConditionalOnProperty reflection 검증 (Test 4)

    10. PaymentTransactionCoordinatorTest에 신규 nested class 추가 (위 behavior Test 5, 6):
        이 테스트는 PaymentOutboxUseCase 스텁 존재로 컴파일은 통과하지만,
        스텁이 UnsupportedOperationException을 던지므로 Plan 02 이전까지는 실행에 실패할 수 있다.
        따라서 이 Plan의 verify 커맨드에는 포함하지 않는다.
        Plan 02 verify에서 `./gradlew test --tests "*.PaymentTransactionCoordinatorTest"`로 실행한다.

    11. RED 확인: `./gradlew test --tests "*.OutboxConfirmAdapterTest"` → GREEN 확인
  </action>
  <verify>
    <automated>./gradlew test --tests "*.OutboxConfirmAdapterTest"</automated>
  </verify>
  <done>OutboxConfirmAdapterTest 4개 케이스 GREEN (executePayment() 1회 호출 포함), OutboxConfirmAdapter 파일 존재, PaymentTransactionCoordinator에 executeStockDecreaseWithOutboxCreation() 추가 확인</done>
</task>

</tasks>

<verification>
- `./gradlew test` 전체 통과 (기존 테스트 회귀 없음)
- PaymentOutbox 도메인 엔티티에 INVALID_STATUS_TO_IN_FLIGHT 예외 전이 포함
- OutboxConfirmAdapter에 @ConditionalOnProperty(havingValue="outbox", matchIfMissing 없음) 확인
- OutboxConfirmAdapter.confirm()에서 executePayment() 호출이 executeStockDecreaseWithOutboxCreation() 보다 먼저 실행됨 확인
- PaymentTransactionCoordinator 기존 executeStockDecreaseWithJobCreation() 미변경 확인
</verification>

<success_criteria>
- PaymentOutboxTest: 도메인 상태 전이 6개 케이스 전체 GREEN
- OutboxConfirmAdapterTest: executePayment() 호출 + confirm() 위임 + ASYNC_202 + @ConditionalOnProperty 4개 케이스 GREEN
- PaymentTransactionCoordinatorTest 신규 케이스(executeStockDecreaseWithOutboxCreation)는 Plan 02 verify에서 GREEN
- `./gradlew test` 전체 GREEN (기존 포함)
</success_criteria>

<output>
완료 후 `.planning/phases/03-db-outbox-adapter/03-01-SUMMARY.md` 작성
</output>
