---
phase: 03-db-outbox-adapter
plan: "02"
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentOutboxUseCase.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/scheduler/OutboxWorker.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentOutboxUseCaseTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/scheduler/OutboxWorkerTest.java
autonomous: true
requirements:
  - OUTBOX-03
  - OUTBOX-04
  - OUTBOX-05
  - OUTBOX-06

must_haves:
  truths:
    - "@Scheduled(fixedDelayString) OutboxWorker가 PENDING 레코드를 배치 조회해 Toss API를 호출하고 상태를 업데이트한다"
    - "워커가 처리 시작 시 레코드를 IN_FLIGHT로 전환하므로 동일 레코드가 중복 처리되지 않는다"
    - "retryCount가 RETRYABLE_LIMIT(5)에 도달하면 레코드가 FAILED로 전환되고 보상 트랜잭션이 실행된다"
    - "markDone()/markFailed()는 멱등하다 — 이미 해당 상태이면 no-op"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentOutboxUseCase.java"
      provides: "createPendingRecord, claimToInFlight, markDone, markFailed, incrementRetryOrFail, recoverTimedOutInFlightRecords, findPendingBatch, findActiveOutboxStatus"
      exports: ["createPendingRecord", "claimToInFlight", "markDone", "markFailed", "findActiveOutboxStatus"]
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/scheduler/OutboxWorker.java"
      provides: "@Scheduled(fixedDelayString) outbox 처리 — IN_FLIGHT 전환 → Toss API → DONE/FAILED"
  key_links:
    - from: "OutboxWorker.processRecord()"
      to: "PaymentOutboxUseCase.claimToInFlight()"
      via: "별도 트랜잭션 (REQUIRES_NEW) — 즉시 커밋"
      pattern: "claimToInFlight"
    - from: "OutboxWorker.processRecord()"
      to: "PaymentCommandUseCase.confirmPaymentWithGateway()"
      via: "트랜잭션 밖 HTTP 호출"
      pattern: "confirmPaymentWithGateway"
    - from: "OutboxWorker.processRecord()"
      to: "PaymentTransactionCoordinator.executePaymentSuccessCompletion()"
      via: "Toss API 성공 시 별도 트랜잭션"
      pattern: "executePaymentSuccessCompletion"
---

<objective>
OutboxUseCase와 OutboxWorker를 구현한다.

Purpose: Plan 01에서 정의한 도메인/포트 위에 스케줄러 기반 비동기 처리 로직을 완성한다. 3단계 트랜잭션 분리(IN_FLIGHT 전환 → Toss API → 결과 저장), 재시도/실패 처리, 가상 스레드 병렬 모드를 구현한다.
Output: PaymentOutboxUseCase (Plan 01 스텁 대체), OutboxWorker (@Scheduled), 전체 6개 요구사항 만족
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-db-outbox-adapter/03-CONTEXT.md
@.planning/phases/03-db-outbox-adapter/03-RESEARCH.md
@.planning/phases/03-db-outbox-adapter/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 계약. 실행자는 이 타입들을 그대로 사용한다. -->

From PaymentOutbox (생성된 도메인 엔티티):
```java
public class PaymentOutbox {
    public static final int RETRYABLE_LIMIT = 5;
    public Long getId(); public String getOrderId();
    public PaymentOutboxStatus getStatus(); public int getRetryCount();
    public LocalDateTime getInFlightAt();

    public static PaymentOutbox createPending(String orderId);
    public void toInFlight(LocalDateTime inFlightAt);  // PENDING만 허용, 아니면 PaymentStatusException
    public void toDone();
    public void toFailed();
    public boolean isRetryable();        // retryCount < RETRYABLE_LIMIT
    public void incrementRetryCount();   // retryCount++, status=PENDING
}
```

From PaymentOutboxRepository (생성된 포트):
```java
public interface PaymentOutboxRepository {
    PaymentOutbox save(PaymentOutbox paymentOutbox);
    Optional<PaymentOutbox> findByOrderId(String orderId);
    List<PaymentOutbox> findPendingBatch(int limit);
    List<PaymentOutbox> findTimedOutInFlight(LocalDateTime before);
    boolean existsByOrderId(String orderId);
}
```

From PaymentTransactionCoordinator (기존 + 신규 메서드):
```java
public PaymentOutbox executeStockDecreaseWithOutboxCreation(String orderId, List<PaymentOrder> paymentOrderList);
public PaymentEvent executePaymentSuccessCompletion(String orderId, PaymentEvent paymentEvent, LocalDateTime approvedAt);
public PaymentEvent executePaymentFailureCompensation(String orderId, PaymentEvent paymentEvent, List<PaymentOrder> paymentOrderList, String failureReason);
```

From PaymentCommandUseCase:
```java
// Toss API 호출 — 트랜잭션 밖에서 실행
public PaymentGatewayInfo confirmPaymentWithGateway(PaymentConfirmCommand command)
        throws PaymentTossRetryableException, PaymentTossNonRetryableException;

// PaymentEvent READY → IN_PROGRESS 전환 — OutboxConfirmAdapter에서 이미 호출됨 (Plan 01)
// 워커에서는 호출하지 않는다 — paymentKey는 confirm 요청 시 이미 기록됨
public PaymentEvent executePayment(PaymentEvent paymentEvent, String paymentKey);
```

From PaymentLoadUseCase:
```java
public PaymentEvent getPaymentEventByOrderId(String orderId);
```

From PaymentGatewayInfo (Toss API 성공 결과):
```java
public PaymentDetails getPaymentDetails();  // getApprovedAt() 포함
```

From PaymentConfirmCommand:
```java
PaymentConfirmCommand.builder()
    .userId(paymentEvent.getBuyerId())
    .orderId(orderId)
    .paymentKey(paymentEvent.getPaymentKey())
    .amount(paymentEvent.getTotalAmount())
    .build();
```

From PaymentScheduler (fixedDelayString 패턴 참조):
```java
@Scheduled(fixedDelayString = "${scheduler.payment-recovery.interval-ms:60000}")
@ConditionalOnProperty(name = "scheduler.payment-recovery.enabled", havingValue = "true")
public void recoverStuckPayments() { ... }
```

Exception types:
```java
// Toss API 재시도 가능 오류
PaymentTossRetryableException.of(PaymentErrorCode.TOSS_RETRYABLE_ERROR)
// Toss API 재시도 불가 오류
PaymentTossNonRetryableException.of(PaymentErrorCode.TOSS_NON_RETRYABLE_ERROR)
```

Plan 01 스텁 주의:
// PaymentOutboxUseCase는 Plan 01에서 UnsupportedOperationException 스텁으로 생성됨.
// 이 Plan에서 전체 구현으로 교체한다. 파일 상단의 스텁 구현을 삭제하고 완전한 구현으로 대체한다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentOutboxUseCase — CRUD + 상태 전이 + 멱등 처리 (RED → GREEN)</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentOutboxUseCase.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/usecase/PaymentOutboxUseCaseTest.java
  </files>
  <behavior>
    - Test 1 (createPendingRecord): save()를 1회 호출하고 PENDING 상태 PaymentOutbox를 반환한다
    - Test 2 (claimToInFlight - 성공): findByOrderId()가 PENDING 레코드를 반환하면 toInFlight() 후 save() 호출, true 반환
    - Test 3 (claimToInFlight - 이미 IN_FLIGHT): findByOrderId()가 IN_FLIGHT 레코드를 반환하면 save() 호출하지 않고 false 반환 (toInFlight()가 PaymentStatusException 발생 → catch)
    - Test 4 (markDone - 멱등): 이미 DONE 상태이면 save() 호출하지 않는다
    - Test 5 (markDone - 정상): IN_FLIGHT 상태이면 toDone() 후 save() 1회 호출
    - Test 6 (markFailed - 멱등): 이미 FAILED 상태이면 save() 호출하지 않는다
    - Test 7 (incrementRetryOrFail - retryable): retryCount=3이면 incrementRetryCount() 후 save() 호출 (OUTBOX-05)
    - Test 8 (incrementRetryOrFail - limit exceeded): retryCount=5이면 markFailed() 호출 (OUTBOX-05)
    - Test 9 (recoverTimedOutInFlightRecords): findTimedOutInFlight() 결과 각각 incrementRetryCount() + save() 호출 (OUTBOX-06)
    - Test 10 (findActiveOutboxStatus - PENDING): PENDING 레코드 존재 시 Optional.of(PENDING) 반환
    - Test 11 (findActiveOutboxStatus - DONE): DONE 레코드 존재 시 Optional.empty() 반환 (DONE/FAILED는 PaymentEvent fallback)
  </behavior>
  <action>
    Plan 01에서 생성된 PaymentOutboxUseCase 스텁 파일을 완전한 구현으로 교체한다.
    UnsupportedOperationException 구현을 삭제하고 아래 전체 구현으로 대체한다.

    PaymentOutboxUseCase 구현 (@Service @RequiredArgsConstructor):

    구현 전략:
    - claimToInFlight()의 파라미터는 outbox 객체(PaymentOutbox)로 받는다 (이미 findPendingBatch로 로드됨)
    - 내부에서 outbox.toInFlight()를 try-catch로 감싸고: 성공 시 save() + true, PaymentStatusException 발생 시 false 반환
    - save() 후 REQUIRES_NEW 트랜잭션이 즉시 커밋되어 다른 워커 인스턴스가 같은 레코드를 못 가져감

    전체 메서드 목록:
    ```java
    @Transactional public PaymentOutbox createPendingRecord(String orderId)
    @Transactional(propagation = REQUIRES_NEW) public boolean claimToInFlight(PaymentOutbox outbox)
    @Transactional public void markDone(String orderId)
    @Transactional public void markFailed(String orderId)
    @Transactional public void incrementRetryOrFail(String orderId, PaymentOutbox currentOutbox)
    @Transactional public void recoverTimedOutInFlightRecords(int timeoutMinutes)
    public List<PaymentOutbox> findPendingBatch(int batchSize)
    public Optional<PaymentOutboxStatus> findActiveOutboxStatus(String orderId)
    ```

    markDone / markFailed: findByOrderId() 후 상태 확인 → 이미 동일 상태면 return (멱등)
    findActiveOutboxStatus: PENDING/IN_FLIGHT만 반환, DONE/FAILED는 Optional.empty()

    PaymentOutboxUseCaseTest:
    - Mockito.mock(PaymentOutboxRepository.class)
    - Mockito.mock(LocalDateTimeProvider.class)
    - 위 11개 behavior 케이스 작성, @DisplayName 한글

    이 Task 완료 후 PaymentTransactionCoordinator도 컴파일 완료된다 (PaymentOutboxUseCase 완전 구현).
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentOutboxUseCaseTest" --tests "*.PaymentTransactionCoordinatorTest"</automated>
  </verify>
  <done>PaymentOutboxUseCaseTest 11개 케이스 GREEN, PaymentTransactionCoordinatorTest 신규 케이스(executeStockDecreaseWithOutboxCreation) 2개 GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OutboxWorker — 3단계 트랜잭션 + 재시도 + 가상 스레드 병렬 모드 (RED → GREEN)</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/scheduler/OutboxWorker.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/scheduler/OutboxWorkerTest.java
  </files>
  <behavior>
    - Test 1 (process - PENDING 없음): findPendingBatch() 결과가 비면 Toss API 호출하지 않는다
    - Test 2 (process - 정상 흐름): PENDING 레코드 1건 → claimToInFlight() → confirmPaymentWithGateway() → executePaymentSuccessCompletion() → markDone() 순서로 1회씩 호출 (OUTBOX-03, OUTBOX-04)
      주의: OutboxConfirmAdapter에서 이미 executePayment()를 호출해 paymentKey를 기록했으므로 워커에서는 executePayment()를 호출하지 않는다.
    - Test 3 (process - claimToInFlight false 반환): claimToInFlight()가 false를 반환하면 Toss API를 호출하지 않는다 (OUTBOX-04)
    - Test 4 (process - PaymentTossRetryableException): Toss API가 RETRYABLE 예외 발생 시 incrementRetryOrFail() 호출, executePaymentFailureCompensation() 호출 안 함 (OUTBOX-05)
    - Test 5 (process - PaymentTossNonRetryableException): Toss API가 NON_RETRYABLE 예외 발생 시 executePaymentFailureCompensation() + markFailed() 호출 (OUTBOX-05, OUTBOX-06)
    - Test 6 (process - IN_FLIGHT 타임아웃 복구): process() 시작 시 recoverTimedOutInFlightRecords() 1회 호출 (OUTBOX-04)
  </behavior>
  <action>
    OutboxWorker 구현 (@Slf4j @Component @RequiredArgsConstructor):

    ```java
    @Slf4j @Component @RequiredArgsConstructor
    public class OutboxWorker {
        private final PaymentOutboxUseCase paymentOutboxUseCase;
        private final PaymentLoadUseCase paymentLoadUseCase;
        private final PaymentCommandUseCase paymentCommandUseCase;
        private final PaymentTransactionCoordinator transactionCoordinator;

        @Value("${scheduler.outbox-worker.batch-size:10}") private int batchSize;
        @Value("${scheduler.outbox-worker.parallel-enabled:false}") private boolean parallelEnabled;
        @Value("${scheduler.outbox-worker.in-flight-timeout-minutes:5}") private int inFlightTimeoutMinutes;

        @Scheduled(fixedDelayString = "${scheduler.outbox-worker.fixed-delay-ms:1000}")
        public void process() {
            // Step 0: IN_FLIGHT 타임아웃 복구
            paymentOutboxUseCase.recoverTimedOutInFlightRecords(inFlightTimeoutMinutes);

            // Step 1: PENDING 배치 조회
            List<PaymentOutbox> pending = paymentOutboxUseCase.findPendingBatch(batchSize);
            if (pending.isEmpty()) return;

            if (parallelEnabled) {
                processParallel(pending);
            } else {
                pending.forEach(this::processRecord);
            }
        }

        private void processParallel(List<PaymentOutbox> records) {
            // Java 21 가상 스레드: try-with-resources로 자동 종료 (awaitTermination 불필요)
            try (ExecutorService executor = Executors.newVirtualThreadPerTaskExecutor()) {
                records.forEach(record -> executor.submit(() -> processRecord(record)));
            }
        }

        private void processRecord(PaymentOutbox outbox) {
            // Step 2: PENDING → IN_FLIGHT (REQUIRES_NEW 트랜잭션, 즉시 커밋)
            boolean claimed = paymentOutboxUseCase.claimToInFlight(outbox);
            if (!claimed) return;

            String orderId = outbox.getOrderId();
            PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(orderId);
            // paymentKey는 OutboxConfirmAdapter에서 이미 executePayment()로 기록됨
            // 워커에서 executePayment() 재호출 불필요 — paymentEvent.getPaymentKey()로 직접 조회

            try {
                // Step 3: Toss API 호출 (트랜잭션 밖)
                PaymentConfirmCommand command = PaymentConfirmCommand.builder()
                        .userId(paymentEvent.getBuyerId())
                        .orderId(orderId)
                        .paymentKey(paymentEvent.getPaymentKey())
                        .amount(paymentEvent.getTotalAmount())
                        .build();
                PaymentGatewayInfo gatewayInfo = paymentCommandUseCase.confirmPaymentWithGateway(command);

                // Step 4-A: 성공 처리 (별도 트랜잭션)
                transactionCoordinator.executePaymentSuccessCompletion(
                        orderId, paymentEvent, gatewayInfo.getPaymentDetails().getApprovedAt());
                paymentOutboxUseCase.markDone(orderId);

            } catch (PaymentTossNonRetryableException e) {
                // Step 4-B: 비재시도 실패 — 보상 트랜잭션 + FAILED
                LogFmt.error(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
                transactionCoordinator.executePaymentFailureCompensation(
                        orderId, paymentEvent, paymentEvent.getPaymentOrderList(), e.getMessage());
                paymentOutboxUseCase.markFailed(orderId);

            } catch (PaymentTossRetryableException e) {
                // Step 4-C: 재시도 가능 — retryCount 증가 또는 FAILED
                LogFmt.warn(log, LogDomain.PAYMENT, EventType.EXCEPTION, e::getMessage);
                paymentOutboxUseCase.incrementRetryOrFail(orderId, outbox);
            }
        }
    }
    ```

    주의사항:
    - LogFmt.error/warn에 LogDomain.PAYMENT, EventType.EXCEPTION 사용 (기존 패턴)
    - PaymentEvent.getBuyerId()와 getTotalAmount() 필드가 실제 존재하는지 PaymentEvent.java에서 확인 후 올바른 getter 사용
    - 워커에서 executePayment()를 호출하지 않는다 — OutboxConfirmAdapter.confirm()에서 이미 호출했으므로 paymentEvent.getPaymentKey()가 null이 아님

    OutboxWorkerTest:
    - Mockito.mock(PaymentOutboxUseCase.class), mock(PaymentLoadUseCase.class), mock(PaymentCommandUseCase.class), mock(PaymentTransactionCoordinator.class)
    - 위 6개 behavior 케이스
    - @DisplayName 한글, given-when-then 구조
  </action>
  <verify>
    <automated>./gradlew test --tests "*.OutboxWorkerTest" --tests "*.PaymentOutboxUseCaseTest"</automated>
  </verify>
  <done>OutboxWorkerTest 6개 케이스 + PaymentOutboxUseCaseTest 11개 케이스 GREEN</done>
</task>

</tasks>

<verification>
- `./gradlew test` 전체 통과 (기존 테스트 회귀 없음)
- OutboxWorker에 @Scheduled(fixedDelayString) 확인 (fixedRateString 아님)
- claimToInFlight()에 @Transactional(propagation=REQUIRES_NEW) 확인
- markDone()/markFailed()가 이미 해당 상태이면 save() 호출 없음 (멱등)
- Java 21 가상 스레드 try-with-resources 패턴 사용 확인
- OutboxWorker.processRecord()에서 executePayment() 호출 없음 확인 (OutboxConfirmAdapter에서 처리됨)
</verification>

<success_criteria>
- PaymentOutboxUseCaseTest: 11개 케이스 GREEN
- PaymentTransactionCoordinatorTest 신규 케이스: 2개 GREEN (Task 1 verify에서 확인)
- OutboxWorkerTest: 6개 케이스 GREEN
- `./gradlew test` 전체 GREEN
- OUTBOX-03: 워커가 PENDING 조회 → Toss API → 상태 업데이트 완료
- OUTBOX-04: IN_FLIGHT 전환 + fixedDelay 방식으로 중복 실행 방지
- OUTBOX-05: retry_count >= 5 시 FAILED 처리
- OUTBOX-06: markDone()/markFailed() 멱등
</success_criteria>

<output>
완료 후 `.planning/phases/03-db-outbox-adapter/03-02-SUMMARY.md` 작성
</output>
