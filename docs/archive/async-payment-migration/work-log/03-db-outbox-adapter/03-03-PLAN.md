---
phase: 03-db-outbox-adapter
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerMvcTest.java
autonomous: true
requirements:
  - OUTBOX-01

must_haves:
  truths:
    - "GET /api/v1/payments/{orderId}/status 호출 시 payment_outbox를 먼저 조회하고 PENDING/IN_FLIGHT이면 즉시 반환한다"
    - "payment_outbox에 PENDING 레코드가 있으면 status=PENDING을 반환한다"
    - "payment_outbox에 IN_FLIGHT 레코드가 있으면 status=PROCESSING을 반환한다"
    - "payment_outbox에 없거나 DONE/FAILED이면 기존 PaymentEvent 기반 조회 로직으로 fallback된다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java"
      provides: "getPaymentStatus()에 outbox 우선 조회 로직 추가"
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java"
      provides: "toPaymentStatusApiResponseFromOutbox(orderId, outboxStatus) 신규 메서드"
  key_links:
    - from: "PaymentController.getPaymentStatus()"
      to: "PaymentOutboxUseCase.findActiveOutboxStatus()"
      via: "outbox 우선 조회 — Optional 반환 시 즉시 응답"
      pattern: "findActiveOutboxStatus"
    - from: "PaymentController.getPaymentStatus()"
      to: "PaymentLoadUseCase.getPaymentEventByOrderId()"
      via: "outbox 없을 때 fallback"
      pattern: "getPaymentEventByOrderId"
---

<objective>
Status 엔드포인트에 Outbox 우선 조회 로직을 추가한다.

Purpose: 비동기 confirm 후 클라이언트가 GET /status로 폴링할 때 payment_outbox의 PENDING/IN_FLIGHT 상태를 올바르게 반환하게 한다.
Output: PaymentController.getPaymentStatus() outbox-first, PaymentPresentationMapper 확장, @WebMvcTest 케이스 추가
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-db-outbox-adapter/03-CONTEXT.md
@.planning/phases/03-db-outbox-adapter/03-01-SUMMARY.md
@.planning/phases/03-db-outbox-adapter/03-02-SUMMARY.md

<interfaces>
<!-- 기존 계약 + Plan 01/02에서 생성된 계약. -->

현재 PaymentController.getPaymentStatus() (변경 전):
```java
@GetMapping("/api/v1/payments/{orderId}/status")
public ResponseEntity<PaymentStatusApiResponse> getPaymentStatus(@PathVariable String orderId) {
    PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(orderId);
    return ResponseEntity.ok(PaymentPresentationMapper.toPaymentStatusApiResponse(paymentEvent));
}
```

변경 후 로직:
```java
// 1. outbox 우선 조회
Optional<PaymentOutboxStatus> outboxStatus = paymentOutboxUseCase.findActiveOutboxStatus(orderId);
if (outboxStatus.isPresent()) {
    return ResponseEntity.ok(PaymentPresentationMapper.toPaymentStatusApiResponseFromOutbox(orderId, outboxStatus.get()));
}
// 2. fallback: PaymentEvent 기반 기존 로직
PaymentEvent paymentEvent = paymentLoadUseCase.getPaymentEventByOrderId(orderId);
return ResponseEntity.ok(PaymentPresentationMapper.toPaymentStatusApiResponse(paymentEvent));
```

PaymentStatusResponse (기존 enum):
```java
public enum PaymentStatusResponse {
    PENDING, PROCESSING, DONE, FAILED
}
```

Outbox → PaymentStatusResponse 매핑 규칙 (CONTEXT.md):
- PaymentOutboxStatus.PENDING → PaymentStatusResponse.PENDING
- PaymentOutboxStatus.IN_FLIGHT → PaymentStatusResponse.PROCESSING
- PaymentOutboxStatus.DONE / FAILED → fallback (findActiveOutboxStatus가 이 경우 Optional.empty() 반환하므로 도달 안 함)

PaymentPresentationMapper 현재 패턴:
```java
public static PaymentStatusApiResponse toPaymentStatusApiResponse(PaymentEvent event) {
    return PaymentStatusApiResponse.builder()
            .orderId(event.getOrderId())
            .status(mapToPaymentStatusResponse(event.getStatus()))
            .approvedAt(event.getApprovedAt())
            .build();
}
```

신규 메서드:
```java
public static PaymentStatusApiResponse toPaymentStatusApiResponseFromOutbox(String orderId, PaymentOutboxStatus outboxStatus) {
    return PaymentStatusApiResponse.builder()
            .orderId(orderId)
            .status(mapOutboxStatusToPaymentStatusResponse(outboxStatus))
            .approvedAt(null)  // outbox 단계에서는 아직 approvedAt 없음
            .build();
}
```

PaymentControllerMvcTest (기존 @WebMvcTest 파일에 케이스 추가):
```java
// 기존 @WebMvcTest 설정 유지, PaymentOutboxUseCase mock 추가
@MockBean private PaymentOutboxUseCase paymentOutboxUseCase;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status 엔드포인트 outbox-first 로직 + @WebMvcTest 케이스 (RED → GREEN)</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentController.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentPresentationMapper.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/presentation/PaymentControllerMvcTest.java
  </files>
  <behavior>
    - Test 1: GET /api/v1/payments/{orderId}/status — outbox에 PENDING 레코드 있으면 status=PENDING 반환 (HTTP 200)
    - Test 2: GET /api/v1/payments/{orderId}/status — outbox에 IN_FLIGHT 레코드 있으면 status=PROCESSING 반환 (HTTP 200)
    - Test 3: GET /api/v1/payments/{orderId}/status — outbox에 레코드 없으면 PaymentEvent 기반 기존 로직 실행 (기존 테스트 유지)
    - Test 4: toPaymentStatusApiResponseFromOutbox() — PENDING 입력 시 status=PENDING, approvedAt=null
    - Test 5: toPaymentStatusApiResponseFromOutbox() — IN_FLIGHT 입력 시 status=PROCESSING, approvedAt=null
  </behavior>
  <action>
    1. PaymentController 변경:
       - 생성자 주입에 PaymentOutboxUseCase paymentOutboxUseCase 추가
       - getPaymentStatus() 메서드를 위의 outbox-first 로직으로 변경
       - PaymentLoadUseCase는 fallback에서 계속 사용

    2. PaymentPresentationMapper 변경:
       - toPaymentStatusApiResponseFromOutbox(String orderId, PaymentOutboxStatus outboxStatus) 신규 static 메서드 추가
       - private static mapOutboxStatusToPaymentStatusResponse(PaymentOutboxStatus status) 추가:
         - PENDING → PaymentStatusResponse.PENDING
         - IN_FLIGHT → PaymentStatusResponse.PROCESSING
         - 그 외(DONE/FAILED) → PaymentStatusResponse.PROCESSING (findActiveOutboxStatus가 필터링하므로 도달 안 하지만 방어적 처리)

    3. PaymentControllerMvcTest 변경 (기존 파일에 케이스 추가):
       - @MockBean PaymentOutboxUseCase paymentOutboxUseCase 추가
       - 기존 status 테스트에서 `when(paymentOutboxUseCase.findActiveOutboxStatus(any())).thenReturn(Optional.empty())` 스텁 추가 (기존 테스트 통과 유지)
       - 신규 테스트: PENDING 반환 케이스, IN_FLIGHT 반환 케이스 (MockMvc GET 요청 + JSON body status 검증)

    4. 테스트 먼저 RED 확인 후 GREEN
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentControllerMvcTest"</automated>
  </verify>
  <done>PaymentControllerMvcTest 전체 통과 (기존 STATUS 테스트 3개 + 신규 outbox 테스트 2개 이상)</done>
</task>

</tasks>

<verification>
- `./gradlew test` 전체 통과 (기존 테스트 회귀 없음)
- PaymentController에 PaymentOutboxUseCase 주입 확인
- PaymentPresentationMapper.toPaymentStatusApiResponseFromOutbox() 존재 확인
- PaymentControllerMvcTest에 MockBean PaymentOutboxUseCase 추가 확인
</verification>

<success_criteria>
- PaymentControllerMvcTest: 기존 케이스 유지 + outbox PENDING/IN_FLIGHT 케이스 GREEN
- `./gradlew test` 전체 GREEN
- OUTBOX-01 완전 충족: confirm → 202 + PENDING 저장, GET /status 폴링 → PENDING/PROCESSING → 최종 DONE
</success_criteria>

<output>
완료 후 `.planning/phases/03-db-outbox-adapter/03-03-SUMMARY.md` 작성
</output>
