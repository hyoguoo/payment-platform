---
phase: 08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmService.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmService.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmServiceTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmServiceTest.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "KafkaAsyncConfirmService가 PaymentConfirmService를 구현하고 ASYNC_202를 반환한다"
    - "OutboxAsyncConfirmService가 PaymentConfirmService를 구현하고 ASYNC_202를 반환한다"
    - "두 서비스 모두 @ConditionalOnProperty가 application 레이어에 선언된다"
    - "기존 KafkaConfirmAdapterTest/OutboxConfirmAdapterTest와 동일한 검증 내용이 새 테스트에서 통과한다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmService.java"
      provides: "Kafka 전략 오케스트레이션 서비스"
      exports: ["KafkaAsyncConfirmService"]
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmService.java"
      provides: "Outbox 전략 오케스트레이션 서비스"
      exports: ["OutboxAsyncConfirmService"]
  key_links:
    - from: "KafkaAsyncConfirmService"
      to: "PaymentConfirmPublisherPort"
      via: "confirmPublisher.publish(orderId)"
      pattern: "confirmPublisher\\.publish"
    - from: "OutboxAsyncConfirmService"
      to: "PaymentTransactionCoordinator"
      via: "transactionCoordinator.executeStockDecreaseWithOutboxCreation(orderId, orders)"
---

<objective>
KafkaConfirmAdapter와 OutboxConfirmAdapter의 오케스트레이션 로직을 application 레이어의
KafkaAsyncConfirmService와 OutboxAsyncConfirmService로 이동한다.
Wave 0 테스트 스텁을 완성해 두 서비스의 단위 테스트를 GREEN 상태로 만든다.

Purpose: infrastructure 레이어에 있던 오케스트레이션 로직을 올바른 레이어(application)로 이동.
         KafkaTemplate 직접 의존 없이 PaymentConfirmPublisherPort를 통해 발행.
Output: KafkaAsyncConfirmService.java, OutboxAsyncConfirmService.java + 단위 테스트 GREEN
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-CONTEXT.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-RESEARCH.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성한 포트 — KafkaAsyncConfirmService가 의존 -->
From src/main/java/.../payment/application/port/out/PaymentConfirmPublisherPort.java:
  public interface PaymentConfirmPublisherPort {
      void publish(String orderId);
  }

<!-- 기존 어댑터에서 추출한 오케스트레이션 순서 (그대로 이동) -->
KafkaConfirmAdapter 오케스트레이션:
  1. paymentLoadUseCase.getPaymentEventByOrderId(command.getOrderId())
  2. paymentCommandUseCase.executePayment(paymentEvent, command.getPaymentKey())
  3. transactionCoordinator.executeStockDecreaseOnly(command.getOrderId(), paymentEvent.getPaymentOrderList())
  4. confirmPublisher.publish(command.getOrderId())   // KafkaTemplate 대신 포트 사용
  5. return PaymentConfirmAsyncResult(ASYNC_202, orderId, amount)

OutboxConfirmAdapter 오케스트레이션:
  1. paymentLoadUseCase.getPaymentEventByOrderId(command.getOrderId())
  2. paymentCommandUseCase.executePayment(paymentEvent, command.getPaymentKey())
  3. transactionCoordinator.executeStockDecreaseWithOutboxCreation(command.getOrderId(), paymentEvent.getPaymentOrderList())
  4. return PaymentConfirmAsyncResult(ASYNC_202, orderId, amount)

From src/main/java/.../payment/presentation/port/PaymentConfirmService.java:
  public interface PaymentConfirmService {
      PaymentConfirmAsyncResult confirm(PaymentConfirmCommand paymentConfirmCommand)
              throws PaymentOrderedProductStockException;
  }

From src/main/java/.../payment/application/dto/response/PaymentConfirmAsyncResult.java:
  @Builder @Getter
  // fields: orderId, amount, responseType
  // enum ResponseType { SYNC_200, ASYNC_202 }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KafkaAsyncConfirmService 생성 + 테스트 완성</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmService.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmServiceTest.java
  </files>
  <behavior>
    - confirm() 호출 시 paymentCommandUseCase.executePayment()를 1회 호출한다
    - confirm() 호출 시 transactionCoordinator.executeStockDecreaseOnly()를 1회 호출한다
    - confirm() 호출 시 confirmPublisher.publish(orderId)를 1회 호출한다
    - confirm() 결과의 responseType은 ASYNC_202, orderId와 amount가 command와 동일하다
    - @ConditionalOnProperty(name="spring.payment.async-strategy", havingValue="kafka", matchIfMissing=false)
  </behavior>
  <action>
    1. KafkaAsyncConfirmService 작성:
       - 패키지: com.hyoguoo.paymentplatform.payment.application
       - @Slf4j, @Service, @RequiredArgsConstructor
       - @ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "kafka")
       - implements PaymentConfirmService
       - 필드: PaymentTransactionCoordinator, PaymentLoadUseCase, PaymentCommandUseCase, PaymentConfirmPublisherPort
       - confirm() 오케스트레이션 순서: load → executePayment → executeStockDecreaseOnly → publish → return ASYNC_202
       - LogFmt.info() 로깅: 기존 KafkaConfirmAdapter 로그 이벤트와 동일하게 유지

    2. KafkaAsyncConfirmServiceTest 완성 (Plan 01에서 생성한 스텁을 완성):
       - FakePaymentConfirmPublisher inner class 사용
       - 기존 KafkaConfirmAdapterTest의 검증 4개를 그대로 이식
       - 클래스 참조만 KafkaConfirmAdapter → KafkaAsyncConfirmService로 변경

    RESEARCH.md Anti-pattern 주의: TOPIC 상수를 이 클래스에 두지 않는다 — KafkaConfirmPublisher에만 존재.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.KafkaAsyncConfirmServiceTest"</automated>
  </verify>
  <done>KafkaAsyncConfirmServiceTest 4개 테스트가 모두 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OutboxAsyncConfirmService 생성 + 테스트 완성</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmService.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmServiceTest.java
  </files>
  <behavior>
    - confirm() 호출 시 paymentCommandUseCase.executePayment()를 1회 호출한다
    - confirm() 호출 시 transactionCoordinator.executeStockDecreaseWithOutboxCreation()를 1회 호출한다
    - confirm() 결과의 responseType은 ASYNC_202다
    - @ConditionalOnProperty(name="spring.payment.async-strategy", havingValue="outbox", matchIfMissing=false)
  </behavior>
  <action>
    1. OutboxAsyncConfirmService 작성:
       - 패키지: com.hyoguoo.paymentplatform.payment.application
       - @Slf4j, @Service, @RequiredArgsConstructor
       - @ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "outbox")
       - implements PaymentConfirmService
       - 필드: PaymentTransactionCoordinator, PaymentLoadUseCase, PaymentCommandUseCase
       - confirm() 오케스트레이션 순서: load → executePayment → executeStockDecreaseWithOutboxCreation → return ASYNC_202

    2. OutboxAsyncConfirmServiceTest 완성:
       - 기존 OutboxConfirmAdapterTest의 검증 4개를 그대로 이식
       - 클래스 참조만 OutboxConfirmAdapter → OutboxAsyncConfirmService로 변경

    PITFALL 방지: Plan 03에서 어댑터를 삭제하기 전에 이 플랜에서 신규 서비스를 먼저 완성.
    @ConditionalOnProperty 중복 Bean 방지: 어댑터는 Plan 03에서 삭제되므로 이 플랜 완료 후 중간에는
    동일 havingValue를 가진 두 Bean이 공존한다. 단, 테스트는 Spring 컨텍스트 없이 순수 단위 테스트이므로 문제없음.
    ./gradlew test 전체 실행은 Plan 03 완료 후에 한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.OutboxAsyncConfirmServiceTest"</automated>
  </verify>
  <done>OutboxAsyncConfirmServiceTest 3개 테스트가 모두 GREEN.</done>
</task>

</tasks>

<verification>
./gradlew test --tests "*.payment.application.*"
</verification>

<success_criteria>
- KafkaAsyncConfirmService.java와 OutboxAsyncConfirmService.java가 application/ 패키지에 존재한다
- 두 서비스 모두 PaymentConfirmService를 implements한다
- @ConditionalOnProperty가 각각 havingValue="kafka", havingValue="outbox"로 선언된다
- ./gradlew test --tests "*.payment.application.*" 통과 (KafkaAsyncConfirmServiceTest + OutboxAsyncConfirmServiceTest GREEN)
</success_criteria>

<output>
완료 후 `.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-02-SUMMARY.md` 생성
</output>
