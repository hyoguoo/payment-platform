---
phase: 08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/port/out/PaymentConfirmPublisherPort.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/kafka/KafkaConfirmPublisher.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmServiceTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmServiceTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/kafka/KafkaConfirmPublisherTest.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "PaymentConfirmPublisherPort 인터페이스가 application/port/out/ 패키지에 존재한다"
    - "KafkaConfirmPublisher가 PaymentConfirmPublisherPort를 구현하고 'payment-confirm' 토픽에 발행한다"
    - "Wave 0 테스트 스텁 3종이 컴파일되고 @Disabled 없이 RED 상태로 실행된다"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/port/out/PaymentConfirmPublisherPort.java"
      provides: "Kafka 발행 포트 인터페이스"
      exports: ["PaymentConfirmPublisherPort"]
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/kafka/KafkaConfirmPublisher.java"
      provides: "KafkaTemplate 래핑 구현체"
      exports: ["KafkaConfirmPublisher"]
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmServiceTest.java"
      provides: "KafkaAsyncConfirmService 단위 테스트 스텁"
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmServiceTest.java"
      provides: "OutboxAsyncConfirmService 단위 테스트 스텁"
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/kafka/KafkaConfirmPublisherTest.java"
      provides: "KafkaConfirmPublisher 단위 테스트"
  key_links:
    - from: "src/main/java/.../application/KafkaAsyncConfirmService.java (Plan 02에서 생성)"
      to: "src/main/java/.../application/port/out/PaymentConfirmPublisherPort.java"
      via: "private final 필드 주입"
    - from: "src/main/java/.../infrastructure/kafka/KafkaConfirmPublisher.java"
      to: "org.springframework.kafka.core.KafkaTemplate"
      via: "kafkaTemplate.send(TOPIC, orderId, orderId)"
---

<objective>
Kafka 발행 포트 인터페이스(`PaymentConfirmPublisherPort`)와 구현체(`KafkaConfirmPublisher`)를 생성하고,
Wave 0 테스트 스텁 3종을 작성해 이후 구현 플랜들이 의존하는 계약과 테스트 프레임을 확립한다.

Purpose: KafkaTemplate 직접 의존을 포트 뒤로 숨기는 것이 Phase 8 핵심 포트 추상화 작업이다.
         테스트 스텁을 먼저 만들어 Nyquist 규칙을 충족한다.
Output: PaymentConfirmPublisherPort 인터페이스, KafkaConfirmPublisher 구현체, 테스트 스텁 3종
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-CONTEXT.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-RESEARCH.md

<interfaces>
<!-- 기존 포트 패턴 — application/port/ 바로 아래에 위치 (참고용) -->
From src/main/java/.../payment/application/port/PaymentGatewayPort.java:
  public interface PaymentGatewayPort { ... }

From src/main/java/.../payment/presentation/port/PaymentConfirmService.java:
  public interface PaymentConfirmService {
      PaymentConfirmAsyncResult confirm(PaymentConfirmCommand paymentConfirmCommand)
              throws PaymentOrderedProductStockException;
  }

From src/main/java/.../payment/application/dto/response/PaymentConfirmAsyncResult.java:
  // @Builder, @Getter
  // orderId: String, amount: BigDecimal, responseType: ResponseType
  // enum ResponseType { SYNC_200, ASYNC_202 }

From src/main/java/.../payment/infrastructure/kafka/KafkaConfirmListener.java:
  // 동일 패키지 — KafkaConfirmPublisher와 공존

<!-- KafkaConfirmAdapter에서 추출한 토픽 상수 및 kafkaTemplate 호출 패턴 -->
From KafkaConfirmAdapter (삭제 예정):
  private static final String TOPIC = "payment-confirm";
  kafkaTemplate.send(TOPIC, command.getOrderId(), command.getOrderId());
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 테스트 스텁 3종 생성</name>
  <files>
    src/test/java/com/hyoguoo/paymentplatform/payment/application/KafkaAsyncConfirmServiceTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/OutboxAsyncConfirmServiceTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/kafka/KafkaConfirmPublisherTest.java
  </files>
  <behavior>
    KafkaAsyncConfirmServiceTest:
    - Test 1: confirm() 호출 시 confirmPublisher.publish(orderId)를 1회 호출한다
    - Test 2: confirm() 호출 시 transactionCoordinator.executeStockDecreaseOnly()를 1회 호출한다
    - Test 3: confirm() 결과의 responseType은 ASYNC_202다
    - Test 4: KafkaAsyncConfirmService에 @ConditionalOnProperty(havingValue="kafka", matchIfMissing=false)가 선언되어 있다

    OutboxAsyncConfirmServiceTest:
    - Test 1: confirm() 호출 시 transactionCoordinator.executeStockDecreaseWithOutboxCreation()를 1회 호출한다
    - Test 2: confirm() 결과의 responseType은 ASYNC_202다
    - Test 3: OutboxAsyncConfirmService에 @ConditionalOnProperty(havingValue="outbox", matchIfMissing=false)가 선언되어 있다

    KafkaConfirmPublisherTest:
    - Test 1: publish(orderId) 호출 시 kafkaTemplate.send("payment-confirm", orderId, orderId)를 1회 호출한다
  </behavior>
  <action>
    세 테스트 파일을 작성한다. KafkaAsyncConfirmService와 OutboxAsyncConfirmService는 Plan 02에서 생성되므로,
    이 시점에서 컴파일 오류가 발생한다. 테스트 파일만 작성하고 컴파일 오류는 Plan 02 이후 해소된다.

    - KafkaAsyncConfirmServiceTest: 패키지 `com.hyoguoo.paymentplatform.payment.application`
      - mockTransactionCoordinator, mockPaymentLoadUseCase, mockPaymentCommandUseCase, fakeConfirmPublisher(FakePaymentConfirmPublisher) 필드
      - FakePaymentConfirmPublisher는 테스트 파일 내 private static inner class로 구현:
        implements PaymentConfirmPublisherPort, List<String> publishedOrderIds 유지
      - @BeforeEach: KafkaAsyncConfirmService 생성자 주입 (4개 필드)
      - createPaymentEvent() helper: PaymentEvent.allArgsBuilder()로 생성

    - OutboxAsyncConfirmServiceTest: 패키지 `com.hyoguoo.paymentplatform.payment.application`
      - mockTransactionCoordinator, mockPaymentLoadUseCase, mockPaymentCommandUseCase 필드
      - @BeforeEach: OutboxAsyncConfirmService 생성자 주입 (3개 필드)

    - KafkaConfirmPublisherTest: 패키지 `com.hyoguoo.paymentplatform.payment.infrastructure.kafka`
      - @SuppressWarnings("unchecked") KafkaTemplate<String, String> mockKafkaTemplate
      - KafkaConfirmPublisher 생성자 주입

    TESTING.md 패턴 준수: Fake > Mock 원칙 → KafkaConfirmPublisherPort는 Fake 구현 사용.
    CONVENTIONS.md 준수: @DisplayName 한글 사용.
  </action>
  <verify>
    <automated>MISSING — Task 2에서 PaymentConfirmPublisherPort가 생성된 후 컴파일 가능.
    Task 2 완료 후: ./gradlew test --tests "*.KafkaConfirmPublisherTest" -x integrationTest</automated>
  </verify>
  <done>세 테스트 파일이 생성되어 있다. PaymentConfirmPublisherPort 생성 후 컴파일 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaymentConfirmPublisherPort 인터페이스 + KafkaConfirmPublisher 구현체 생성</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/application/port/out/PaymentConfirmPublisherPort.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/kafka/KafkaConfirmPublisher.java
  </files>
  <behavior>
    - PaymentConfirmPublisherPort.publish(String orderId) 호출 시 KafkaException 전파
    - KafkaConfirmPublisher.publish(orderId) → kafkaTemplate.send("payment-confirm", orderId, orderId) 1회 호출
  </behavior>
  <action>
    1. `application/port/out/` 디렉토리 생성 후 PaymentConfirmPublisherPort 작성:
       ```java
       package com.hyoguoo.paymentplatform.payment.application.port.out;

       public interface PaymentConfirmPublisherPort {
           void publish(String orderId);  // 실패 시 KafkaException 전파
       }
       ```

    2. `infrastructure/kafka/KafkaConfirmPublisher` 작성:
       - @Component, @RequiredArgsConstructor
       - private static final String TOPIC = "payment-confirm";
       - private final KafkaTemplate<String, String> kafkaTemplate;
       - publish(): kafkaTemplate.send(TOPIC, orderId, orderId)
       - TOPIC 상수는 이 클래스에만 존재 — KafkaAsyncConfirmService에 두지 않는다 (RESEARCH.md Anti-pattern)

    CONVENTIONS.md: @RequiredArgsConstructor + private final, 클래스 레벨 @Slf4j 불필요 (단순 위임)
    포트 패키지 위치: CONTEXT.md에서 application/port/out/ 명시
  </action>
  <verify>
    <automated>./gradlew test --tests "*.KafkaConfirmPublisherTest"</automated>
  </verify>
  <done>
    KafkaConfirmPublisherTest가 GREEN. PaymentConfirmPublisherPort가 컴파일되고 KafkaConfirmPublisher가 구현.
    KafkaAsyncConfirmServiceTest, OutboxAsyncConfirmServiceTest는 아직 컴파일 오류 (Plan 02에서 해소).
  </done>
</task>

</tasks>

<verification>
./gradlew test --tests "*.KafkaConfirmPublisherTest"
</verification>

<success_criteria>
- PaymentConfirmPublisherPort 인터페이스 파일이 application/port/out/ 패키지에 존재한다
- KafkaConfirmPublisher가 GREEN 테스트를 통과한다
- Wave 0 테스트 스텁 3종이 파일로 존재한다 (KafkaAsyncConfirmServiceTest, OutboxAsyncConfirmServiceTest 컴파일 오류는 허용)
</success_criteria>

<output>
완료 후 `.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-01-SUMMARY.md` 생성
</output>
