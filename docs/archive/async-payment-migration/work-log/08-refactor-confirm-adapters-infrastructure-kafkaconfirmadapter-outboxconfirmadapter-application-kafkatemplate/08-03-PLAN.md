---
phase: 08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - src/main/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImpl.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImplTest.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapter.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapter.java
  - src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapter.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapterTest.java
  - src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapterTest.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "PaymentConfirmServiceImpl이 PaymentConfirmService를 직접 implements한다"
    - "PaymentConfirmServiceImpl.confirm()이 PaymentConfirmAsyncResult(SYNC_200)를 반환한다"
    - "PaymentConfirmServiceImpl에 @ConditionalOnProperty(havingValue='sync', matchIfMissing=true)가 선언된다"
    - "infrastructure/adapter/ 패키지에 클래스 파일이 하나도 없다"
    - "./gradlew test가 전체 통과한다 (KafkaConfirmListenerIntegrationTest 포함)"
  artifacts:
    - path: "src/main/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImpl.java"
      provides: "Sync 전략 서비스 (PaymentConfirmService 구현체)"
      contains: "implements PaymentConfirmService"
    - path: "src/test/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImplTest.java"
      provides: "PaymentConfirmServiceImpl 단위 테스트 (반환 타입 변경 반영)"
  key_links:
    - from: "PaymentConfirmServiceImpl"
      to: "PaymentConfirmService (presentation/port)"
      via: "implements PaymentConfirmService"
      pattern: "implements PaymentConfirmService"
    - from: "Spring ApplicationContext"
      to: "PaymentConfirmService Bean"
      via: "@ConditionalOnProperty (sync/outbox/kafka 각각 하나만 활성)"
---

<objective>
SyncConfirmAdapter를 제거하고 PaymentConfirmServiceImpl이 PaymentConfirmService를 직접 implements하도록 변경한다.
기존 adapter 3종(KafkaConfirmAdapter, OutboxConfirmAdapter, SyncConfirmAdapter)과 해당 테스트를 모두 삭제한다.
infrastructure/adapter/ 패키지가 완전히 비워져야 하며, 전체 테스트 스위트가 GREEN이어야 한다.

Purpose: 레이어 역전 해소 완료. infrastructure 레이어에 PaymentConfirmService 구현체가 없어야 한다.
Output: 어댑터 3종 삭제, PaymentConfirmServiceImpl 수정, 전체 테스트 GREEN
</objective>

<execution_context>
@/Users/hyoguoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyoguoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-CONTEXT.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-RESEARCH.md
@.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-02-SUMMARY.md

<interfaces>
<!-- PaymentConfirmServiceImpl 현재 상태 — 변경 대상 -->
현재 PaymentConfirmServiceImpl:
  - @Slf4j, @Service, @RequiredArgsConstructor
  - implements 없음 (PaymentConfirmService를 implements하지 않음)
  - public PaymentConfirmResult confirm(PaymentConfirmCommand) — PaymentConfirmResult 반환
  - 필드: PaymentLoadUseCase, PaymentTransactionCoordinator, PaymentCommandUseCase, PaymentFailureUseCase

변경 후 PaymentConfirmServiceImpl:
  - @ConditionalOnProperty(name="spring.payment.async-strategy", havingValue="sync", matchIfMissing=true) 추가
  - implements PaymentConfirmService 추가
  - public PaymentConfirmAsyncResult confirm(PaymentConfirmCommand) — PaymentConfirmAsyncResult 반환
  - 기존 confirm() 로직을 private doConfirm(PaymentConfirmCommand)으로 추출
  - public confirm()은 doConfirm() 결과를 PaymentConfirmAsyncResult(SYNC_200)으로 래핑하여 반환

<!-- PaymentConfirmService 인터페이스 -->
From presentation/port/PaymentConfirmService.java:
  PaymentConfirmAsyncResult confirm(PaymentConfirmCommand) throws PaymentOrderedProductStockException;

<!-- SyncConfirmAdapter 래핑 패턴 (참고 — 이 패턴을 ServiceImpl에 흡수) -->
SyncConfirmAdapter.confirm():
  PaymentConfirmResult result = paymentConfirmServiceImpl.confirm(paymentConfirmCommand);
  return PaymentConfirmAsyncResult.builder()
      .responseType(ResponseType.SYNC_200)
      .orderId(result.getOrderId())
      .amount(result.getAmount())
      .build();
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentConfirmServiceImpl 리팩터 + 테스트 업데이트</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImpl.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/application/PaymentConfirmServiceImplTest.java
  </files>
  <behavior>
    - confirm() 반환 타입이 PaymentConfirmAsyncResult다
    - confirm() 결과의 responseType은 SYNC_200이다
    - @ConditionalOnProperty(name="spring.payment.async-strategy", havingValue="sync", matchIfMissing=true)가 선언된다
    - 기존 내부 로직(재고 감소, Toss API 호출, 실패 처리)이 private doConfirm()으로 그대로 유지된다
  </behavior>
  <action>
    PaymentConfirmServiceImpl 수정:
    1. 클래스 선언에 @ConditionalOnProperty 추가:
       @ConditionalOnProperty(name = "spring.payment.async-strategy", havingValue = "sync", matchIfMissing = true)
    2. 클래스 선언에 implements PaymentConfirmService 추가
    3. 기존 public confirm(PaymentConfirmCommand) 메서드를 private doConfirm(PaymentConfirmCommand)으로 리네임
       - 반환 타입: PaymentConfirmResult 유지
       - 기존 로직 전체 이동 (LogFmt, try-catch 블록, executeStockDecreaseWithJobCreation 등 변경 없음)
    4. 새 public confirm(PaymentConfirmCommand) 메서드 추가:
       - @Override
       - 반환 타입: PaymentConfirmAsyncResult
       - throws PaymentOrderedProductStockException
       - 내부: doConfirm(command) 호출 → SYNC_200으로 래핑 반환

    PaymentConfirmServiceImplTest 수정:
    - confirm() 반환 타입이 PaymentConfirmResult → PaymentConfirmAsyncResult로 변경되므로
      기존 result.getOrderId(), result.getAmount() assert를 유지하되 타입 수정
    - 테스트 메서드 시그니처에서 PaymentConfirmResult → PaymentConfirmAsyncResult로 변경
    - @ConditionalOnProperty 검증 테스트 추가:
      PaymentConfirmServiceImpl.class.getAnnotation(ConditionalOnProperty.class)로
      havingValue="sync", matchIfMissing=true 검증

    PITFALL 방지 (RESEARCH.md Pitfall 1):
    - doConfirm()은 PaymentTossConfirmException을 던질 수 있고, PaymentConfirmService 인터페이스는
      PaymentOrderedProductStockException만 선언한다. public confirm()에서 별도 try-catch 필요 없음
      — doConfirm()이 던지는 RuntimeException들은 unchecked이므로 인터페이스 선언에 없어도 전파됨.
  </action>
  <verify>
    <automated>./gradlew test --tests "*.PaymentConfirmServiceImplTest"</automated>
  </verify>
  <done>PaymentConfirmServiceImplTest가 모두 GREEN. confirm() 반환 타입이 PaymentConfirmAsyncResult.</done>
</task>

<task type="auto">
  <name>Task 2: 어댑터 3종 + 테스트 삭제 및 전체 테스트 스위트 검증</name>
  <files>
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapter.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapter.java,
    src/main/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapter.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/KafkaConfirmAdapterTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/OutboxConfirmAdapterTest.java,
    src/test/java/com/hyoguoo/paymentplatform/payment/infrastructure/adapter/SyncConfirmAdapterTest.java
  </files>
  <action>
    1. 파일 삭제 (6개):
       - infrastructure/adapter/KafkaConfirmAdapter.java 삭제
       - infrastructure/adapter/OutboxConfirmAdapter.java 삭제
       - infrastructure/adapter/SyncConfirmAdapter.java 삭제
       - test/.../infrastructure/adapter/KafkaConfirmAdapterTest.java 삭제
       - test/.../infrastructure/adapter/OutboxConfirmAdapterTest.java 삭제
       - test/.../infrastructure/adapter/SyncConfirmAdapterTest.java 삭제

    2. infrastructure/adapter/ 디렉토리가 비어있으면 삭제한다
       (Java 패키지 빈 디렉토리는 컴파일에 영향 없지만 CONTEXT.md "빈 패키지 → 제거" 요구사항 충족)

    3. 전체 테스트 스위트 실행:
       ./gradlew test

    PITFALL 방지 (RESEARCH.md Pitfall 2):
    - production 클래스와 테스트 파일을 동시에 삭제해 "package does not exist" 컴파일 오류 방지
    PITFALL 방지 (RESEARCH.md Pitfall 3):
    - Plan 02에서 KafkaAsyncConfirmService(havingValue="kafka")와 OutboxAsyncConfirmService(havingValue="outbox")가 이미 생성되어 있고,
      이 플랜에서 KafkaConfirmAdapter/OutboxConfirmAdapter를 삭제하므로 @ConditionalOnProperty 중복 Bean 없음.

    KafkaConfirmListenerIntegrationTest 회귀 확인:
    - KafkaConfirmListener는 이 Phase 범위 밖 (변경 없음)이지만, KafkaConfirmPublisher가 추가되어
      Kafka 통합 테스트 컨텍스트에서 올바르게 Bean이 로드되는지 확인 필요
    - 만약 KafkaConfirmListenerIntegrationTest가 실패한다면 application-test.yml 또는 @TestConfiguration에
      KafkaConfirmPublisher Bean이 누락된 경우임 — KafkaTemplate이 이미 등록되어 있으면 자동 주입됨
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>
    - infrastructure/adapter/ 패키지에 Java 파일이 없다
    - ./gradlew test 전체 통과 (KafkaConfirmListenerIntegrationTest 포함)
    - Spring 컨텍스트에서 PaymentConfirmService Bean이 전략별로 하나만 활성화된다
  </done>
</task>

</tasks>

<verification>
./gradlew test
</verification>

<success_criteria>
- infrastructure/adapter/ 패키지에 Java 클래스 파일이 없다
- PaymentConfirmServiceImpl이 PaymentConfirmService를 implements한다
- PaymentConfirmServiceImpl.confirm()의 반환 타입이 PaymentConfirmAsyncResult다
- PaymentConfirmServiceImplTest를 포함한 전체 테스트 스위트가 GREEN이다
- application/ 패키지에 KafkaAsyncConfirmService, OutboxAsyncConfirmService, PaymentConfirmServiceImpl 세 전략 구현체가 있다
</success_criteria>

<output>
완료 후 `.planning/phases/08-refactor-confirm-adapters-infrastructure-kafkaconfirmadapter-outboxconfirmadapter-application-kafkatemplate/08-03-SUMMARY.md` 생성
</output>
