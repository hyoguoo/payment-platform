spring:
  application:
    name: payment-platform

  threads:
    virtual:
      enabled: true

  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 25
        order_inserts: true
        order_updates: true

  output:
    ansi:
      enabled: always

  myapp:
    nicepay:
      client-key: ${NICEPAY_CLIENT_KEY:S2_af2a1ef3b59d42eda4b1e57992f67c20}
      secret-key: ${NICEPAY_SECRET_KEY:18d78a3b09e54948a19f0e2e5e4e4e25}
      api-url: ${NICEPAY_API_URL:https://sandbox-api.nicepay.co.kr}

springdoc:
  packages-to-scan: com.hyoguoo.paymentplatform
  default-produces-media-type: application/json
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  cache:
    disabled: true

spring.profiles.default: local

payment:
  retry:
    max-attempts: 5
    backoff-type: FIXED
    base-delay-ms: 5000
    max-delay-ms: 60000
  idempotency:
    maximum-size: 10000
    expire-after-write-seconds: 10
  gateway:
    type: TOSS
    toss:
      base-url: https://api.tosspayments.com
      secret-key: ${TOSS_SECRET_KEY:}
      connect-timeout: 3000
      read-timeout: 10000

scheduler:
  outbox-worker:
    fixed-delay-ms: 5000
    batch-size: 50
    parallel-enabled: true
    in-flight-timeout-minutes: 5

metrics:
  payment:
    state:
      polling-interval-seconds: 10
    health:
      polling-interval-seconds: 10
      thresholds:
        stuck-in-progress-minutes: 5
        max-retry-count: 5
